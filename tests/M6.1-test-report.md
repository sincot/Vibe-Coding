# M6.1 自动化测试 — 测试报告

> 范围：M6.1「自动化测试」四项要求的测试审查、整理、补足、端到端冒烟回归脚本实现与
> 阶段全量常规回归。**本轮未改动 `src/` 生产代码**，仅新增 `scripts/regression.sh`
> 与文档，并修复脚本自身一处显示缺陷。
>
> 完成时间：2026-09-24。完整日志见 `build/regression-logs/<时间戳>/server.log`
> 与 `/tmp/opencode/m61_ctest.log`（临时）。

---

## 1. 范围与定位

- 判题核心单元测试（输出比对 + 状态边界）：复用并整理既有 gtest 用例，未新建重复体系。
- C++17 / C11 的已知 AC/WA：复用 `judge_integration`、`m34_classification`、`submit_api`，
  并在 `scripts/regression.sh` 中补一条真实 HTTP 端到端验证。
- 认证、权限、持久化、Rejudge、在途恢复：复用既有 CTest 集成测试，本轮全量重跑。
- `scripts/regression.sh`：端到端**冒烟**回归入口（非全量测试入口）。

---

## 2. 执行命令、并发、退出码与日志

| 阶段 | 实际命令 | 并发 | 退出码 | 用时 | 日志位置 |
|---|---|---|---|---|---|
| 构建 | `cmake --build build --parallel 1` | 1 | 0 | 1.52s（增量） | `/tmp/opencode/m61_build.log` |
| 冒烟回归（正常） | `bash scripts/regression.sh` | 串行 | 0 | 约 20s | `build/regression-logs/<ts>/server.log` |
| 冒烟回归（启动失败） | `OJ_JUDGE_WORKSPACE=/tmp/oj-regression-bad-ws bash scripts/regression.sh` | 串行 | 1 | 约 3s | `/tmp/opencode/m61_fail_start.out` |
| 冒烟回归（断言失败） | `OJ_REGRESSION_INJECT_FAILURE=1 bash scripts/regression.sh` | 串行 | 1 | 约 20s | `/tmp/opencode/m61_fail_assert.out` |
| 全量常规回归 | `ctest --test-dir build --parallel 1 --output-on-failure --timeout 120` | 1 | 0 | 241.83s | `/tmp/opencode/m61_ctest.log` |

---

## 3. 测试对象与用例对应表

### 3.1 判题核心单元测试（输出比对 + 状态边界）

被测函数为真实限定名，行号经核实。

| 编号 | 功能 | 被测函数及源码位置 | 测试文件及用例名 | 覆盖方式 |
|---|---|---|---|---|
| T-001 | 完全匹配 | `oj::judge::normalize_output` / `outputs_match`；`src/judge/comparator.cpp:18,50` | `test_judge_unit.cpp` `NormalizeOutput.IdenticalTextUnchanged` | 单元直接 |
| T-002 | 行尾空白/制表符 | 同上 | `NormalizeOutput.TrailingSpacesAndTabsIgnored` | 单元直接 |
| T-003 | `\r\n` 行尾 | 同上 | `NormalizeOutput.CarriageReturnTreatedAsTrailingWhitespace` | 单元直接 |
| T-004 | 文末空行 | 同上 | `NormalizeOutput.TrailingBlankLinesIgnored` | 单元直接 |
| T-005 | 行首空白显著 | 同上 | `NormalizeOutput.LeadingWhitespaceIsSignificant` | 单元直接 |
| T-006 | 行内差异显著 | 同上 | `NormalizeOutput.InternalWhitespaceIsSignificant` | 单元直接 |
| T-007 | 中间空行保留 | 同上 | `NormalizeOutput.MiddleBlankLinesPreserved` | 单元直接 |
| T-008 | 空输出/纯空白 | 同上 | `NormalizeOutput.EmptyAndWhitespaceOnly` | 单元直接 |
| T-009 | 汇总：空集合 / 全 AC / WA / 严重度 | `oj::judge::summarize_cases`；`src/judge/judge.cpp:16` | `test_judge_unit.cpp` `SummarizeCases.{EmptyIsSyserr,AllAcIsAc,WaWinsOverAc,SeverityPrecedence}` | 单元直接 |
| T-010 | 单点分类 AC/WA/CE/TLE/RE/MLE/SYSERR 优先级 | `oj::judge::classify_case`；`src/judge/classification.cpp:9` | `test_judge_classification.cpp` `ClassifyCase.*`（12 项） | 单元直接 |
| T-011 | 缺失内存证据不误判 MLE | 同上 | `ClassifyCase.SignalWithoutMemoryEvidenceIsNotMle` | 单元直接 |
| T-012 | 混合结果/严重度顺序无关 | `summarize_cases` | `test_judge_classification.cpp` `SummarizeCases.*`（5 项） | 单元直接 |
| T-013 | 语言解析 | `oj::judge::parse_language`；`src/judge/executor.cpp:9` | `test_judge_unit.cpp` `ParseLanguage.*` | 单元直接 |
| T-014 | 编排（编译一次/顺序执行/WA 不阻断/部分执行/CE/SYSERR） | `oj::judge::JudgeEngine::judge`；`src/judge/judge.cpp:35` | `test_judge_unit.cpp` `JudgeEngineFake.*`（15 项） | 单元直接（FakeExecutor） |
| T-015 | 全局硬上限/编译超时/取消（部分执行与结果保留） | `oj::judge::JudgeEngine::judge` | `test_judge_deadline.cpp`（10 项） | 单元直接（FakeExecutor） |

> **数量约定说明**：M6.1 原写「判题核心单元测试共 5～10 个用例」。上述直接对应
> 「输出比对 + 状态边界」的核心集合为 T-001～T-009 共 **12 项**（比对 8 + 汇总 4），
> 已超出该区间；连同 T-010～T-015 扩展测试更多。按「不为满足数量而删除有价值的测试
> 或合并独有断言」的要求，全部保留并在此**明确记录数量偏离**，未静默修改需求。

### 3.2 C++17 / C11 已知 AC/WA（真实编译/沙箱/执行/比对/返回链路）

| 编号 | 功能 | 接口入口 / 关键调用链 | 测试文件 | 覆盖方式 |
|---|---|---|---|---|
| T-016 | 真实 g++/gcc 的 C++17/C11 AC/WA | `JudgeEngine` + `LocalExecutor`（真实 fork/exec） | `test_judge_integration.cpp` | 集成直接 |
| T-017 | 默认 ASan/UBSan 模板下 AC/WA/CE 与异常分类 | `LocalExecutor::compile` + `classify_case` | `test_m34_classification.cpp`（`m34_classification`） | 集成直接 |
| T-018 | 经 HTTP 提交并持久化的 AC/WA/CE | `POST /api/problems/{id}/submit` → `SubmitService` → SQLite | `test_submit_api.cpp` | 集成直接 |
| T-019 | 端到端 HTTP：C++17/C11 各 AC/WA + 判题状态断言 | `scripts/regression.sh` | 脚本（真实服务 + 真实沙箱） | 操作/集成 |

### 3.3 既有认证、权限、持久化、Rejudge、在途恢复回归（CTest）

| 编号 | 功能 | 接口入口 / 关键调用链 | 测试文件（CTest 名） | 覆盖方式 |
|---|---|---|---|---|
| T-020 | 注册/账号唯一/并发 | `register` + `RandomAccountGenerator` + SQLite UNIQUE | `register_api` / `register_gtest` | 集成/单元 |
| T-021 | JWT/登录/限速/鉴权/认证边界 | `auth::*`、`LoginService`、`RateLimiter` | `jwt_gtest` / `rate_limit_gtest` / `login_gtest` / `login_api` / `m51_auth_regression` | 单元/集成 |
| T-022 | 改密/首改限制/权限检查 | `PasswordChangeService`、`check_admin` | `password_unit` / `password_api` | 单元/集成 |
| T-023 | 全部管理员入口四类身份矩阵与越权 | `require_admin` + 各管理员接口 | `m52_access_control` | 集成 |
| T-024 | 提交与状态持久化、停止清理 | `SubmitService`、`HttpServer::stop` | `m35_persistence_shutdown` | 集成 |
| T-025 | Rejudge 与统计联动 | `POST /api/admin/submissions/{id}/rejudge` | `rejudge_api` | 集成 |
| T-026 | 在途任务持久化与崩溃恢复 | `InFlightStore`、`RecoveryService` | `m37_in_flight_unit` / `m37_recovery_api` | 单元/集成 |
| T-027 | 管理员题目/用例/用户管理 | 各 `/api/admin/*` | `problem_admin_unit` / `admin_problems_api` / `testcase_admin_unit` / `admin_testcases_api` / `user_admin_unit` / `admin_users_api` | 单元/集成 |
| T-028 | 题目列表/详情/状态/历史/排行 | `GET /api/problems*`、`/api/submissions*`、`/api/status`、`/api/leaderboard` | `problems_unit` / `problems_api` / `problem_list_query_unit` / `problems_list_api` / `problem_owner_status_unit` / `m44_history_unit` / `m44_history_api` / `m45_leaderboard_unit` / `m45_leaderboard_api` | 单元/集成 |
| T-029 | 判题调度/沙箱/异常与安全 | `JudgeManager`、`LocalExecutor`、seccomp/setrlimit | `judge_manager_unit` / `sandbox_unit` / `sandbox_integration` / `m53_judge_abuse` / `m53_service_abuse` | 单元/集成 |

---

## 4. 场景与执行结果表 — `scripts/regression.sh`（23 项断言）

实际结果均为脚本运行时打印的 `[PASS]`，退出码 0。

| 编号 | 测试场景 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|---|
| R-01 | `POST /api/register` 合法昵称/密码 | HTTP 201 | 201 | 通过 |
| R-02 | 注册响应 `account` | 10 位纯数字 | 匹配 `^[0-9]{10}$` | 通过 |
| R-03 | 重复昵称再次注册 | HTTP 409 | 409 | 通过 |
| R-04 | 错误密码登录 | HTTP 401 | 401 | 通过 |
| R-05 | 正确账号/密码登录 | HTTP 200 | 200 | 通过 |
| R-06 | 登录响应 `token` | 非空 | 非空 | 通过 |
| R-07 | 无 token `GET /api/me` | HTTP 401 | 401 | 通过 |
| R-08 | 带 token `GET /api/me` | HTTP 200 | 200 | 通过 |
| R-09 | `/api/me` 账号 | 与登录账号一致 | 一致 | 通过 |
| R-10 | `GET /api/problems` | HTTP 200 | 200 | 通过 |
| R-11 | 列表含种子题 A+B Problem | 找到且 id 为数字 | 找到 | 通过 |
| R-12 | C++17 已知正确程序 | JSON `status=AC` | AC | 通过 |
| R-13 | 上述提交返回 id | 有效数字 | 有效 | 通过 |
| R-14 | C11 已知正确程序 | JSON `status=AC` | AC | 通过 |
| R-15 | 上述提交返回 id | 有效数字 | 有效 | 通过 |
| R-16 | C++17 已知错误程序 | JSON `status=WA` | WA | 通过 |
| R-17 | 上述提交返回 id | 有效数字 | 有效 | 通过 |
| R-18 | C11 已知错误程序 | JSON `status=WA` | WA | 通过 |
| R-19 | 上述提交返回 id | 有效数字 | 有效 | 通过 |
| R-20 | 未登录提交 | HTTP 401 | 401 | 通过 |
| R-21 | 重启后 `GET /api/submissions?mine` | HTTP 200 | 200 | 通过 |
| R-22 | 重启后提交历史 `total` | 4 | 4 | 通过 |
| R-23 | 重启后各提交状态集合 | 2×AC + 2×WA | `AC,AC,WA,WA` | 通过 |

### 4.1 脚本失败路径验证

| 编号 | 测试场景 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|---|
| F-01 | 指定非 tmpfs 工作目录且未允许，服务拒绝启动 | 脚本非零退出、保留诊断、临时数据清理 | 退出码 1；日志含「判题工作目录检查失败…不是 tmpfs」；`/tmp/oj-regression.*` 与 `oj_server` 均无残留 | 通过 |
| F-02 | `OJ_REGRESSION_INJECT_FAILURE=1` 注入必然失败断言 | 脚本非零退出、正确清理 | 退出码 1；汇总「通过：23 失败：1」；无残留 | 通过 |

---

## 5. 场景与执行结果表 — 全量常规回归（49 项 CTest）

`ctest --test-dir build --parallel 1 --output-on-failure --timeout 120`
=> **100% tests passed, 0 tests failed out of 49**（退出码 0，241.83s）。
`ctest -N` 确认 `oj_m53_special`（破坏性类别）未注册进常规回归。

| # | CTest 名 | 结果 | 用时 | # | CTest 名 | 结果 | 用时 |
|---|---|---|---|---|---|---|---|
| 1 | db_integration | 通过 | 1.75s | 26 | submit_scheduling_api | 通过 | 13.57s |
| 2 | register_api | 通过 | 3.95s | 27 | problem_admin_unit | 通过 | 0.01s |
| 3 | register_gtest | 通过 | 3.29s | 28 | admin_problems_api | 通过 | 13.04s |
| 4 | jwt_gtest | 通过 | 0.03s | 29 | testcase_admin_unit | 通过 | 0.00s |
| 5 | rate_limit_gtest | 通过 | 0.01s | 30 | admin_testcases_api | 通过 | 18.00s |
| 6 | login_gtest | 通过 | 4.42s | 31 | problem_list_query_unit | 通过 | 0.00s |
| 7 | login_api | 通过 | 5.30s | 32 | problems_list_api | 通过 | 5.93s |
| 8 | password_unit | 通过 | 5.34s | 33 | user_admin_unit | 通过 | 0.00s |
| 9 | password_api | 通过 | 13.26s | 34 | admin_users_api | 通过 | 20.69s |
| 10 | problems_unit | 通过 | 0.01s | 35 | m35_cleanup_unit | 通过 | 0.00s |
| 11 | problems_api | 通过 | 4.83s | 36 | m35_persistence_shutdown | 通过 | 13.04s |
| 12 | config_unit | 通过 | 0.02s | 37 | rejudge_api | 通过 | 15.66s |
| 13 | project_structure_unit | 通过 | 0.01s | 38 | m37_in_flight_unit | 通过 | 1.13s |
| 14 | http_server_unit | 通过 | 1.01s | 39 | m37_recovery_api | 通过 | 8.10s |
| 15 | static_files_unit | 通过 | 1.83s | 40 | problem_owner_status_unit | 通过 | 0.76s |
| 16 | judge_unit | 通过 | 0.02s | 41 | m43_problem_page_api | 通过 | 3.69s |
| 17 | judge_deadline_unit | 通过 | 2.41s | 42 | m44_history_unit | 通过 | 1.29s |
| 18 | judge_manager_unit | 通过 | 0.12s | 43 | m44_history_api | 通过 | 4.01s |
| 19 | sandbox_unit | 通过 | 0.66s | 44 | m45_leaderboard_unit | 通过 | 1.64s |
| 20 | sandbox_integration | 通过 | 6.98s | 45 | m45_leaderboard_api | 通过 | 1.17s |
| 21 | judge_integration | 通过 | 15.14s | 46 | m51_auth_regression | 通过 | 8.66s |
| 22 | judge_classification_unit | 通过 | 0.00s | 47 | m52_access_control | 通过 | 12.09s |
| 23 | m34_classification | 通过 | 5.50s | 48 | m53_judge_abuse | 通过 | 2.71s |
| 24 | submit_unit | 通过 | 0.00s | 49 | m53_service_abuse | 通过 | 6.18s |
| 25 | submit_api | 通过 | 14.55s | | | | |

---

## 6. 失败项定位与修复记录

| 编号 | 任务/功能 | 失败用例/位置 | 涉及函数（已确认缺陷位置） | 触发输入 | 预期 vs 实际 | 分类 | 根因 | 修复与复测 |
|---|---|---|---|---|---|---|---|---|
| X-01 | M6.1 regression.sh 显示 | `scripts/regression.sh` 工作目录摘要行 | 脚本自身 `echo`（非业务函数） | `OJ_JUDGE_WORKSPACE` 指向非 tmpfs 目录 | 预期准确标注文件系统；实际误标为 `（tmpfs）` | 测试缺陷（脚本显示） | 仅按是否允许非 tmpfs 判断标签，未实际检测 `stat -f %T` | 改为按 `stat` 实测文件系统标注；复测正常路径 23/23、启动失败路径 F-01 输出正确 |

- 本轮**未发现业务实现缺陷**。全量 CTest 首轮即 49/49 通过，无失败断言。
- 未通过删除断言、跳过用例或放宽预期使测试通过。

---

## 7. 资源观察

| 阶段 | 可用内存最低 | I/O 等待最高 | 备注 |
|---|---|---|---|
| 冒烟回归 | 约 855 MiB | 约 1 | 未接近 512/256 MiB 保护线 |
| 全量常规回归 | 约 709 MiB | 约 7 | 构建固定单并发、测试串行；结束后无遗留 `oj_server`/`cc1plus`/判题目录 |

构建方式修正：沿用 `cmake --build build --parallel 1`，未使用无数量 `-j`。

---

## 8. 未验证边界

- 真实图形浏览器渲染与真机窄视口：环境无可用图形浏览器，未执行（前端脚本/DOM 验证为可选，
  位于 `tests/frontend/`，未经 CTest 注册，本轮未执行）。
- 破坏性/压力测试（`oj_m53_special`：持续输出 TLE、受控超内存、有限次进程创建、宿主机内存
  压力）：有意不注册 CTest，本轮按规范保持独立、未执行；环境不满足时不通过关闭安全机制
  获取通过结果。
- 冒烟回归脚本通过只代表该端到端链路通过，**不代表全部功能或整个项目已交付**；M6.2 备份
  恢复、M6.3 部署文档、M6.4 最终验收仍未实现。
- 单元测试以 FakeExecutor 覆盖的编排/分类逻辑，不代表真实编译、资源限制或沙箱已验证；
  后者由 `judge_integration` / `m34_classification` / `sandbox_integration` / `m53_*` 等
  集成测试覆盖。

---

## 9. 复审轮（按 TESTING_GUIDE 独立复跑）

**复审结论**：对 M6.1 交付物（判题核心单元测试、C++17/C11 真实 AC/WA、`scripts/regression.sh`、
既有认证/权限/持久化回归接入）逐项审查并独立复跑，**未发现新的实现缺陷或测试缺陷**，
未改动任何 `src/` 生产代码。上一轮的 X-01（脚本显示缺陷）已修复且复测正常。

本节为同一代码状态的**独立复跑**，不计入第 5 节 CTest 用例数量，也不作为新用例累加。

| 复审项 | 命令 | 并发 | 退出码 | 结果 |
|---|---|---|---|---|
| 增量构建 | `cmake --build build --parallel 1` | 1 | 0 | 1.26s，峰值 RSS 16.9 MiB |
| A 判题核心单元（gtest） | 依次运行 `oj_judge_unit` / `oj_judge_classification_unit` / `oj_judge_deadline_unit` | 串行 | 0 | 29 + 22 + 10 = **61/61 通过** |
| B 真实编译/沙箱/HTTP | `oj_judge_integration` / `oj_m34_classification_test` / `oj_submit_api_test` | 串行 | 0 | 断言 119 + 54 + 156 = **329/329 通过** |
| C 冒烟回归正常路径 | `bash scripts/regression.sh` | 串行 | 0 | **23/23 断言**，无残留 |
| C 冒烟回归启动失败 | `OJ_JUDGE_WORKSPACE=<非 tmpfs> bash scripts/regression.sh` | 串行 | 1 | 明确报错、保留诊断、无残留 |
| C 冒烟回归断言失败 | `OJ_REGRESSION_INJECT_FAILURE=1 bash scripts/regression.sh` | 串行 | 1 | 汇总「通过 23 失败 1」、无残留 |
| D 阶段全量常规回归 | `ctest --test-dir build --parallel 1 --output-on-failure --timeout 120` | 1 | 0 | **49/49 通过**，242.05s |
| 日志保密 | `grep` 保留日志中的密钥/token 特征 | — | — | 无密钥/token 泄露 |

- 资源观察（复审轮）：构建阶段可用内存充裕；全量回归执行期可用内存最低 **644 MiB**、
  I/O 等待最高 **2**；结束后无 `oj_server`/`cc1plus`/判题目录残留。
- 覆盖缺口审查：M6.1 第 5 项（完全匹配/行尾空白/文末空行/空输出/行首空白/行内差异/
  中间空行）与第 6 项（AC/WA/CE/TLE/RE/MLE/SYSERR 边界、混合结果、部分执行、缺失指标）
  在 `test_judge_unit.cpp` + `test_judge_classification.cpp` 中均有有效断言，无缺口需补。
- 未验证边界同第 8 节（真实图形浏览器、`oj_m53_special` 破坏性测试本轮未执行）。

---

## 10. 可选 C++ 覆盖率采集（G6.1，非 M6.1 完成条件）

> 本轮按用户要求额外采集 C++ 覆盖率。**它不属于 SPEC M6.1 的四项要求，不改变 M6.1
> 的完成判定**；工具链为可选的本地安装（免 sudo），不注册 CTest、不进入常规回归。

- 工具：`gcov 11.2` + `gcovr 5.0` + `lcov/genhtml 1.14`。因本机 `sudo` 需密码，改用
  `apt-get download` + `dpkg-deb -x` 免 sudo 解包到 `tests/coverage/.tools/`（脚本
  `tests/coverage/install_tools_local.sh`）。新增脚本：`tests/coverage/run_coverage.sh`。
- 独立覆盖率构建：`build-cov`（`Debug` + `--coverage`，未改 `build/`、未改 `CMakeLists.txt`），
  单并发构建；单测/集成测试串行，另用 instrumented `oj_server` 跑 `regression.sh` 覆盖
  `src/main.cpp` 与服务生命周期。
- 缺陷修复记录（脚本自身，非业务代码）：

  | 编号 | 用例/位置 | 涉及函数（已确认缺陷位置） | 预期 vs 实际 | 分类 | 根因 | 修复与复测 |
  |---|---|---|---|---|---|---|
  | X-02 | `tests/coverage/run_coverage.sh` 配置阶段 | 脚本（无业务函数） | 期望正常配置；实际因 `build-cov` 未创建导致日志重定向失败、退出 1 | 测试缺陷（脚本） | 配置日志写入不存在的目录 | 配置前 `mkdir -p "$BUILD"`；复测退出码 0，报告生成成功 |

- 覆盖率下测试结果：CTest **49/49 通过**（243s）、`regression.sh` **23/23 通过**；
  两次运行结果一致（分支率 50.1%/49.9%，属受控并发时序的正常波动）。
- 覆盖率（本轮实测，`src/`）：

  | 维度 | 覆盖 | 覆盖/总数 |
  |---|---|---|
  | 行 | **78.9%** | 5395 / 6841 |
  | 函数 | **95.8%** | 385 / 402 |
  | 分支 | **49.9%** | 5344 / 10704 |

- 分目录（行 / 函数 / 分支）：`judge` 71.1% / 88.6% / 47.5%；`db` 75.0% / 98.2% / 46.7%；
  `auth` 84.8% / 97.1% / 56.6%；`http` 88.6% / 100% / 50.8%；`problem` 98.7% / 100% / 75.6%；
  `submit` 67.6% / 100% / 36.7%；`user` 97.9% / 100% / 76.2%。
- M6.1 判题核心关键文件：`comparator.cpp` 行 100% / 分支 81.8%（输出比对核心）；
  `classification.cpp` 95.3% / 64.9%；`judge.cpp` 89.5% / 59.9%；`deadline.cpp` 92.9% / 75.0%。
  低覆盖集中在 `local_executor.cpp`（67.1% / 43.6%）与 `sandbox.cpp`（49.9% / 34.5%）：
  fork/seccomp/资源限制/特权错误等分支需 root 或特定命名空间故障场景，受控测试未覆盖，
  属**未验证边界**，不声称“全部分支已覆盖”。
- 报告产物：`build-cov/coverage/index.html`（gcovr，行/分支）、
  `build-cov/coverage/html-lcov/index.html`（lcov，含函数）、`lcov-summary.txt`、`summary.txt`；
  使用说明见 `tests/coverage/README.md`。`build-cov/` 与 `tests/coverage/.tools/` 已加入
  `.gitignore`。
- 资源观察：覆盖率流程（增量）本轮约 5:49，构建/链接峰值 RSS ≈ 524 MiB，执行期可用内存最低
  约 559 MiB（>512 MiB 保护线）。

---

## 11. 第二轮完整重跑与失败记录（环境抖动）

应要求完整重跑一遍：常规构建 → 全量 CTest → `regression.sh`（正常 + 两条失败路径）→
覆盖率流程。结果：

| 步骤 | 结果 |
|---|---|
| `cmake --build build --parallel 1` | 退出码 0，1.69s，峰值 RSS 16.6 MiB |
| 第一次 `ctest --parallel 1`（49 项） | **48/49**，`m51_auth_regression` 失败（见 X-03） |
| 第二次 `ctest --parallel 1`（49 项） | **49/49**，退出码 0，242.6s，可用内存最低 682 MiB |
| `regression.sh` 正常 | 退出码 0，**23/23** |
| `regression.sh` 启动失败 | 退出码 1，诊断保留，无残留 |
| `regression.sh` 断言失败注入 | 退出码 1（通过 23 失败 1），无残留 |
| `tests/coverage/run_coverage.sh` | 退出码 0，5:47；覆盖率下 CTest 49/49、regression 23/23 |

### 失败项 X-03

- 清单编号/任务：T-122 认证回归；测试 `m51_auth_regression`（`tests/integration/test_m51_auth_regression.cpp:295`）。
- 场景：`受控并发失败登录不能突破阈值` —— 20 路并发错误密码登录。
- 预期：恰好 5 次 `401`、其余 15 次 `429`、0 个其它状态。
- 实际：`401=5`（通过）、`429=10`（失败，期望 15）、`other=5`（失败，期望 0）。
- 涉及函数：`oj::auth::RateLimiter`（`src/auth/rate_limit.cpp`）、登录处理
  `HttpServer::handle_login`（`src/http/server.cpp:809`）——**仅为涉及函数，非已确认缺陷位置**。
- 问题分类：**环境/资源（时序抖动，未完全定位）**。该轮执行期采样到一次瞬态可用内存
  485 MiB（低于 512 MiB 保护线，同机另有 opencode 常驻约 1.1 GiB）。
- 已确认证据：隔离下连续复跑该二进制 **3/3 通过且严格 5/15/0**；随后完整重跑 CTest
  **49/49 通过**。限速安全属性未被突破（放行恰为 5 次）。
- 根因：**未完全定位**（疑似共享主机内存压力下 20 路并发客户端出现连接级失败，被测试计入
  `other`）。未发现可复现的产品逻辑缺陷。
- 处理：保留原测试断言不变（不通过放宽预期迎合抖动）；如实记录，第二次完整重跑通过。
  该测试的并发展望受宿主资源影响，后续在低内存时段复现时再定位连接失败具体状态码。
