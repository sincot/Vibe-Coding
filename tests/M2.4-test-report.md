# M2.4 测试与回归报告

- 范围：M2.4 管理员用户接口（`GET /api/admin/users` 用户列表分页查询 + `PUT /api/admin/users` 重置密码 / 修改角色），含统一管理员权限与首改检查、最后管理员保护、重置后首改策略与旧 token 行为。
- 代码状态：新增纯函数 `src/user/admin_user_validation.{h,cpp}`；新增数据访问层 `src/db/user_admin.{h,cpp}`（`UserAdminStore`）；扩展 `HttpServer`（`src/http/server.{h,cpp}`：`GET`/`PUT /api/admin/users` 路由、`handle_admin_list_users`、`handle_admin_update_user`、`admin_user_summary_json`、`user_admin_store_` 成员）；新增单元/集成测试与文档更新。未进入 M2.5，未实现后台页面，未新增删除/封禁/邮件找回等未要求功能，未改数据模型，未改 M1.7 前端。
- 构建方式：`cmake --build build --parallel 1`（单并发）。
- 回归级别：**阶段全量常规回归**（改动涉及 HTTP 路由/权限层、数据访问层与构建系统，执行全部 27 项常规测试）。
- 环境：Linux，4 vCPU，3.3 GiB 无 Swap；隔离临时库（`/tmp`）+ 随机端口真实 HTTP + 可注入 `EchoExecutor`。
- 日志：实施轮构建 `/tmp/opencode/m24_build.log`、单元/集成/回归 `/tmp/opencode/m24_ctest.log`、`/tmp/opencode/m24_api5.log`；首轮审查构建 `/tmp/opencode/m24r_build.log`、单元 `/tmp/opencode/m24r_unit.log`、集成 `/tmp/opencode/m24r_api.log`、全量回归 `/tmp/opencode/m24r_ctest.log`、内存采样 `/tmp/opencode/m24r_mem.log`；复核轮（M2.5 回滚后同代码状态）构建 `/tmp/opencode/m24v2_build.log`、单元 `/tmp/opencode/m24v2_unit.log`、集成 `/tmp/opencode/m24v2_api.log`、全量回归 `/tmp/opencode/m24v2_ctest.log`、内存采样 `/tmp/opencode/m24v2_mem.log`。

## 命令与结果

| 步骤 | 命令 | 并发 | 退出码 | 结果 |
|---|---|---|---|---|
| 构建 | `cmake --build build --parallel 1` | 1 | 0 | 成功 |
| M2.4 单元 | `./build/oj_user_admin_unit`（`ctest -R user_admin_unit`） | — | 0 | 14/14 用例通过 |
| M2.4 集成 | `./build/oj_admin_users_api_test`（`ctest -R admin_users_api`） | 内部受控 | 0 | **14 场景 / 121 check 通过**（复核轮新增并发重置+改角色） |
| 全量回归 | `ctest --test-dir build --parallel 1 --timeout 180 --output-on-failure` | 1 | 0 | 27/27 通过 |

> 复核轮（2026-09-22，M2.5 回滚后与首轮审查同一代码状态）：补充 T-012 并发重置+改角色
> 与 T-001/T-011 边界（空/带空白 action、`user_id:1.0`），复测全部通过；生产代码未改动。

## 表 A：测试对象与用例对应表

| 编号 | 任务/功能 | 被测函数及源码位置 | 测试文件及用例名/检查方式 | 覆盖方式 |
|---|---|---|---|---|
| T-001 | 管理员请求参数解析/校验 | `oj::useradmin::parse_request` `src/user/admin_user_validation.cpp:60` | `ParseRequest.*`（10 用例） | 单元直接；新增 |
| T-002 | 角色枚举校验 | `oj::useradmin::is_valid_role` `admin_user_validation.cpp:56` | `IsValidRole.Enumeration` | 单元直接；新增 |
| T-003 | 列表分页参数校验 | `oj::useradmin::parse_page` `admin_user_validation.cpp:119` | `ParsePage.*`（2 用例） | 单元直接；新增 |
| T-004 | 列表查询、分页、无敏感字段 | `oj::UserAdminStore::list_users` `src/db/user_admin.cpp:22`；`handle_admin_list_users` `src/http/server.cpp:1254` | `test_list_fields_order_and_no_leak`、`test_pagination` | 集成间接；新增 |
| T-005 | 重置密码原子写入 + 标记 | `oj::UserAdminStore::reset_password` `user_admin.cpp:79`；`handle_admin_update_user` `server.cpp:1298` | `test_reset_password_lifecycle`、`test_side_data_unchanged` | 集成间接；新增 |
| T-006 | 改角色（含最后管理员保护） | `oj::UserAdminStore::change_role` `user_admin.cpp:108` | `test_role_change_promote_and_demote`、`test_last_admin_and_self_demotion`、`test_concurrent_role_changes` | 集成间接；新增 |
| T-007 | 权限组合（登录+首改+admin） | `oj::auth::check_admin` `src/auth/authorize.cpp:14`；`HttpServer::require_admin` `server.cpp:664` | `test_permissions`、`test_role_change_promote_and_demote` | 集成间接；新增 |
| T-008 | 越权字段忽略 | `parse_request` 字段白名单 | `test_extra_fields_ignored`、`test_invalid_requests_no_change` | 集成间接；新增 |
| T-009 | 重启持久化 | `UserAdminStore::reset_password`/`change_role` + SQLite | `test_persistence_restart` | 集成间接；新增 |
| T-010 | 查询参数忽略、分页整数倍边界、同角色 no-op、超大请求体 | `handle_admin_list_users`/`handle_admin_update_user`；`UserAdminStore::change_role`；`cpp-httplib` payload 上限 | `test_query_param_and_boundary_edges`（审查轮新增） | 集成间接；新增 |
| T-011 | 请求体类型/数值边界与旧密码无关性 | `parse_request`/`parse_user_id`/`parse_page` | `ParseRequest.NonObjectBodyRejected`（补 null/数字/bool）、`UserIdMustBePositiveInteger`（补 int64 边界、`1.0`）、`ParseRequest.ResetPasswordDoesNotRequireOldPassword`、`ParsePage.DefaultsAndBoundaries`（补纯空白）、`ActionRequiredStringAndKnown`（补空/带空白 action） | 单元直接；补充 |
| T-012 | 并发重置密码 + 改角色（同用户） | `UserAdminStore::reset_password`（单 UPDATE）`user_admin.cpp:78`；`UserAdminStore::change_role`（事务）`user_admin.cpp:107` | `test_concurrent_reset_and_role`（复核轮新增） | 集成间接；新增 |
| T-R1 | 回归·注册/登录/改密/me | `RegisterService`；`LoginService`；`ChangePasswordService`；`authenticate_request` | `register_*`、`login_*`、`password_*`、`jwt_gtest`、`rate_limit_gtest` | 回归；已有 |
| T-R2 | 回归·管理员题目/用例接口 | `ProblemAdminStore`、`TestcaseAdminStore`、`handle_admin_*` | `admin_problems_api`、`admin_testcases_api`、`*_unit` | 回归；已有 |
| T-R3 | 回归·题目查询/提交/判题 | `ProblemStore`、`SubmitService`、`JudgeEngine` | `problems_*`、`submit_*`、`judge_*` | 回归；已有 |
| T-R4 | 回归·其余（DB/HTTP/静态/配置/结构） | 各对应模块 | `db_integration`、`http_server_unit`、`static_files_unit`、`config_unit`、`project_structure_unit` | 回归；已有 |

## 表 B：场景与执行结果表

| 编号 | 测试场景 | 预期结果 | 实际结果及证据 | 状态 |
|---|---|---|---|---|
| S1 | 管理员改密后可获取列表；3 条记录仅含 6 个字段、按 id 升序；响应不含 `password` 字段与真实哈希值；普通用户 role/flag/created_at 正确 | 字段正确、无敏感泄露、稳定排序 | `test_list_fields_order_and_no_leak` 全通过 | 通过 |
| S2 | 22 用户：第 1/2 页 20+2、total=22、total_pages=2、合并 id 1..22 无重漏；page=3/1000000 空列表且 total 不变；page=0/-1/abc/1.5/+1/1000001/9999999 均 400 | 分页稳定、非法参数按约定 | `test_pagination` 全通过 | 通过 |
| S3 | 未登录 401；普通用户查询/修改 403；未改密管理员 403+PASSWORD_CHANGE_REQUIRED；被拒后用户数/哈希/角色不变 | 权限限制与数据库不变 | `test_permissions` 全通过 | 通过 |
| S4 | 重置后旧密码 401、新密码 200、库内为新密码有效 argon2id 哈希、`reset_pwd_flag=1`；成功响应不回显新密码；旧 token 仍可 `GET /api/me` 且看到 flag=1；普通用户提交 403+PASSWORD_CHANGE_REQUIRED；改密后同一 token 提交 AC、flag 清除 | 新旧登录、哈希、标记、提交限制与旧 token 行为正确 | `test_reset_password_lifecycle` 全通过 | 通过 |
| S5 | 前置 403 → 提升后原 token 立即可访问管理员接口；提升+标记=1 时 403 首改；改密后可访问；降级后原 token 403 | 提升/降级按数据库角色实时生效 | `test_role_change_promote_and_demote` 全通过 | 通过 |
| S6 | 空/超长/缺失/类型错误的 `new_password`、非法/缺失 `role`、未知 action、user_id=0、不存在用户、非法 JSON 均返回约定状态码；之后原密码/角色/标记不变 | 非法输入处理且不部分更新 | `test_invalid_requests_no_change` 全通过 | 通过 |
| S7 | `reset_password` 夹带 `role/account/nickname/reset_pwd_flag/password_hash`：仅密码与标记更新；`change_role` 夹带 `new_password/account/reset_pwd_flag`：仅 role 更新 | 不同操作只改对应字段、不能越权 | `test_extra_fields_ignored` 全通过 | 通过 |
| S8 | 用户先 AC 一题，记录提交数/AC/首次 AC/次数；重置密码并提升角色后账号/昵称/注册时间、提交数、AC 状态、首次 AC 时间、提交次数均不变 | 旁路数据不受影响 | `test_side_data_unchanged` 全通过 | 通过 |
| S9 | 唯一管理员自我降级 409 且仍为 admin、仍可访问；提升第二名管理员后自我降级 200、剩 1 名、原 token 403 | 最后管理员保护与自我降级规则 | `test_last_admin_and_self_demotion` 全通过 | 通过 |
| S10 | 2 名管理员并发互降 3 轮：每轮恰好一个 200、另一个 403/409，终态始终恰好 1 名管理员 | 并发保护不可绕过 | `test_concurrent_role_changes` 全通过 | 通过 |
| S11 | 关闭数据库连接后 GET/PUT 均 500，错误体不含 sqlite/SELECT/users | 内部故障隔离且不泄露 | `test_db_failure` 全通过 | 通过 |
| S12 | 同一临时库重启服务：角色=admin、flag=1、新密码哈希可验证、旧密码 401、新密码 200 | 重启后修改保留 | `test_persistence_restart` 全通过 | 通过 |

## 测试审查补充（第二轮）

| 编号 | 测试场景 | 预期结果 | 实际结果及证据 | 状态 |
|---|---|---|---|---|
| S13 | `?page_size=1000&foo=bar&page=1`：客户端参数被忽略；唯一管理员 `admin->admin` no-op；普通用户 `user->user` no-op | `page_size` 恒 20；no-op 返回 200 不改数据 | `test_query_param_and_boundary_edges` 全通过 | 通过 |
| S14 | 直接构造恰好 40 用户：`total_pages=2`、两页各 20、`page=3` 空、跨页 id 1..40 连续；`new_password` 2 MiB 的 PUT 请求体 | 整除边界正确；超大请求体 `413` 且密码/标记不变 | `test_query_param_and_boundary_edges` 全通过（413 后哈希与 flag 未变） | 通过 |
| S15 | 单元：`null`/数字/布尔请求体被拒；`user_id` = int64 最大值合法、int64 最大值+1 被拒；纯空白 `page` 默认 1；重置不要求 `old_password` | 边界行为与文档一致 | `oj_user_admin_unit` 14/14 通过 | 通过 |
| S16 | 3 轮并发对同一用户发起「重置密码」与「改角色」 | 两请求均 200；终态角色为新角色、flag=1、新密码可验证、旧密码失效 | `test_concurrent_reset_and_role` 全通过 | 通过 |
| S17 | 单元补边界：空 action、带空白 action `" reset_password "`、`user_id:1.0` 均被拒 | 严格取值/整数语义 | `ParseRequest.*` 14/14 通过 | 通过 |

## 失败项记录（7.2）

无。

## 汇总与验证边界

- 全量 CTest 测试项 **27**，通过 **27**，失败 **0**，跳过 **0**，均实际执行（复核轮复测）。
- M2.4 单元 gtest：**14 用例执行、14 通过、0 失败、0 跳过**（复核轮扩充断言，用例数不变）。
- M2.4 集成：**14 场景 / 121 条 check，通过 121，失败 0**（复核轮新增并发重置+改角色）。
- 受影响旧功能回归：改动仅在 server 路由/初始化与新增数据层，未修改既有业务函数；全量回归覆盖注册、登录、JWT、限速、本人改密、`/api/me`、管理员题目/用例接口、题目查询、提交与判题、静态托管与配置，全部通过。
- 复核轮资源观察：构建前可用内存约 572 MiB；全量回归期间采样最低 **584 MiB**，未低于 512/256 MiB 保护线，未发生构建/测试交叠，单并发执行。
- 环境观察（**非本阶段引入**）：在内存受限（无 Swap）下，集成测试服务端偶发 `sqlite: out of memory` 的鉴权查询失败（本轮 2 次），均未影响任何断言（后续请求正常）；既有 `admin_testcases_api` 亦出现 4 次。判定为环境/资源瞬时抖动，非实现缺陷；服务端对写入失败不做部分修改。真实峰值内存与覆盖率未采集，不声称全覆盖。
- 失败项记录（7.2）：**无失败项**。复核轮未发现实现缺陷、测试缺陷或构建问题；不需要修复与复测。
- 未验证/边界：
  - 本阶段为后端接口，M2.5 后台页面与 M1.7 前端未改动，真实浏览器渲染未执行（与既有声明一致）。
  - 未新增会话撤销机制，旧 token 在重置/改角色后仍有效至 `exp`（已在 README/SPEC 明确记录，非缺陷）。
  - `list_users` 的 `COUNT` 与 `SELECT` 为两条独立语句、未包在同一只读事务内：并发注册下 `total` 与当前页可能出现瞬时不一致（SPEC 未要求快照一致性），列为已知边界，未修改实现。
  - 未采集 `UserAdminStore`/`admin_user_validation` 的行/分支覆盖率；`handle_admin_update_user` 中「重置不存在用户仍先做一次 argon2 计算再返回 404」为既有实现选择，管理员专用入口，未视为缺陷。
  - 判题器仍为 M1.5 开发环境实现；完整沙箱/线程池/Rejudge 属 M3，未涉及。
