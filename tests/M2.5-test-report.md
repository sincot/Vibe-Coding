# M2.5 测试与回归报告

- 范围：M2.5 后台管理页面（原生前端后台：管理员入口与访问检查 + 题目管理 + 测试用例管理 + 用户管理），无后端接口改动。
- 代码状态：新增 `web/js/pages/admin-common.js`、`admin-problems.js`、`admin-testcases.js`、`admin-users.js`；修改 `web/js/main.js`（后台路由注册）、`web/js/router.js`（`adminOnly` 守卫与无权页面）、`web/js/nav.js`（管理入口/页脚入口）、`web/js/util.js`（模态框/确认框/分页）、`web/index.html`（页脚入口与说明）、`web/css/styles.css`（后台样式）。未引入前端框架/构建，未改后端接口与数据模型，未进入 M3。
- 构建方式：`cmake --build build --parallel 1`（单并发）。
- 回归级别：**阶段全量常规回归**（M2 收尾；改动虽仅前端，但为确认静态托管与既有后端行为不受影响，执行全部 27 项常规测试）。
- 环境：Linux，3.3 GiB 无 Swap；隔离临时库（`/tmp`）+ 随机端口 + 真实静态托管 `web/`；jsdom 24（临时安装于 `/tmp`，不入仓库）。
- 日志：构建 `/tmp/opencode/m25_build.log`、全量回归 `/tmp/opencode/m25_ctest.log`、DOM 验证 `/tmp/opencode/m25_dom.log`。

## 命令与结果

| 步骤 | 命令 | 并发 | 退出码 | 结果 |
|---|---|---|---|---|
| 构建 | `cmake --build build --parallel 1` | 1 | 0 | 成功 |
| 全量回归 | `ctest --test-dir build --parallel 1 --timeout 180 --output-on-failure` | 1 | 0 | 27/27 通过 |
| DOM 页面验证 | `bash /tmp/opencode/domtest/run.sh`（真实服务 + jsdom 执行 `web/js`） | 1 | 0 | **21 场景 / 77 项断言通过，0 失败，无脚本错误** |

> 说明：本阶段为页面开发，验证以真实服务 + DOM 级执行为主；后端接口沿用 M2.1～M2.4，未新增接口测试。

## 表 A：测试对象与用例对应表（DOM 验证）

| 编号 | 任务/功能 | 被测函数及源码位置 | 测试文件及用例名/检查方式 | 覆盖方式 |
|---|---|---|---|---|
| T-001 | 后台路由守卫（身份/角色/首改） | `render` `web/js/router.js`；`renderAdminForbidden` 同文件；`requiresPasswordChange`/`isAdmin` `web/js/auth.js` | S1/S3/S4/S16/S18：`verify.mjs` DOM 检查 hash 与页面文本 | 集成间接；新增 |
| T-002 | 管理入口显示 | `renderNav` `web/js/nav.js` | S4/S18/S21：检查 `#site-nav`、`#footer-admin` 文本 | 集成间接；新增 |
| T-003 | 后台子导航/外壳 | `adminSubNav`/`adminShell` `web/js/pages/admin-common.js` | S5 及各页 `#/admin/...` 可达性 | 集成间接；新增 |
| T-004 | 错误映射与权限撤销处理 | `adminErrorMessage`/`handleRevoked`/`refreshCurrentUser` `admin-common.js` | S17（网络）/S18（403 撤销） | 集成间接；新增 |
| T-005 | 题目列表（分页/可见性筛选） | `renderAdminProblems`/`load` `web/js/pages/admin-problems.js` | S5/S6/S8/S13：列表渲染、`visible=all` 与游客对比 | 集成间接；新增 |
| T-006 | 题目创建表单与校验 | `renderAdminProblemForm`/`buildForm`/`validateProblemForm` 同文件 | S6/S7/S20：字段、单位、样例保存校验 | 集成间接；新增 |
| T-007 | 题目公开/隐藏 | `buildRow` 内 `toggleBtn` 处理器 同文件 | S8：切换后游客/管理员可见范围 | 集成间接；新增 |
| T-008 | 题目删除与冲突 | `buildRow` 内 `deleteBtn` 处理器 同文件 | S13（取消/确认）、S14（409 冲突） | 集成间接；新增 |
| T-009 | 测试用例管理（ord/原样文本） | `renderAdminTestcases`/`buildCaseEditor`/`buildCreateForm` `web/js/pages/admin-testcases.js` | S9（增改删/空白保留）、S10（跨题隔离/快速切换） | 集成间接；新增 |
| T-010 | 用户列表与重置密码 | `renderAdminUsers`/`openResetPasswordDialog` `web/js/pages/admin-users.js` | S11：旧/新密码登录、首改标记 | 集成间接；新增 |
| T-011 | 角色修改与自我降级 | `manageRole`/`afterMutation` 同文件 | S12（提升/降级）、S18（被撤销）、S19（最后管理员）、S21（自我降级） | 集成间接；新增 |
| T-012 | 模态框/确认框/分页 | `openModal`/`confirmDialog`/`pagination` `web/js/util.js` | 上述场景中的确认/取消/分页交互 | 集成间接；新增 |
| T-013 | 文本安全渲染 | `h`/`preBlock`/`setMessage` `web/js/util.js` | S6（题面 XSS）、S15（昵称 XSS） | 集成间接；新增 |
| T-014 | 重复提交与写入失败 | `saving` 守卫、各写操作 catch 分支 | S20 | 集成间接；新增 |
| T-R1 | 回归·公开题目列表与提交链路 | `renderProblems`、`SubmitService` | S2、S14 提交产生；`problems_*`/`submit_*` 回归 | 回归；已有 |
| T-R2 | 回归·全部后端接口与基础设施 | 各后端模块 | `ctest` 27/27 | 回归；已有 |

## 表 B：场景与执行结果表

| 编号 | 测试场景 | 预期结果 | 实际结果及证据 | 状态 |
|---|---|---|---|---|
| S1 | 游客（无 token）访问 `#/admin` | 重定向 `#/login` 且带 `redirect` | hash 匹配、含 `redirect=` | 通过 |
| S2 | 游客访问 `#/problems` | 可渲染题目列表、种子题可见 | 渲染 table 且含 A+B/求和 | 通过 |
| S3 | 普通用户直访 `#/admin` | 显示无权访问，不误判 401、不跳登录 | 文本含「无权访问后台」，hash 不含 login | 通过 |
| S4 | 未改密管理员直访 `#/admin`；完成首次改密 | 引导到 `#/password`；改密后 flag 清除、导航出现管理入口 | hash `#/password` → 改密 → `/api/me` flag=0，导航/页脚入口出现 | 通过 |
| S5 | 管理员进入 `#/admin`、`#/admin/problems` | 后台首页与题目列表可访问 | 页面文本与表格正常 | 通过 |
| S6 | 创建含 HTML 特殊字符题目的题（标签/时限/内存/样例） | 创建成功；字段按约定保存；XSS 按文本渲染 | 列表可查、`difficulty=hard`、tags、`time_limit_ms=1500`、`memory_limit_kb=32768`、样例 `3\n`；无 `img[src=x]` | 通过 |
| S7 | 编辑题目并刷新 | 字段按服务端实际值更新 | 回填标题；标题/难度/可见性经 API 校验一致 | 通过 |
| S8 | 公开→隐藏切换 | 影响普通用户可见范围，管理员仍可见 | 游客列表先有后无；管理员 `visible=all` 仍见 | 通过 |
| S9 | 隐藏用例新增（空输出/空格/换行）→编辑→删除 | 内容原样保存、`ord` 生效、删除后消失、公开详情不含隐藏用例 | 新增 `"  a  b\n\n"`/`""` 原样；编辑为 `"x\n\n  y "`/`"out "`、ord=7；删除后不存在；公开详情不含标记 | 通过 |
| S10 | 题目 A/B 各有用例，快速连续导航 | 只显示当前题（B）用例，不串用 | 快速导航后仅 B 的 `BBB-ONLY`，无 `AAA-ONLY` | 通过 |
| S11 | 用户列表；重置目标用户密码 | 旧密码 401、新密码 200、flag=1 | 三项断言均符合 | 通过 |
| S12 | 角色提升/降级 | 后端角色一致变化 | API 列表 role=admin → user | 通过 |
| S13 | 删除取消 / 确认 | 取消不删除；确认后记录不存在 | GET：取消后 200、确认后 404 | 通过 |
| S14 | 删除已有提交的题目 | 409 冲突并清晰提示，题目保留 | 提交后删除弹「无法删除」，题目仍 200 | 通过 |
| S15 | 含 `<b>` 昵称 | 用户列表按文本显示、不注入元素 | 无 `table b`，文本含 `<b>bad</b>` | 通过 |
| S16 | 无效 token 访问后台 | 清理凭证并跳转登录 | hash `#/login`，`getToken()` 为空 | 通过 |
| S17 | 读取网络失败 | 显示错误、不卡加载、恢复后可继续 | 出现网络错误块、无「加载中」、恢复后表格正常 | 通过 |
| S18 | 使用中权限被其他管理员撤销 | 刷新状态、退出后台、隐藏入口 | hash 回 `#/problems`，导航无管理入口；随后恢复角色 | 通过 |
| S19 | 仅剩一名管理员时自我降级 | 后端 409，前端展示保护提示，角色不变 | 弹「无法修改角色」，`/api/me` 仍 admin | 通过 |
| S20 | 连续点击提交；写入网络失败 | 只创建一条；不假成功、不跳转、保留输入 | 唯一标题仅 1 条；错误提示含网络/无法确认，停留表单页且输入保留 | 通过 |
| S21 | 存在第二名管理员时自我降级 | 成功并立即退出后台、隐藏入口 | hash 回 `#/problems`，导航/页脚入口消失 | 通过 |
| S22 | 控制台检查 | 无脚本错误 / 未处理 Promise 拒绝 | jsdom `jsdomError`/`console.error`/`unhandledRejection` 均为空 | 通过 |

## 缺陷与修复记录（7.2）

| 编号 | 任务/功能 | 失败用例/操作 | 涉及函数及位置 | 预期 vs 实际 | 分类 | 根因与修复 | 复测 |
|---|---|---|---|---|---|---|---|
| D-001 | 题目编辑表单提交 | DOM S7「编辑题目并刷新后校验」 | `buildForm` `web/js/pages/admin-problems.js` | 预期保存成功并返回列表；实际页面提示「id is not defined」，未发起 PUT | 实现缺陷（前端） | `buildForm` 提交分支引用了外层 `id`，但该变量未作为参数传入，触发 `ReferenceError`。修复：`renderAdminProblemForm` 调用 `buildForm(problem, isEdit, id, …)` 并新增形参 `id` | S7 及后续 S8/S15 复测通过 |

> D-001 为页面级开发验证阶段发现的前端实现缺陷；既有后端 gtest/集成测试未覆盖前端，故本轮无既有测试回归失败。

## 汇总与验证边界

- 全量 CTest 测试项 **27**，通过 **27**，失败 **0**，跳过 **0**，均实际执行。
- DOM 页面验证：**21 场景 / 77 项断言执行、77 通过、0 失败、0 跳过**；浏览器脚本错误与未处理拒绝 **0**。
- 受影响旧功能回归：本次改动仅前端 `web/` 与文档，未修改任何后端业务函数、路由或数据模型；全量回归覆盖注册、登录、JWT、限速、改密、题目查询、题目/用例/用户管理接口、提交与判题、静态托管、配置与结构，全部通过，证明后端链路未受影响。
- 未验证项：
  - **真实图形浏览器渲染与手动点击未执行**（环境无 Chrome/Firefox/无头浏览器，二进制下载受限）。DOM 验证在 jsdom 中真实执行 `web/js` 模块并连接真实服务，但不等同于真实浏览器布局与交互。
  - **真机窄屏（375/768px）视觉适配**未执行；已按既有断点（≤900px/≤720px）编写响应式规则，但未在真实设备确认。
  - 未采集前端代码覆盖率（项目无前端测试框架，保持无构建），不声称全分支覆盖。
  - 未新增会话撤销机制：被重置密码或改角色后旧 token 仍有效至 `exp`，但权限按数据库最新值实时生效（S18 已验证）。此为既有约定，非缺陷。
  - 未涉及 M3（Rejudge、判题线程池、沙箱加固）与 M4（提交历史/排行榜/CodeMirror）。

---

## 测试审查轮（第二轮）

- 审查范围：M2.5 全部前端页面，**与实现轮同一代码状态，未修改 `web/` 任何文件**（仅新增测试资产与文档）。
- 依据 `TESTING_GUIDE.md` 核实实现轮 DOM 验证的覆盖缺口并补充：表单校验负路径、列表筛选/空态、用例 `ord` 自动规则、公开样例只读、可见性失败保真、保存中状态、敏感数据不入 localStorage、分页控件、记录不存在处理。
- 新增可重复运行资产：`tests/frontend/run.sh`、`tests/frontend/admin_pages_dom.mjs`（31 场景）、`tests/frontend/README.md`。脚本在隔离临时库 + 随机端口真实启动 `oj_server` 后用 jsdom 执行真实 `web/js`；**不注册 CTest、不引入前端构建或运行依赖**，jsdom 仅经 `JSDOM_DIR` 外部提供。
- 环境：同实现轮；回归前可用内存 635 MiB，执行期间未低于 512/256 MiB 保护线，单并发构建/测试。
- 日志：DOM 审查 `/tmp/opencode/m25_review_dom.log`；全量回归 `/tmp/opencode/m25_review_ctest.log`。

### 命令与结果（审查轮）

| 步骤 | 命令 | 并发 | 退出码 | 结果 |
|---|---|---|---|---|
| DOM 页面验证 | `bash tests/frontend/run.sh` | 1 | 0 | **31 场景 / 115 项断言通过，0 失败，无脚本错误** |
| 全量常规回归 | `ctest --test-dir build --parallel 1 --timeout 180 --output-on-failure` | 1 | 0 | 27/27 通过 |

### 表 A2：审查补充场景与被测函数

| 编号 | 功能 | 被测函数及源码位置 | 场景 | 预期 | 验证方式 |
|---|---|---|---|---|---|
| R1 | 题目表单前端校验 | `validateProblemForm` `web/js/pages/admin-problems.js:570`、`byteLength` `:629` | 空标题、时限/内存越界、重复标签、>20 标签、单标签>30 字节；修正后创建 | 逐项被拒且停留表单页；修正后可创建 | DOM 集成；新增 |
| R2 | 公开样例维护 | `buildForm` `admin-problems.js:386`、`addSampleRow` 同上 | 编辑表单移除/新增样例后整体替换；隐藏用例不受影响 | 样例整体替换、隐藏用例保持 | DOM 集成；新增 |
| R3 | 用例 `ord` 规则 | `parseOrd` `web/js/pages/admin-testcases.js:319`、`buildCreateForm` `:246` | 缺省追加、显式 `ord`、编辑/新增越界 | 缺省=最大+1；显式生效；越界前端拒绝且服务端不变 | DOM 集成；新增 |
| R4 | 列表筛选/空态 | `buildFilters` `admin-problems.js:106`、`normalizeVisibility` `:345`、`load` `:153` | 无结果、仅隐藏、难度 hard | 空态提示；筛选后列表全部匹配 | DOM 集成；新增 |
| R5 | 可见性失败保真 | `buildRow` 可见性处理器 `admin-problems.js:237` | 切换请求网络失败 | 不假成功、状态保持隐藏 | DOM 集成；新增 |
| R6 | 保存中状态 | `buildForm` 提交分支 `:513`、`setBusy` `web/js/util.js:242` | 写入延迟期间重复提交 | 按钮禁用/显示保存中；仅创建一条 | DOM 集成；新增 |
| R7 | 敏感数据不落存储 | `openResetPasswordDialog` `web/js/pages/admin-users.js:203`、`renderAdminTestcases` | 加载隐藏用例、DOM 重置密码后检查 localStorage | 隐藏用例与密码均不在本地存储 | DOM 集成；新增 |
| R8 | 分页控件 | `pagination` `util.js:213`、`load` `admin-problems.js:153`/`admin-users.js:52` | 多页题目翻页；用户单页 | 信息与服务端一致、翻页内容不同、边界按钮禁用 | DOM 集成；新增 |
| R9 | 公开样例只读 | `buildContent` `admin-testcases.js:65`、`buildCaseEditor` `:148` | 样例与隐藏用例并列展示 | 样例只读无写按钮；隐藏用例可编辑 | DOM 集成；新增 |
| R11 | 记录不存在处理 | `renderRequestError` `web/js/pages/admin-common.js:122`、`adminErrorMessage` `:28` | 访问已删除题目的编辑/用例页 | 提示记录不存在、不假成功 | DOM 集成；新增 |

### 表 B2：审查补充场景执行结果

| 编号 | 场景 | 预期结果 | 实际结果及证据 | 状态 |
|---|---|---|---|---|
| R1 | 空标题/时限0/内存0/重复标签/21标签/31字节标签 | 全部被拒并停留新建页；修正后创建 | 6 类负路径均报对应错误且 hash 不变；修正后 API 可查到 | 通过 |
| R2 | 移除 OLD1、新增 NEWSAMPLE 后保存 | 公开样例整体替换；隐藏用例保留 | 详情 samples=[OLD2,NEWSAMPLE]；隐藏用例 `HIDDEN-KEEP` 仍在 | 通过 |
| R3 | 连续新增无 `ord`、显式 `ord=5`、越界 `1000001` | 0、1、5；越界被拒且服务端不变 | 断言均符合（`/tmp/opencode/m25_review_dom.log` 行 108-115） | 通过 |
| R4 | 搜索无结果、visible=0、difficulty=hard | 空态；仅隐藏；仅难 | 三种筛选均返回匹配集合 | 通过 |
| R5 | 网络失败下切换可见性 | 状态保持、不假成功 | badge 仍为「隐藏」 | 通过 |
| R6 | 延迟写入期间二次提交 | 禁用+保存中；仅一条 | disabled=true、文案含「保存中」、仅 1 条记录 | 通过 |
| R7 | 加载隐藏用例 + DOM 重置密码 | localStorage 无隐藏用例/密码 | dump 不含 marker 与 secret | 通过 |
| R8 | 21+ 题目翻页 | 第 1/2 页信息正确、内容不同、上一页可用 | 分页信息与服务端 `total_pages` 一致；p1≠p2；用户单页时下一页禁用 | 通过 |
| R9 | 样例与隐藏用例并列 | 样例只读、隐藏可编辑 | 样例卡片无按钮、隐藏卡片有保存/删除 | 通过 |
| R11 | 已删除题目的编辑/用例页 | 提示不存在 | 均显示「记录不存在，或已被其他操作删除」 | 通过 |

### 失败项定位与修复记录（审查轮，7.2）

审查轮首轮执行 110 项中失败 5 项，全部归因于**测试缺陷（异步竞态/选择器重复）**，未发现实现缺陷；修正测试后复测 115/115 通过。

| 编号 | 场景/用例 | 涉及函数（源码） | 触发与预期 vs 实际 | 分类 | 根因与修复 | 复测 |
|---|---|---|---|---|---|---|
| TR-01 | R3「用例 ord 规则」 | 被测实现 `buildCreateForm`；测试辅助 `addCase` `tests/frontend/admin_pages_dom.mjs` | 连续新增用例时预期 DOM 已重渲染；实际 `q(".case-card.create")` 为 null → `querySelectorAll` 抛错 | 测试缺陷（未等待 DOM 重渲染） | `addCase` 前 `waitFor(.case-card.create)`；编辑前 `waitFor(可编辑卡片≥3)` | 通过 |
| TR-02 | R4「仅隐藏筛选 / 难度筛选」 | 被测实现 `buildFilters`/`load`；测试等待条件 | 点击筛选后立即读列表；实际读到重渲染前的旧列表（含「公开」） | 测试缺陷（异步竞态） | 改为 `pollUntil`：先确认 hash 含筛选值，再等待所有行徽章匹配 | 通过 |
| TR-03 | R8「第二页内容不同 / 上一页可用」 | 被测实现 `pagination`/`load`；测试选择器 | 翻页后立即读取；实际仍为第 1 页，且每行标题与操作各含一个 `/edit` 链接致计数翻倍 | 测试缺陷（竞态 + 选择器重复） | 等待 hash `page=2` 且分页信息「第 2 /」；改为每行首个链接；断言未削弱 | 通过 |

> 实现轮缺陷 D-001（编辑表单提交引用未定义 `id`）已在实现轮修复并复测；审查轮未发现新的实现缺陷。

### 汇总（审查轮）

- 审查轮新增测试：**10 场景 / 38 项断言**（`tests/frontend/`，可重复运行，未注册 CTest）。
- DOM 页面验证（累计）：**31 场景 / 115 项断言执行、115 通过、0 失败、0 跳过**；浏览器脚本错误/未处理拒绝 0。
- 全量常规回归：CTest **27 项执行、27 通过、0 失败、0 跳过**。
- 旧功能回归选择依据：审查轮仅新增测试与文档、未触碰 `web/` 与后端源码，理论影响为零；仍执行全量回归以符合阶段收尾要求，结果与实现轮一致。
- 曾失败并已修复：TR-01/TR-02/TR-03（均为测试缺陷）；无未解决失败。
- 未验证（不变）：真实图形浏览器渲染与手动点击、真机窄屏布局、前端覆盖率；jsdom 验证不等同真实浏览器。
