# M5.2 数据访问与权限回归 测试报告

> 范围：以 SPEC 与已确认接口约定为预期，建立并验证「身份 → 操作 → 数据对象 → 允许/拒绝」
> 访问矩阵，覆盖管理员接口权限、提交归属、题目可见性、历史与可见性变化、隐藏用例、
> 用例归属、非法对象、写入字段控制、删题/删用例、Rejudge 及受控并发的一致性。
> 遵守 `TESTING_GUIDE.md`：单并发构建/测试、隔离数据、资源监控、失败复测。
> 本轮为**测试审查/覆盖补充/回归验证**任务，除新增测试与 `CMakeLists.txt` 注册外，
> **未修改任何业务实现**（仅修复本轮新增测试自身的缺陷，见第 5 节）。

## 1. 任务、功能与实际被测函数

### 1.1 访问矩阵（身份 × 入口）

| 入口 | 游客 | 普通用户本人 | 另一普通用户 | 未完成首改管理员 | 有权限管理员 | 角色被撤销者 |
|---|---|---|---|---|---|---|
| `GET /api/problems` | 仅 visible=1 | 仅 visible=1 + solved | 同普通 | 仅 visible=1（按普通处理） | 全部 + visible 筛选 | 仅 visible=1 |
| `GET /api/problems/{id}` | 隐藏 404 | 隐藏 404 | 同普通 | 隐藏 404 | 可见（含隐藏） | 隐藏 404 |
| `GET /api/problem-tags` | 可见范围标签 | 可见范围标签 | 同普通 | 可见范围标签 | 含隐藏题标签 | 可见范围标签 |
| `POST /api/problems/{id}/submit` | 401 | 本人提交 | 同普通 | 403 须改密 | 可提交含隐藏题 | 登录后可提交 |
| `GET /api/submissions?mine` | 401 | 仅本人 | 仅本人 | 仅本人 | 仅本人 | 仅本人 |
| `GET /api/submissions/{id}` | 401 | 本人 200 | 404 | 404（按普通处理） | 任意记录 200 | 无管理读取他人 |
| `GET /api/status` | 401 | 仅本人 | 仅本人 | 仅本人 | 仅本人 | 仅本人 |
| `GET /api/leaderboard` | 公开 | 公开 | 公开 | 公开 | 公开 | 公开 |
| `POST/PUT/DELETE /api/admin/problems*` | 401 | 403 | 403 | 403 + `PASSWORD_CHANGE_REQUIRED` | 200/201 | 403 |
| `GET/POST/PUT/DELETE /api/admin/problems/{id}/testcases*` | 401 | 403 | 403 | 403 + `PASSWORD_CHANGE_REQUIRED` | 200/201 | 403 |
| `GET/PUT /api/admin/users` | 401 | 403 | 403 | 403 + `PASSWORD_CHANGE_REQUIRED` | 200 | 403 |
| `POST /api/admin/submissions/{id}/rejudge` | 401 | 403 | 403 | 403 + `PASSWORD_CHANGE_REQUIRED` | 200 | 403 |

> 角色被撤销：鉴权每次按 `sub` 回查数据库，旧 token 立即按最新角色判定（T-007 验证）。
> 「本人」指数据对象归属与当前已验证用户一致；管理员读取他人提交/源码见 T-002。

### 1.2 实际被测函数（真实限定名与位置）

| 编号 | 功能 | 关键被测函数及源码位置 |
|---|---|---|
| F1 | 管理员权限组合 | `oj::auth::check_admin` `src/auth/authorize.cpp:14`；`requires_password_change` `:10`；`HttpServer::require_admin` `src/http/server.cpp:968`；`enforce_admin` `:194` |
| F2 | 提交归属/历史 | `HttpServer::handle_submission_list` `server.cpp:1382`；`handle_submission_detail` `:1448`；`SubmissionStore::list_by_user/find_by_id` `src/db/submissions.cpp:111/:60` |
| F3 | 题目可见性 | `HttpServer::handle_problem_list/detail/tags` `server.cpp:1015/1119/1092`；`ProblemStore::query/find_by_id/list_samples/list_tags/viewer_solved` `src/db/problems.cpp` |
| F4 | 提交与在途 | `HttpServer::handle_submit` `server.cpp:1168`；`SubmitService::persist_in_flight/submit` `src/submit/submit.cpp`；`InFlightStore` `src/db/in_flight.cpp` |
| F5 | 用例管理 | `TestcaseAdminStore::create/update/remove` `src/db/testcase_admin.cpp:51/138/196`；`HttpServer::handle_admin_*testcase` `server.cpp:1721-1922` |
| F6 | 删题 | `ProblemAdminStore::remove` `src/db/problem_admin.cpp:188`；`HttpServer::handle_admin_delete_problem` `server.cpp:1685` |
| F7 | Rejudge/统计 | `RejudgeService::rejudge` `src/submit/rejudge.cpp:20`；`UserProblemStatusStore::recompute` `src/db/submissions.cpp:266`；`LeaderboardStore::query` `src/db/leaderboard.cpp` |
| F8 | 用户/角色 | `UserAdminStore::reset_password/change_role` `src/db/user_admin.cpp:79/108`；`HttpServer::handle_admin_update_user` `server.cpp:1968` |
| F9 | 输入解析 | `parse_problem_id` `server.cpp:230`；`parse_page_param` `:332`；`send_error/send_json` `:32/:37`；`svr_.set_payload_max_length` `:502` |

## 2. 新增/修改的测试与注册

- 新增集成：`tests/integration/test_m52_access_control.cpp`（14 场景 / **238 项断言**），
  使用隔离临时库 + 随机端口 + 专用测试密钥 + 可控执行器（`FakeExecutor`/`GatedExecutor`），
  不触碰正式数据库、不执行破坏性压力场景。
- 测试审查轮补充 3 个此前未直接覆盖的边界（共 18 项断言）：**S-001** 在途任务
  `pending→claimed→pending` 各阶段均阻止删题（覆盖「删除 vs 崩溃恢复」关联约束，
  确定性构造）；**S-002** 管理员用例读取返回完整隐藏用例内容与 `is_sample=false`；
  **S-003** 10 个管理员入口携带伪造 Bearer 均 401。审查未发现既有断言存在空断言或恒真断言。
- 修改构建：`CMakeLists.txt` 新增测试目标 `oj_m52_access_control_test` 与 `add_test(NAME m52_access_control ...)`。
- 复用（未改动）既有覆盖：`admin_problems_api`、`admin_testcases_api`、`admin_users_api`、
  `rejudge_api`、`submit_api`、`submit_scheduling_api`、`problems_api`、`problems_list_api`、
  `m44_history_api`、`m45_leaderboard_api`、`m37_recovery_api`、`static_files_unit` 等。
- 前端/页面边界（T-013）复用历史验证：`static_files_unit`（托管越界防护）、
  `tests/frontend/m41_infrastructure_dom.mjs`、`m44_history_dom.mjs`、`admin_pages_dom.mjs`
  （未改 `web/`，按 TESTING_GUIDE 二.8 不无故复跑）。

## 3. 测试对象与用例对应表

| 编号 | 任务/功能 | 被测函数及源码位置 | 测试文件及用例名/检查方式 | 覆盖方式 |
|---|---|---|---|---|
| T-001 | 全部管理员入口四类身份矩阵 | `require_admin`/`enforce_admin`/`check_admin`；10 个 handler | `test_admin_endpoint_permission_matrix` | 集成直接（新增） |
| T-002 | 提交归属与隐私 | `handle_submission_detail/list`；`check_admin` | `test_submission_ownership_privacy` | 集成直接（新增） |
| T-003 | 题目可见性与标签 | `handle_problem_list/detail/tags`；`ProblemStore::list_tags` | `test_problem_visibility_and_tags` | 集成直接（新增；`problem-tags` 系首个 C++ 测试） |
| T-004 | 可见性变化历史保留 | `handle_admin_update_problem`；`handle_user_status`；`handle_submit` | `test_visibility_change_history_retained` | 集成直接（新增） |
| T-005 | 用例归属与字段控制 | `TestcaseAdminStore::create/update/remove` | `test_testcase_ownership_and_field_control` | 集成直接（新增） |
| T-006 | 非法/不存在对象边界 | `parse_problem_id`；`send_error`；`set_payload_max_length` | `test_illegal_ids_and_error_boundaries` | 集成直接（新增） |
| T-007 | 服务端字段控制与权限撤销 | `handle_submit`；`handle_admin_create_problem`；`handle_admin_rejudge`；`check_admin` | `test_write_field_control_and_revocation` | 集成直接（新增） |
| T-008 | 删题关联一致性 | `ProblemAdminStore::remove`；`handle_admin_delete_problem` | `test_delete_problem_consistency` | 集成直接（新增） |
| T-009 | 用例增删改判题一致 | `list_testcases`；`admin_testcase` handlers | `test_testcase_modify_judge_consistency` | 集成直接（新增） |
| T-010 | Rejudge 统计联动 | `RejudgeService::rejudge`；`recompute`；`LeaderboardStore::query` | `test_rejudge_statistics_consistency` | 集成直接（新增；首次经真实 HTTP rejudge 验证排行榜） |
| T-011 | 受控并发 | `handle_submit`；`ProblemAdminStore::remove` | `test_controlled_concurrency` | 集成直接（新增） |
| S-001 | claimed/pending 在途删题保护 | `ProblemAdminStore::remove` `problem_admin.cpp:188`；`in_flight_tasks.state` | `test_delete_problem_consistency` | 集成直接（审查轮新增） |
| S-002 | 管理员用例读取完整性 | `handle_admin_list_testcases` `server.cpp:1721`；`admin_testcase_json` `:302` | `test_testcase_ownership_and_field_control` | 集成直接（审查轮新增） |
| S-003 | 伪造 token 管理员入口拒绝 | `extract_bearer_token`；`authenticate_request`；`require_admin` | `test_admin_endpoint_permission_matrix` | 集成直接（审查轮新增） |
| R-001 | 全量常规回归 | 全部已注册测试 | `ctest --parallel 1`（47 项） | CTest 回归（本轮执行） |

## 4. 场景与执行结果表（实际执行）

### T-001 管理员入口权限矩阵（56 项）

| 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|
| 10 个入口 × 游客 | 401 | 10/10 返回 401 | 通过 |
| 10 个入口 × 普通用户 | 403 | 10/10 返回 403 | 通过 |
| 10 个入口 × 未改密管理员 | 403 + `PASSWORD_CHANGE_REQUIRED` | 10/10 返回 403 且 code 正确 | 通过 |
| 改密后同 token 管理员另 10 个入口 | 201/200 | 10/10 符合 | 通过 |

> 补齐既有覆盖缺口：`PUT /api/admin/problems/{id}` 的未改密管理员、`PUT /api/admin/users` 的游客。

### T-002 提交归属与隐私（18 项）

| 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|
| 他人按 ID 访问提交详情 | 404 且不含源码/隐藏用例 | 404；`SRC_A_MARKER`/`MAGIC_EXPECTED_A`/`SECRET_INPUT_A` 均不出现 | 通过 |
| 历史带 `user_id/account/role` 组合参数 | 仍仅本人，total=0 | 4 组参数均 total=0、空列表 | 通过 |
| 本人访问 | 200 含源码 | 通过 | 通过 |
| 未改密管理员访问他人 | 404（按普通处理） | 通过 | 通过 |
| 改密后管理员访问他人 | 200 含源码 | 通过 | 通过 |

### T-003 题目可见性与标签（24 项）

| 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|
| 游客/普通用户列表 | 不含隐藏、total=1 | 通过 | 通过 |
| 直接按隐藏 ID 取详情 | 404 | 游客/普通均 404 | 通过 |
| `visible=0` + `role/user_id` 伪造 | 不能暴露隐藏、total=1 | 通过 | 通过 |
| `GET /api/problem-tags` | 可见范围标签，无隐藏专用标签 | publictag 出现、hiddentag 不出现 | 通过 |
| 向隐藏题提交 | 404 且不落库/不产生在途 | submissions=0、in_flight=0 | 通过 |
| 管理员 | 可见隐藏题详情与标签 | 通过 | 通过 |

### T-004 可见性变化后的历史保留（15 项）

| 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|
| 转隐藏后题面/新提交 | 404 | 通过 | 通过 |
| 历史提交与次数 | 保留且 `submit_count=1` | 通过 | 通过 |
| 本人历史/详情/状态 | 可读，状态 accepted，不含隐藏用例 | status body 为 `accepted`、count=1；无 `HIST_INPUT` | 通过 |
| 重新公开 | 详情恢复 | 通过 | 通过 |

### T-005 用例归属与字段控制（13 项）

| 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|
| 建用例携带 `problem_id/is_sample/id` | 归属 URL 题目、固定隐藏、忽略 id | 通过 | 通过 |
| A 题路径 + B 题用例 ID 改/删 | 404 且 B 不变 | 通过 | 通过 |
| 隐藏用例改 `is_sample:true` | 仍为隐藏 | 通过 | 通过 |
| 仅传 `is_sample` | 400 | 通过 | 通过 |
| 公开样例经用例接口改 | 404，样例不变 | 通过 | 通过 |

### T-006 非法/不存在对象（26 项）

| 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|
| 题目 ID 溢出/0/-1/小数/十六进制/带号/空格 | 400 | 8/8 通过 | 通过 |
| 提交 ID 与用例 ID 溢出/非法 | 400 | 8/8 通过 | 通过 |
| 不存在提交 | 404 且不含 SQL/路径 | 通过 | 通过 |
| 非法 JSON / 错误类型 | 400 且无部分写入 | 通过 | 通过 |
| >1 MiB 请求体 | 413 且未建题 | 通过 | 通过 |
| 不存在用例 | 404 且原用例不变 | 通过 | 通过 |

### T-007 服务端字段控制与权限撤销（16 项）

| 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|
| 提交携带 `user_id/status/created_at/role` | 归属=调用者、时间为服务端 | 通过 | 通过 |
| 建题携带 `id/seed_key/created_at` | 服务端分配、忽略 | 通过 | 通过 |
| Rejudge 携带源码/语言/归属 | 不采用请求体 | 通过 | 通过 |
| 提升后旧 token | 立即获管理能力 | 通过 | 通过 |
| 降级后旧 token | 403 | 通过 | 通过 |

### T-008 删题一致性（12 项）

| 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|
| 有提交 | 409 且数据完整 | 通过 | 通过 |
| 无提交 | 200，用例/状态清理 | 通过 | 通过 |
| 有未结算在途 | 409；置中断后可删并清残留 | 通过 | 通过 |
| 其它题目/学生提交 | 不受影响 | 通过 | 通过 |

### T-009 用例增删改与判题一致（13 项）

| 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|
| 逐点顺序 | 按 `(ord,id)` | ORD_ZERO→ORD_ONE→SAMPLE | 通过 |
| 改期望输出 | 历史结果不变 | 历史详情仍含旧 `NOPE0` | 通过 |
| 新提交 | 使用新用例 | 改后点 AC、总体仍 WA、不泄露答案 | 通过 |
| 删除用例 | 新提交少一点，历史不变 | 2 点 vs 历史 3 点 | 通过 |
| 公开样例 | 不受隐藏用例变更影响 | 通过 | 通过 |

### T-010 Rejudge 与统计联动（14 项）

| 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|
| 初始 AC | pass_count=1、榜 AC=1、提交=1 | 通过 | 通过 |
| 改用例后重判 | 更新原 ID、结果 WA、不增记录/次数 | 通过 | 通过 |
| 重判后统计 | 通过人数 0、榜 AC 0、提交仍 1、首次 AC=null | 通过 | 通过 |

### T-011 受控并发（13 项）

| 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|
| Rejudge 与新提交并发（编译阶段同时挂起） | 均 200、共 2 条提交、次数 2、通过 1 | 通过 | 通过 |
| 提交在途时删题 | 409 | 通过 | 通过 |
| 结算后删题 | 409（有提交），在途记录已清 | 通过 | 通过 |

### S-001～S-003 测试审查轮补充（18 项）

| 编号 | 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|---|
| S-001 | 在途任务被认领为 `claimed` 时删题 | 409 | 通过 | 通过 |
| S-001 | 重置回 `pending` 时删题 | 409 | 通过 | 通过 |
| S-001 | 在途任务保留、清除后可删且无残留 | 未误删；200 且 in_flight=0 | 通过 | 通过 |
| S-002 | 管理员读取含隐藏用例列表 | 200，含隐藏 `input`、`is_sample=false` | 通过 | 通过 |
| S-003 | 10 个管理员入口携带伪造 Bearer | 全部 401 | 10/10 通过 | 通过 |

## 5. 失败项定位与修复记录

本轮新增测试初次运行 12 项失败，**均为新增测试自身的缺陷，不涉及产品实现**；修正测试后全部通过。

| 清单编号 | 失败用例/文件 | 涉及业务函数 | 触发输入/操作 | 预期 → 实际 | 分类与根因 | 修复与复测 |
|---|---|---|---|---|---|---|
| T-004/T-010 | `test_visibility_change_history_retained`、`test_rejudge_statistics_consistency`、`pass_count`/`leaderboard_field` 辅助 | `handle_user_status`、`LeaderboardStore::query` | 读取 `/api/status`、`/api/leaderboard`、`/api/problems` 后遍历 | 状态 accepted/通过人数 1 → 断言失败 | **测试缺陷**：`for (auto& x : parse_json(res)["..."])` 遍历临时 `json` 的子对象，临时对象在范围初始化语句结束后析构，产生悬垂引用（UB） | 改为先存入具名局部变量再遍历；T-004/T-010 复测通过 |
| T-009 | `test_testcase_modify_judge_consistency` | `ProblemStore::list_testcases`；`JudgeEngine` | 题目含 1 公开样例 + 2 隐藏用例后提交 | 逐点结果 2 项 → 实际 3 项 | **测试缺陷（预期设定错误）**：实现按 `(ord,id)` 对 `testcases` 全表判题，公开样例（`is_sample=1`）同样作为测试点参与判题；SPEC 未禁止 | 修正断言为 3 点并核对顺序/删除后的变化；复测通过（另见第 7 节歧义） |

> 产品实现未发现权限绕过或数据不一致缺陷；未通过删除断言/放宽要求使其通过。

## 6. 旧功能回归

- **选择依据**：本轮仅新增测试与构建注册，未改业务代码；但为满足阶段性与影响范围不确定时的要求，执行覆盖全部已实现功能的**全量常规回归**。
- **命令**：`cmake -S . -B build`（配置，退出码 0）→ `cmake --build build --parallel 1 --target oj_m52_access_control_test`（增量，退出码 0）→ `ctest --test-dir build --parallel 1 --output-on-failure --timeout 240`（退出码 0）。
- **结果**：**47/47 通过，0 失败**（总 231.66s）。M5.2 单项 `m52_access_control` 12.25s（238/238 断言）。
- **关键回归**：`admin_problems_api` 13.04s、`admin_testcases_api` 17.96s、`admin_users_api` 20.69s、`rejudge_api` 15.69s、`submit_api` 14.39s、`submit_scheduling_api` 13.50s、`m44_history_api` 4.02s、`m45_leaderboard_api` 1.19s、`m37_recovery_api` 8.08s、`static_files_unit` 1.88s 全部通过。
- **阶段全量**：属阶段（M5）常规回归，非压力/破坏性测试；压力与沙箱恶意场景留待 M5.3。

## 7. 验证边界、未验证项与歧义

- **删题 vs 崩溃恢复**：审查轮以 S-001 确定性覆盖 `claimed`/`pending` 在途状态下的删题保护与
  清除后清理（`m37_recovery_api` 已覆盖恢复扫描本身）；未构造「运行期恢复线程」并发（恢复为
  启动流程，不引入为测试而改产品代码）。
- **复用历史验证**：T-013 页面/静态资源边界由 `static_files_unit`（M1.7/M2.x）与前端 DOM 脚本覆盖，
  本轮未改 `web/`/静态托管代码，不复跑；真实图形浏览器与真机窄视口仍属未验证项（环境无浏览器）。
- **歧义（保持现状、未擅自修改规则）**：公开样例（`is_sample=1`）是否应作为判题测试点，SPEC 未明确；
  当前实现将题目全部 `testcases` 按 `(ord,id)` 参与判题。因样例内容本就公开，不构成隐藏用例泄露，
  故未改动，仅记录。另沿用 M5.1 待澄清项：`README.md:770` 昵称长度单位标注（实现按字节）。
- 本轮未采集覆盖率，不声称函数/分支全覆蓋；通过仅代表上表场景通过。

## 8. 资源观察

- 构建/测试前可用内存约 **1.0 GiB**（审查轮两次采样 994/977 MiB），高于 512 MiB 保护线；
  全程串行（构建 `--parallel 1`、CTest `--parallel 1`），未出现 OOM、I/O 阻塞或卡顿；
  全量回归 47 项合计 231.66s，M5.2 单项 12.25s。
- 未启动正式服务、未停止正式服务、未修改正式数据库与 `.env`/密钥；全部测试使用 `/tmp` 下
  隔离临时库、随机端口与专用测试密钥，结束即清理。
