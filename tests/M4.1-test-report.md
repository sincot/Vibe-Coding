# M4.1 页面基础设施 — 测试报告

范围：M4.1「页面基础设施」（hash 路由整合、导航/昵称/退出、API 错误处理与 401 跳转、
登录后返回原目标、首次强制改密流程）。改动集中在 `web/` 前端与文档，后端源码未改。

## 一、执行环境与方式

- 服务器：Linux，约 3.3 GiB 内存、无 Swap；执行前后可用内存约 0.5–1.3 GiB，未触及
  512 MiB 保护线；全程单并发、串行执行。
- 增量构建：`cmake --build build --parallel 1`，退出码 0（无源文件变更）。
- jsdom 30.1.1 安装于 `/tmp/opencode/domtest`（缓存命中，非运行依赖）。
- 真实浏览器：`@playwright/cli` 0.1.21（命令 `playwright-cli`）+ Chromium 真实内核，
  在服务器本机以无头方式执行；浏览器缺失的系统库 `libasound.so.2`/`libgbm.so.1`/
  `libwayland-server.so.0` 以 `apt-get download` + `dpkg-deb -x` 解出并 `LD_LIBRARY_PATH`
  指向（无需 root，不影响系统）。Windows 有头运行步骤见
  `tests/frontend/browser/README.md`（本轮未由本环境执行，原因见第五节）。
- 判题工作目录使用真实 tmpfs `/dev/shm`（与 M3.3 一致）。
- 所有前端测试均在隔离临时库 + 随机端口 + 独立 web 静态托管下运行，不触碰 `data/oj.db`。

## 二、测试对象与用例对应表

| 编号 | 任务/功能 | 被测函数及源码位置 | 测试文件及用例名/检查方式 | 覆盖方式 |
|---|---|---|---|---|
| T-01 | 路径规范化 | `normalizePath` web/js/router.js:45 | m41_logic_test.mjs T-01（4 项） | 单元·新增 |
| T-02 | 默认/未知/非法参数路由 | `parseHash` router.js:77、`matchRoute` router.js:94 | T-02（8 项） | 单元·新增 |
| T-03 | 返回目标防开放重定向 | `sanitizeTarget` router.js:129 | T-03（10 项） | 单元·新增 |
| T-04 | 登录后目标权限判定 | `resolvePostAuthTarget` router.js:153 | T-04（5 项） | 单元·新增 |
| T-05 | 认证状态机 | `setAuth/setUser/clearAuth` auth.js:121/132/153 | T-05（8 项） | 单元·新增 |
| T-06 | 会话核实单飞/401/网络失败 | `ensureAuth` session.js:25 | T-06（7 项） | 单元·新增 |
| T-07 | API 错误分类 | `ApiError.is*` api.js:26-58 | T-07（11 项） | 单元·新增 |
| T-08/09 | 401 去重、表单 401 不误清 | `request` api.js:97、`skipAuthRedirect` | T-08/T-09（5 项） | 单元·新增 |
| T-10 | 改密要求去重 | `notifyPasswordChangeRequired` api.js:91 | T-10（2 项） | 单元·新增 |
| T-11 | 页面生命周期 | `createLifecycle` lifecycle.js:12 | T-11（8 项） | 单元·新增 |
| T-12 | 待返回目标存储 | `savePendingTarget/peek/consume` storage.js:11-40 | T-12（3 项） | 单元·新增 |
| T-13 | 退出后旧核实响应不恢复身份 | `ensureAuth` 的 epoch 校验 session.js:47 | T-13（1 项） | 单元·新增·时序 |
| M41-1~11 | M4.1 路由/导航/返回目标/退出/401/过期响应 | `render`、`completePostAuthRedirect`、`performLogout`、`renderNav` | m41_infrastructure_dom.mjs（37 项） | 集成·新增 |
| M41-12 | 快速连续切页只渲染最终页 | `render` 渲染代次 router.js | M41-12（4 项） | 集成·新增·时序 |
| M41-13 | 退出后晚返回请求不恢复页面 | `lifecycle.dispose`+`render` | M41-13（4 项） | 集成·新增·时序 |
| B-01~B-12 | 桌面/窄视口布局、路由、登录/退出、认证失效、权限不足、首改返回 | web/js 各模块 + web/css | browser/m41_browser_scenarios.js（37 项，真实 Chromium） | 集成·新增·真实浏览器 |
| T-40 | M2.5 后台页面旧功能回归 | admin-problems/testcases/users、`api.js`、`nav.js` | admin_pages_dom.mjs（31 场景/128 项） | 集成·旧回归 |
| T-41 | 静态托管/HTTP 后端抽样 | 后端未改 | `static_files_unit`、`http_server_unit`（CTest） | 回归·复用+抽样 |

## 三、场景与执行结果表

| 编号 | 测试场景 | 预期结果 | 实际结果及证据 | 状态 |
|---|---|---|---|---|
| T-01~T-12 | 逻辑 12 组 | 见对应清单 | 72/72 断言通过 | 通过 |
| T-13 | 延迟 `/api/me` 后先退出再放行 | 不恢复身份、token 保持清空 | 1/1 通过 | 通过（修复后复测） |
| M41-1~11 | DOM 11 场景（路由/登录/首改/退出/401/过期响应/单次请求） | 见对应清单 | 37/37 通过 | 通过（含 F-1 修复） |
| M41-12 | 连续 `navigate` 多路由 | 仅渲染最终页、无残留表单 | 4/4 通过 | 通过 |
| M41-13 | 延迟 `/api/admin/users` 中退出并放行 | 请求被取消/忽略、未恢复身份 | 4/4 通过 | 通过（修复后复测） |
| B-01 | 1280×800 题目列表 | 3 行、无横向溢出、游客导航 | 5/5 通过（截图 desktop-1280x800-problems.png） | 通过 |
| B-05 | 1280×800 题目页 | grid 两列、无横向溢出 | 2/2 通过（desktop-1280x800-problem.png） | 通过 |
| B-02/03/04 | 360×640 / 390×844 / 768×1024 | 列表与题面无横向溢出、题面单列 | 各 3/3 通过（narrow-*.png） | 通过 |
| B-06/07 | 游客 `#/admin`→登录；首改保留目标 | redirect 参数；改密后回 `#/admin` | 9/9 通过 | 通过 |
| B-08~B-12 | 普通用户回退、退出清理、认证失效、未知/非法路由、登录后导航 | 见清单 | 16/16 通过 | 通过 |
| T-40 | M2.5 31 场景 | 与既有约定一致（S4 按 M4.1 行为更新） | 128/128 通过 | 通过（含 F-2 修复） |
| T-41 | 2 个 CTest | 通过 | 2/2 通过 | 通过 |

真实浏览器脚本另断言「无未捕获脚本错误 / 未处理拒绝」：**0 项**（预期的 401/403/404
资源加载错误已排除）。

## 四、失败项定位与修复

### F-1：非法参数路由被 not-found 兜底覆盖（实现缺陷，T-21）

- 触发：`#/problems/%E0%A4%A`；涉及 `renderBadAddress`（web/js/router.js:243）。
- 预期「地址参数无效」，实际「页面不存在」；根因是非法参数与未知路由共用 not-found 兜底。
- 修复：非法参数跳过兜底并输出「地址参数无效」。复测 M41-2 通过。

### F-2：M2.5 jsdom harness 未暴露 sessionStorage（测试缺陷，T-40）

- S4 起 20 项级联失败；根因是 M4.1 的 `storage.js` 使用全局 `sessionStorage`，旧 harness
  只绑定了 `localStorage`。
- 修复：`tests/frontend/admin_pages_dom.mjs` 增加 `globalThis.sessionStorage = window.sessionStorage`。
  复测 128/128 通过。
- 已确认需求变化：S4 期望由「改密后 `#/problems`」更新为「回原目标 `#/admin`」（M4.1 第 16 条）。

### F-3：M41-13 测试辅助逻辑产生未处理拒绝（测试缺陷，T-时序）

- 现象：M41-13 报 `unhandledRejection: AbortError`。
- 根因：晚返回时 `resolve(orig(input, init))` 采纳了请求被 abort 后已拒绝的 Promise，
  该拒绝无处理者。
- 修复：以 `orig(...).then(resolve, reject)` 接管底层请求（生产代码无缺陷）。复测 M41-13
  及全套 45/45、逻辑 73/73 通过。

## 五、汇总与验证边界

- 实际执行命令与结果：
  - `cmake --build build --parallel 1`：退出码 0（无重编译）。
  - `node tests/frontend/m41_logic_test.mjs`：73/73 通过（退出码 0）。
  - `bash tests/frontend/run_m41.sh`：45/45 通过（退出码 0）。
  - `bash tests/frontend/browser/run_m41_browser.sh`（无头真实 Chromium）：37/37 通过，
    截图 6 张，退出码 0。
  - `bash tests/frontend/run.sh`（M2.5 回归）：128/128 通过（退出码 0）。
  - `ctest -R 'static_files_unit|http_server_unit' --parallel 1`：2/2 通过。
- 本轮合计执行 285 项断言/检查（逻辑 73 + DOM 45 + 真实浏览器 37 + M2.5 回归 128 +
  CTest 2），通过 285、失败 0（两个失败场景与一个测试辅助缺陷均修复后复测）。
- 新增文件：`tests/frontend/m41_logic_test.mjs`、`m41_infrastructure_dom.mjs`、
  `run_m41.sh`、`browser/m41_browser_scenarios.js`、`browser/run_m41_browser.sh`、
  `browser/run_m41_browser.ps1`、`browser/README.md`；修改 `admin_pages_dom.mjs`、`run.sh`。
- 回归范围：M4.1 仅改 `web/`，后端源码/接口未变；未重跑全量 CTest，按规范复用历史全量
  结果（M3.7：39/39），抽样 `static_files_unit`、`http_server_unit` 通过。
- **真实浏览器执行口径**：本轮由本环境实际执行的是**服务器本机 Linux 无头 Chromium**，
  已覆盖真实渲染、桌面/窄视口布局与全部 M4.1 场景。**Windows 有头浏览器**未由本环境
  执行——本 AI 会话无法操作你本地的 Windows shell / playwright-cli，也不掌握端口转发
  会话；已提交 `run_m41_browser.ps1` 与 README 供你在本地有头复核，属**未由本环境执行**，
  不据此声称 Windows 有头结果。
- 本轮未新增覆盖率采集（按要求）；未采集前端覆盖率，不声称全部分支已覆盖。
- 时序竞态补充了 T-13、M41-12、M41-13 三个关键场景，未做穷举并发。

## 六、加载问题定位（Windows 转发后页面空白/一直加载）

- **服务端有明确超时的请求**：对实际监听端口 8080，`/api/health`、`/`、`/index.html`、
  `/css/styles.css`、`/js/*.js`（含全部页面模块）、`/api/problems` 均返回 `200`，耗时
  < 2ms，MIME 正确（HTML `text/html`、CSS `text/css`、JS `application/javascript`、
  API `application/json`）；`/api/me` 无 token 返回 `401`；未知路径 `404`。服务未卡住。
- **真实浏览器整页加载**（Linux 无头 Chromium，实际 Chromium 内核）：23/23 资源全部
  `200` + 正确 MIME，**0 个 pending 请求、0 条 Console/Page 错误**；无 token 渲染题目
  列表，伪造 token 渲染登录页并清理凭证。M4.1 的身份初始化、`/api/me`、路由守卫与
  `render()` 未出现等待或异常导致的加载停滞。
- **已确认根因**：转发目标当时**没有正在运行的 `oj_server`**（本机无进程、8080 未监听）。
  服务存在两个“启动即退出”的常见原因，均可复现为转发后空白/加载：
  1. 判题工作目录非 tmpfs：未设 `OJ_JUDGE_WORKSPACE` 且 `/opt/oj-tmpfs` 未挂载时，
     启动检查失败，退出码 1；
  2. 首次初始化数据库未设 `OJ_ADMIN_PASSWORD`：初始化失败，退出码 1。
- **修复（针对已确认原因）**：新增 `scripts/run_dev_server.sh`，自动为判题选择可用的
  tmpfs（`/opt/oj-tmpfs` → `/dev/shm` → 显式非 tmpfs 告警），并在首次初始化缺
  `OJ_ADMIN_PASSWORD` 时**直接报错退出**而不是静默失败；启动后打印监听地址与自检命令。
  README 增加启动排错与 `curl /api/health` 自检说明。该改动不涉及权限检查或身份初始化。
- **防御性加固**：`web/js/api.js` 为所有请求增加 15s 超时（`DEFAULT_TIMEOUT_MS`，可被
  `timeoutMs` 覆盖）。后端不可达/连接被接受却无响应时抛出明确超时错误（`timeout:true`,
  `network:true`, `status:0`），页面据此显示失败并可重试，不再永久停在「加载中」；超时
  不清理本地 token（无假登出）。写请求仍按“结果无法确认、不自动重试”处理。
- **原失败场景复测（真实浏览器）**：用请求拦截模拟“连接被接受但永不响应”——
  - `/api/problems` 挂起：约 15.3s 后页面显示「题目加载失败：网络连接失败…重试」，
    不再无限加载；
  - `/api/me` 挂起：约 15.3s 后身份核实失败并按游客处理，页面恢复可操作；
  - 直接探针确认超时分类为 `timeout=true/network=true/status=0`，且 **token 未被清除**。
- **新增用例**：逻辑 T-14（无响应请求按超时返回、超时快速返回、主动取消仍归 aborted），
  逻辑总数 73 → 76；其余 M4.1 DOM 45/45、M2.5 回归 128/128、真实浏览器 37/37 均复测通过。
- **Windows 转发实测（用户手动）**：修复后经 VS Code/SSH 端口转发访问，页面正常显示登录
  界面；输入 `admin`/初始密码后按要求进入改密页；改密成功后进入主页面。该链路
  （游客 → 登录 → 首次强制改密 → 进入主页面）在 Windows 真实浏览器上复现通过，原“空白/
  一直加载”失败场景不再出现。
- **有头模式完整场景（服务器本机）**：以 Xvfb 虚拟显示（1280×800×24）运行
  `playwright-cli open --headed` + 同一 M4.1 场景脚本，真实 Chromium **有头**执行
  **37/37 通过、0 脚本错误**，生成 6 张截图（桌面题目列表/题目页/后台首页、360×640 /
  390×844 / 768×1024 题目页）。截图中文显示为方框仅因服务器未装 CJK 字体，非页面缺陷。
- **未验证项（本轮范围）**：上述有头运行在**服务器虚拟显示**完成，不等同于 Windows 有头；
  Windows 有头完整场景（`run_m41_browser.ps1 -Headed`）仍属可选复核，本环境无法观测其
  截图与 Network 明细。
