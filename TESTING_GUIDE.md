# 阶段测试与回归规范

本文件供 OpenCode 在完成指定开发任务后执行测试审查、补充和回归验证。它规定测试流程和资源约束，业务行为以工程需求文档及已确认的接口约定为准。

核心交付要求：让用户看清 **任务 → 功能 → 实际被测函数 → 测试场景 → 预期与实际结果 → 是否通过**。不能只提供“全部通过”或测试数量。单元测试验证函数或类的方法的行为，集成测试验证多个函数协作后的功能；两者都要明确验证边界。

## 每次使用方式

用户应明确本次任务编号或范围，例如 `M1.4` 或 `M0.1～M1.3`。可发送：

> 请先阅读 @TESTING_GUIDE.md，对已完成的 M1.4 进行测试审查、补充和回归验证。单元测试使用 GoogleTest，源码放在 @tests/unit/。执行前列出任务、功能、真实被测函数和测试场景，执行后逐项报告是否通过；失败时说明涉及函数、预期与实际结果、已确认的原因及复测结果。按规范验证受影响的旧功能，满足条件时执行全量常规回归。遵守单并发和资源保护要求，修复相关缺陷，不继续开发后续功能。展示测试清单后直接继续执行，无需逐项等待确认。

将示例中的任务编号替换为实际范围。如果用户未提供范围且无法从当前上下文确定，先澄清范围，不自行扩大到整个项目。

## 一、测试设计与组织

1. 阅读工程文档、当前实现和已有测试，核实指定范围的实现情况。以需求及接口约定设计用例，覆盖正常路径、边界条件和错误路径，不照搬当前实现作为预期结果。
2. 基于 GoogleTest（gtest）设计并实现单元测试，按被测模块组织，沿用已有目录与命名约定，复用已有测试及公共 fixture，避免重复编写。
3. 必要时使用 mock/fake 隔离外部依赖。真实数据库、HTTP 和子进程链路采用适当的集成测试；文件结构、部署说明等采用操作或配置检查，不强行全部写成 gtest。
4. 单元测试源码放在 `tests/unit/`，集成测试按现有约定区分组织，构建产物留在构建目录。
5. 将新增测试接入现有 CMake/CTest 配置，确认能被发现并执行。
6. 本次新增测试以指定任务为范围；旧功能回归按下节要求扩展到受影响功能，必要时覆盖全部已实现功能。不提前测试尚未实现的模块，不继续开发后续功能。
7. 优先使用可控时钟、随机数接口或 fake 稳定触发过期、碰撞等边界场景，避免长时间等待和依赖随机概率。测试辅助接口不得破坏正式运行逻辑，测试专用路由不得暴露到正式服务。

### 1.1 识别功能及实际被测函数

1. 从实际源码梳理指定任务的功能入口、关键业务函数、类的方法及必要调用关系。使用真实限定名，例如 `命名空间::类名::方法名`；重载函数增加必要参数类型。附源码路径和经核实的行号，不编造函数名或位置。
2. 区分被测业务函数和 gtest 用例名称：前者是项目实现，后者是验证它的 `TestSuite.TestName`。报告中分别列出。
3. 围绕函数的职责检查输入、返回值、异常或错误码，以及数据库、对象状态等副作用。应包含关键正常、边界和错误场景，不能仅调用函数而没有有效断言。
4. 集成测试列出接口入口和关键调用链，并标明间接覆盖。接口测试通过，不等于链路中所有函数的全部分支均已验证；mock/fake 替代的组件也不能因此声称真实实现已通过。
5. 私有辅助函数可以通过公开接口间接验证，不为“一函数一测试”强行暴露内部接口或重构。对未独立测试的关键函数说明间接覆盖方式或覆盖缺口，不机械地为每个简单访问器编写测试。
6. 匿名回调使用所属接口或注册位置定位。文件结构、构建配置等非函数验证标注“配置/操作检查”，不强行填写函数名。

### 1.2 执行前展示测试清单

按任务和功能分组，在对话中展示下表。表格中的占位说明必须替换成从代码和需求取得的真实内容。

| 编号 | 任务/功能 | 被测函数及源码位置 | 场景：前置条件、输入与操作 | 预期结果 | 验证方式及新增/已有回归 |
|---|---|---|---|---|---|
| T-001 | 实际任务与功能 | 真实限定名；路径:行号 | 明确触发条件和动作 | 可断言的返回值或状态变化 | 单元/集成/操作；直接/间接覆盖；新增/回归 |

- 描述具体行为，例如“重复昵称注册应被拒绝，数据库用户数量不增加”，不要只写“注册测试”。预期响应码等具体数值依据实际接口约定确定。
- 同一函数的重要不同场景分别列出；参数化场景可按规则分组，但列明输入类别，执行结果应能追溯到具体用例。
- 清单同时包含必要的旧功能回归。无法执行的项目保留在清单中并说明阻碍，不能悄悄省略。
- 展示后直接继续执行，不将清单展示变成审批步骤。遇到需求歧义时指出问题，并继续不依赖该问题的工作。
- 新发现覆盖缺口时更新清单，保持编号能够对应最终结果。不要为了填表编造用例或扩大到尚未实现的功能。

## 二、旧功能回归要求

每次验证新任务时，必须评估对已完成功能的影响，不能只运行新增测试。

1. 根据改动文件、公共接口和依赖关系，列出直接及间接受影响的旧功能，建立“改动函数/接口 → 受影响旧功能及关键函数 → 对应测试”的简要对应关系，并执行这些已有测试。例如修改数据库层，应检查注册、登录、改密等依赖它的功能。
2. 修改公共模块、数据库结构、认证权限逻辑、配置或构建系统时，扩大回归范围。影响范围无法可靠判断时，在资源允许的情况下执行全部已实现功能的常规自动化测试，不凭猜测省略。
3. 每个开发阶段完成时，执行覆盖全部已实现功能的常规回归。阶段是工程文档中的 M0、M1 等里程碑；单个 M1.4 等小节完成时，默认执行新增测试及受影响回归。用户明确要求全量常规回归时，按其要求执行。
4. 全部常规回归包括已注册的常规单元测试和集成测试，不等于所有测试无差别执行。压力、超内存及其他破坏性测试按专用隔离要求另行执行，不混入日常回归；常规自动化未覆盖的必要操作检查仍需单独验证并报告。
5. 所有回归继续遵守单并发构建、串行测试及资源监控要求。资源不足时可以分批执行并累计结果；仍无法执行的项目明确标为未验证，不能记为通过。
6. 旧测试失败时，先判断是新增改动引入的回归、原有缺陷、环境问题，还是已确认的需求变化。未经依据确认，不删除断言或修改预期值来迎合当前实现。
7. 保留有价值的旧测试供后续任务复用。清理重复测试前比较实际断言、初始化与清理行为，将独有覆盖迁移到保留的测试，并同步更新构建注册和脚本引用，不能仅凭文件名相似删除。
8. 报告旧功能回归的选择依据、覆盖范围、实际结果及未覆盖项。阶段回归或影响范围不确定时进行全量常规验证属于必要验证；同一代码状态已通过的检查，不无故反复执行。

## 三、已知故障与构建修正

制定本规范时，服务器约有 **3.3 GiB 内存、没有 Swap**，与 VS Code Server、OpenCode 共用资源。每次开始前应重新检查实际状态，不能把这些历史数据当作实时数据。

此前执行过以下错误命令，**不得直接复制执行**：

```text
cmake --build build -j 2>&1 | tail -30
```

其中 `2>&1` 是日志重定向，不是并发数量。无数量的 `-j` 在当时的 GNU Make 构建中启用了无限并发，造成可用内存降至约 20 MiB、I/O 等待达到 95%～96%、SSH 断连。终止该构建进程组后，服务器资源恢复。

必须遵守：

1. 开始前检查可用内存、I/O 等待及本项目正在运行或残留的构建、测试任务。资源紧张时先定位原因，不叠加启动任务。
2. 核实进程所属项目、父子关系、当前 PID 和进程组，只处理已确认的本项目任务，不使用历史进程编号，不批量终止所有 node、编译器或其他无关进程。
3. 检查本次真正执行的构建命令，以及项目实际存在的脚本、CMake 配置、Presets、VS Code tasks 和嵌套依赖构建入口。已有入口已验证且未发生相关变化时，可复用检查结果，避免每次重复改写。
4. 当前服务器的开发验证构建并发固定为 **1**，禁止无数量的 `-j`、`-j$(nproc)` 及其他自动提高并发的设置。
5. 将已验证有效的方式记录到现有项目级代理说明或开发说明，可引用本文件，避免复制多份完整规范。不只修改文档，也要检查实际执行命令。
6. 检查与修正完成、资源允许后，继续执行验证，无需仅为常规构建另行等待确认。

## 四、构建与测试执行

1. 沿用实际构建目录和配置，优先增量构建，不无故删除构建目录、全量重编译或重新下载依赖。
2. 构建使用以下模板；将占位符替换为实际路径后执行：

   ```text
   cmake --build <实际构建目录> --parallel 1
   ```

3. 同时只允许一轮项目构建，检查脚本及嵌套依赖是否另行启动并发。
4. 确认构建进程结束且退出码为 0，再运行测试，禁止构建和测试交叠执行。
5. 保存完整构建与测试日志，终端可以只展示摘要。若使用日志管道，启用 Bash `pipefail` 或准确获取构建进程退出码，不能把 `tail` 或 `tee` 成功当作构建成功。
6. 先确认测试已正确注册，再执行本次范围内的测试。若已接入 CTest，使用以下模板：

   ```text
   ctest --test-dir <实际构建目录> --parallel 1 --output-on-failure --timeout 120
   ```

7. 上述 CTest 命令默认运行该目录注册的测试。按本次范围需要，根据实际测试名称或标签添加过滤条件，不编造目标名，也不误启动范围外的压力或破坏性测试。直接运行 gtest 时，使用实际可执行文件路径、合适的过滤条件和外部超时。
8. 单项超时根据测试目的合理设置，不通过反复延长超时掩盖卡死。
9. 检查测试内部是否另有线程、子进程或编译任务，避免外层串行、内部失控并发。
10. 确认测试确实被发现并执行，不能将“没有测试”、构建失败导致未运行或测试跳过记为通过。
11. 取消或超时后，检查测试服务、子进程及临时资源是否已清理。
12. 执行期间按功能组简要说明“正在验证哪个功能及哪些关键函数、已发现什么”，不只输出命令或逐行刷屏日志。计划执行、正在执行和已验证结果必须区分。

## 五、数据隔离与特殊测试

1. 使用独立数据库、临时目录、测试密钥和测试端口，结束后清理，不影响正式数据，不输出真实密码或密钥。
2. 验证 SQLite WAL 时使用临时文件数据库，不能用纯内存数据库替代该项验证。
3. 密码哈希测试串行执行，不削弱正式密码哈希配置。
4. 并发功能采用单独、受控的小规模并发测试。外层串行执行不等于不验证并发行为，但内部并发数量必须明确受控。
5. 超内存、死循环、fork 轰炸等测试，仅在资源限制和隔离已验证有效的专用环境中执行，并确保超时后能清理全部相关子进程。
6. 不因单个测试进程设置了超时，就认定其子进程数量或总内存已经受限。
7. 环境不具备安全执行条件时，保留测试代码并明确列为未验证，不在当前开发服务器上直接尝试。此类测试必须与常规测试隔离，不能被日常回归命令意外执行。

## 六、资源监控与停止条件

1. 串行构建不能保证单个编译任务一定不会耗尽内存。构建前、执行期间及结束后，观察可用内存、主要进程内存及 I/O 等待，分别记录构建和测试阶段的数据。执行期间应有实际监控安排，不能只在结束后检查。
2. 当前服务器暂采用以下保守保护线：
   - 可用内存低于 **512 MiB** 时，不启动新构建或测试。
   - 运行中若连续采样低于 **256 MiB**，或持续出现严重 I/O 阻塞和交互卡顿，主动停止本轮已确认的任务，保留日志并分析，不能只停止启动后续任务。
   - 这些阈值是本机操作保护线，不是通用系统标准，也不能代替硬性资源隔离。瞬时峰值仍可能导致资源耗尽。
3. 发生资源不足后，不自动重复执行相同命令。先定位具体目标、编译单元或测试，再提出调整方案。
4. 不擅自修改 Swap、系统配置、升级服务器或重启服务器。

## 七、缺陷修复与结果交付

1. 发现缺陷时进行必要修复，复测原失败场景及受影响的已有测试，不借机开发后续功能。
2. 优先采用最小必要改动，避免为了测试进行大范围重构。
3. 不通过删除断言、跳过失败用例、放宽业务要求或削弱安全配置让测试通过。
4. 需求不明确或实现与文档冲突时，明确指出，不擅自增加业务规则。
5. 修复后按第二节规定的回归范围复测；满足阶段完成或影响范围不确定等条件时，执行全部常规回归。已通过且未受后续修改影响的结果可复用，不无故重复执行。

### 7.1 逐项测试结果

必须在对话中展示按任务和功能分组的实际结果，而不只是提供日志路径。沿用测试清单编号，采用以下两张关联表，避免单张表过宽：

**测试对象与用例对应表**

| 编号 | 任务/功能 | 被测函数及源码位置 | 测试文件及用例名/检查方式 | 覆盖方式 |
|---|---|---|---|---|
| T-001 | 实际任务与功能 | 真实限定名；路径:行号 | 实际测试文件与 TestSuite.TestName，或操作检查 | 单元直接/集成间接；新增/旧功能回归 |

**场景与执行结果表**

| 编号 | 测试场景 | 预期结果 | 实际结果及证据 | 状态 |
|---|---|---|---|---|
| T-001 | 本次验证的具体输入或行为 | 需求规定的行为 | 实际断言、响应或状态检查结果；必要时引用日志 | 通过/失败/跳过/未执行 |

规则：

1. 结果必须与实际执行证据一致。可以依据通过的断言说明它验证了什么，但不能把未检查的行为写成观察结果。
2. 同一函数有通过和失败的场景时分别列出，不能笼统写“该函数通过”。“通过”只说明列出的场景通过，不证明该函数所有行为都正确。
3. 构建失败时写明“构建失败，相关测试未执行”；超时、资源不足、环境异常需注明，不直接等同于业务逻辑缺陷。进程中途退出且无法确认执行结果的用例不得推定通过。
4. 通过的参数化用例可以分组，但应列明输入类别和实际数量；失败项必须单独展开。旧功能回归同样提供对应关系和结果，不能只有一个总数。
5. 复用此前有效结果时标注“复用历史验证”，说明对应代码状态及未受本次修改影响的依据，不将其计入本轮实际执行数量。
6. 若结果很多，完整逐项表保存为项目中的 Markdown 报告，并在对话中展示各任务的功能、关键函数、场景通过/失败/未验证数量及全部失败项，提供完整报告路径。不能仅交付机器日志或一句“全部通过”。

### 7.2 失败项定位与修复记录

每个失败项单独记录：

- 清单编号、所属任务和功能。
- 失败测试的完整用例名及测试文件位置。
- 涉及的业务函数及源码位置。
- 触发输入、前置条件和操作。
- 预期结果与实际结果。
- 失败断言、错误信息或关键日志位置。
- 问题分类：实现缺陷、测试缺陷、构建问题、环境/资源问题，或尚待定位。
- 已确认的根因和缺陷位置；没有证据时明确标记待确认。
- 必要修复及原场景、受影响旧功能的复测结果。

失败测试调用了某函数，不代表根因一定在该函数。区分“涉及函数”和“已确认缺陷函数/位置”，不能猜测归因。修复后仍保留原失败和修复记录，最终场景结果可标为“通过（修复后复测）”，未解决的失败不得隐藏。

### 7.3 汇总与验证边界

最后汇总：

- 本次任务范围；验证了哪些功能、函数及场景；新增或修改的测试文件。
- 旧功能回归的选择依据、覆盖的旧功能、对应函数和测试；说明本次是影响范围回归还是阶段全量常规回归。
- 实际构建与测试命令、并发设置、退出码及完整日志位置。
- 执行、通过、失败、跳过及未执行的数量；区分 CTest 测试项、gtest 用例和操作检查。重跑不作为新的独立用例累加，历史复用结果单列；无法统计的数量应说明，不能编造。
- 哪些功能/函数的相关测试曾失败、哪些已修复、哪些仍未解决。
- 构建方式的修正和资源观察结果。
- 未验证的功能、关键函数或分支及原因，包括仅有间接覆盖的边界。

未实际采集覆盖率时，不编造函数、行或分支覆盖率，也不声称“全部函数/分支已覆盖”。即使全部已运行测试通过，也不等同于系统没有任何缺陷。

任务状态以实际实现和验证结果为准。只有完成对应验证后，才能将工程文档中的任务标记为完成；因环境或资源限制未执行的验证必须如实保留。
