# SPEC — 仿 LeetCode 在线判题系统（OJ）

> 版本：v0.1  状态：已确认定稿
> 后端：C++（cpp-httplib）  前端：原生 HTML/CSS/JS + CodeMirror  存储：SQLite

---

## 1. 项目概述

一个面向**教学班（5 人同时）**的仿 LeetCode 在线判题系统。发布题目 → 学生浏览/做题 → 在线判题（标准 ACM 模式）→ 保存提交记录与做题状态 → 排行榜。项目由 Vibe-Coding 协作完成。

### 成功标准
- 功能完整、可在一台 Linux 机器上一键运行。
- 核心链路端到端跑通：注册 → 看题 → 提交 → 判题 → 反馈 → 排行榜。
- 判题安全：不可信用户代码在进程级沙箱中执行，恶意提交最多拖垮自身，不影响系统与他人。

---

## 2. 需求规格

### 2.1 用户与权限
| 编号 | 需求 |
|---|---|
| AUTH-01 | 公开注册：系统**随机分配一个 10 位纯数字账号**，该账号一次性分配、绝不重复分配；用户同时自定义唯一昵称与密码 |
| AUTH-02 | `nickname` 全局唯一，重复则注册失败 |
| AUTH-03 | 密码仅存哈希（argon2id/bcrypt），不落明文 |
| AUTH-04 | 会话采用 JWT，前端随请求携带；登录接口做限速防爆破 |
| AUTH-05 | 预置管理员账号（`admin`），首次登录强制改密 |
| ROLE-01 | 普通用户：仅可查看可见题目、做题、查看**本人**提交记录与做题状态标记 |
| ROLE-02 | 管理员：题目增删改查、测试用例管理、Rejudge、用户管理、题目可见性控制；管理员也可做题 |

### 2.2 题库
| 编号 | 需求 |
|---|---|
| PRB-01 | 题目元数据：标题、描述、样例输入/输出、多组测试用例（输入文本/输出文本）、难度（易/中/难）、标签、时限(ms)、内存上限(KB) |
| PRB-02 | 时限/内存上限有默认值（默认 2s / 64MB），按题可覆盖；备注：因 ASan 判题时延显著，默认放宽到 2s 并建议出题者按题校准 |
| PRB-03 | 题面为纯文本（先不做 Markdown 渲染）；测试用例支持文本粘贴逐点录入/编辑/删除 |
| PRB-04 | 题目前端支持搜索 + 难度/标签筛选 |
| PRB-05 | 题目详情只返回样例，隐藏用例绝不随题面下发；提交 **WA** 时，判题反馈向提交者本人返回失败测试点的**输入 / 期望输出 / 用户输出**比对详情（教学复盘用，其余通过点不泄露） |

### 2.3 判题系统（核心）
| 编号 | 需求 |
|---|---|
| JUDGE-01 | 支持语言：C++17（`g++ -O2 -std=c++17`，带 `-lm`）+ ASan/UBSan 全开；C11（`gcc -O2 -std=c11 -lm`）+ ASan/UBSan 全开 |
| JUDGE-02 | 每次提交**创建一个独立子进程**执行，主线程 watchdog 超时 `SIGKILL` 强杀并 `waitpid` 回收，不留僵尸 |
| JUDGE-03 | 沙箱：进程级隔离（fork + `setrlimit`），seccomp-bpf 禁用 `socket()`/`open()` 等危险系统调用（禁网络 / 禁读写文件 / 禁读 /proc） |
| JUDGE-04 | 运行目录：tmpfs 下一次性随机目录，仅本次提交可见，用后即销毁 |
| JUDGE-05 | 输出字节上限 64KB（防刷屏打爆磁盘） |
| JUDGE-06 | AC 判定：去除每行行尾空白与文末空行后逐字符比对 |
| JUDGE-07 | **全部测试点跑完**后汇总反馈，逐测试点结果可见；**WA** 时反馈附失败测试点的输入 / 期望输出 / 用户实际输出 |
| JUDGE-08 | 状态机：`AC` / `WA` / `CE`(编译错误) / `TLE`(超时) / `RE`(运行错误/崩溃) / `MLE`(超内存) / `SYSERR`(系统内部错误) |
| JUDGE-09 | 判题在线程池中执行（worker 数 = min(CPU 核数, 8)），HTTP 层同步返回"
，但提交→判题不串行阻塞 |
| JUDGE-10 | 全局兜底：即使题目时限配置为无限大，单次判题硬上限（如 60s）强杀 |
| JUDGE-11 | 每次提交完整结果（状态、逐点结果、编译信息、耗时/内存）写入持久化日志 |
| JUDGE-12 | 判题进程异常（如编译环境故障）标记为 `SYSERR`，不假死，用户可重试 |

### 2.4 提交与做题记录（全部持久化）
| 编号 | 需求 |
|---|---|
| PERS-01 | `submissions`：提交 ID、用户、题目、语言、**完整用户源码**、状态、逐测试点结果、编译信息、耗时/内存、时间戳 —— 全部落 SQLite |
| PERS-02 | `user_problem_status`：用户×题目 的 AC/未AC 状态、首次 AC 时间、提交次数 —— 供题目列表状态标记与排行榜 |
| PERS-03 | 重启不丢数据；SQLite 开启 WAL |
| PERS-04 | cron 定期 `sqlite3 .dump` 备份（保留在交付清单） |

### 2.5 排行榜
| 编号 | 需求 |
|---|---|
| RANK-01 | 排序：AC 数↓ → 总提交次数↑ → 首次 AC 时间戳↑ → 注册时间↑ |
| RANK-02 | 需要记录并持久化：每人 AC 数、总提交次数、首次 AC 时间戳 |

### 2.6 前端页面与交互
| 编号 | 需求 |
|---|---|
| UI-01 | 页面：登录/注册、题目列表（搜索+筛选）、题目页（详情+做题 左右分屏）、提交详情/历史、排行榜、后台管理（题目/用例/用户/Rejudge） |
| UI-02 | 题目页（详情与做题合并，左右分屏）：**左侧**展示标题、难度、标签、题面描述、样例输入/输出（仅公开样例，隐藏用例绝不泄露）、时限/内存上限、本人该题 AC/未AC 状态；**右侧**为做题区，垂直分栏：CodeMirror 编辑器 + 提交按钮/语言选择 + 逐测试点结果 |
| UI-03 | 做题区细节：CodeMirror 编辑器（C/C++ 语法高亮，CDN 引入），语言选择（C++17/C11），`Ctrl+Enter` 提交，提交后原地分屏展示逐测试点结果（状态/耗时/内存；**WA** 点额外展示输入 / 期望输出 / 你的输出） |
| UI-04 | 前端无构建流程，原生资源由 cpp-httplib 静态托管；hash 路由；CodeMirror 从 CDN 引入 |
| UI-05 | 题目列表对已登录用户显示本人 AC/未AC 状态标记 |
| UI-06 | API 全部走 JSON；权限校验在后端完成（学生只能访问自己提交与可见题目） |

#### 2.6.1 主页（题目列表页）结构

```
┌──────────────────────────────────────────────────────────┐
│  NAV                              ┌─────────┐ ┌────────┐ │
│  Logo   题目  排行榜  提交历史      │ 昵称 ▼ │ │ 退出   │ │
│                                   └─────────┘ └────────┘ │
├──────────────────────────────────────────────────────────┤
│  标题：题目列表                                             │
│  ┌─搜索─────────────────────────┐                          │
│  │ [🔍 输入关键字…           ]    │                          │
│  └──────────────────────────────┘                          │
│  难度：[全部 ▾]   标签：[全部 ▾]     (筛选条件可组合)          │
│  ┌────────────────────────────────────────────────────────┐│
│  │ #   状态   标题             难度   标签        通过人数  ││
│  ├────────────────────────────────────────────────────────┤│
│  │ 1   ✅AC   A+B Problem      易    [入门]       42      ││
│  │ 2   ⬜     排序练习          中    [排序][二分]   18      ││
│  │ 3   ⬜     图的最短路径      难    [图论]         3       ││
│  └────────────────────────────────────────────────────────┘│
│               （点击任意行 → 题目页：左题面右编辑器）                  │
│  每页 20 条        [1] [2] [3] 下一页                          │
├──────────────────────────────────────────────────────────┤
│  Footer：© OJ · 管理员入口（admin 专属）                     │
└──────────────────────────────────────────────────────────┘
```

- 状态列：已登录用户显示 AC（✅）/未AC（⬜），游客不显示（UI-04）
- 筛选栏：搜索 + 难度 + 标签组合筛选（PRB-04），一次请求带 `?q=&difficulty=&tag=`
- 行点击 → 进入题目页（左侧题面+样例，右侧编辑器）
- 分页：每页 20 条，`GET /api/problems` 支持 `page` 参数
- 管理员可在此看到/过滤「仅自己可见」题目
- 未登录访问做题/提交/后台等受保护路由时重定向至登录页，登录成功后跳回原目标页

### 2.7 非功能需求
| 类别 | 要求 |
|---|---|
| 性能 | 教学班 5 人同时提交，单机可扛；同步返回足够快（一个典型提交 < 数秒） |
| 安全 | JWT + 密码哈希；admin 首登强制改密；seccomp 禁网络/文件；tmpfs 目录隔离；登录限速；输出限长 |
| 可靠性 | SYSERR 不假死；子进程必回收；WAL + cron 备份 |
| 日志 | 提交结果全量落库；服务侧运行日志 |
| 可维护 | 判题器抽象 `IExecutor` 接口（sandbox 策略可替换），判题核心可单测 |

---

## 3. 系统架构

### 3.1 架构图

```
┌─────────────────────────────────────────────┐
│  Browser（原生 HTML/CSS/JS + CodeMirror）     │
│  路由：hash；无构建；静态资源由后端托管         │
└──────────────────┬──────────────────────────┘
                   │ HTTP (JSON API + 静态资源)
                   ▼
┌─────────────────────────────────────────────┐
│            cpp-httplib Server                │
│  ┌────────┬────────┬─────────┬────────────┐ │
│  │ Auth   │ Problem│ Submit/ │ Static     │ │
│  │ JWT    │ CRUD   │ History │ Files      │ │
│  │ 鉴权   │ 管理    │ Ranking │ 托管       │ │
│  └───┬────┴────────┴────┬────┴────────────┘ │
│      │                  │                    │
│      │            JudgeManager (提交入口)     │
│      │             有界线程池 min(Ncpu,8)     │
│      │                  │                    │
└──────┼──────────────────┼────────────────────┘
       │                  ▼
       │          ┌─────────────────────────────┐
       │          │     JudgeWorker (每提交1个)  │
       │          │  fork() 子进程 + watchdog    │
       │          │  超时 SIGKILL + waitpid 回收 │
       │          ▼                             │
       │   JudgeRuntime — 进程级沙箱             │
       │    编译 g++/gcc (O2 + ASan/UBSan)      │
       │    运行 setrlimit + seccomp(禁网/文件) │
       │    tmpfs 随机目录 · 输出≤64KB           │
       │    加载隐藏用例 → 逐点比对 → 汇总        │
       └──────────────┬─────────────────────────┘
                      ▼
              ┌───────────────────┐
              │   SQLite oj.db    │
              │  WAL 模式         │
              │  users/problems/  │
              │  testcases/       │
              │  submissions/     │
              │  user_problem_status
              └─────────┬─────────┘
                        │ cron .dump
                        ▼
                 backup/oj-YYYYMMDD.sql
```

### 3.2 数据模型（SQLite）
```
users(id PK, account TEXT UNIQUE /*10位数字*/,
      nickname TEXT UNIQUE, password_hash TEXT,
      role TEXT /*'admin'|'user'*/, reset_pwd_flag INT,
      created_at)
problems(id PK, title, description, difficulty TEXT,
         tags TEXT, time_limit_ms INT, memory_limit_kb INT,
         visible INT, seed_key TEXT /*内置种子题稳定标识，普通题为 NULL*/,
         created_at, updated_at)
testcases(id PK, problem_id FK, ord INT, input TEXT, output TEXT,
          is_sample INT /*1=公开样例 0=隐藏用例*/)
submissions(id PK, user_id FK, problem_id FK, language TEXT,
            source_code TEXT, status TEXT /*AC|WA|CE|TLE|RE|MLE|SYSERR*/,
            per_case TEXT /*JSON 逐点结果，WA 点含输入/期望输出/用户输出*/, compile_msg TEXT,
            runtime_ms INT, memory_kb INT, created_at)
user_problem_status(id PK, user_id FK, problem_id FK,
                    status TEXT /*'accepted'|'none'*/,
                    first_ac_at DATETIME, submit_count INT,
                    UNIQUE(user_id, problem_id))
in_flight_tasks(id PK, task_id TEXT UNIQUE /*持久化任务标识*/,
                user_id FK, problem_id FK, language TEXT,
                source_code TEXT, submitted_at TEXT /*原提交时间*/,
                state TEXT /*'pending'|'claimed'|'interrupted'*/,
                owner TEXT, claimed_at DATETIME, reason TEXT)
```

> `in_flight_tasks`（M3.7）：提交被接收后、最终结算前保存的可恢复在途任务（用户、
> 题目、语言、完整源码、原提交时间与持久化任务标识）。**独立于 `submissions`**，
> 因此结算前不计入 `submit_count`、AC 状态、通过人数与排行榜，既有读取/统计接口
> 无需过滤即可保持兼容。任务完成时在同一结算事务内删除对应行；无法判题时置
> `interrupted` 保留信息。迁移对旧库只新增表（`CREATE TABLE IF NOT EXISTS`），
> 不删表、不清空，已有终态提交不会被识别为待恢复任务。

### 3.3 API 边界
| 方法·路径 | 权限 | 说明 |
|---|---|---|
| `POST /api/register` | 公开 | 分配 10 位账号 + 昵称 + 密码 |
| `POST /api/login` | 公开 | 返回 JWT |
| `GET /api/me` | 登录 | 个人信息 |
| `POST /api/me/password` | 登录 | 改密（admin 首登强制调用） |
| `GET /api/problems` | 公开 | 列表，登录时带本人状态，支持搜索/难度/标签筛选与分页（`q`/`difficulty`/`tag`/`page`，每页 20） |
| `GET /api/problem-tags` | 公开 | 题目标签选项（去重、稳定排序，按当前身份可见范围返回；M4.2 标签筛选的最小读取能力） |
| `GET /api/problems/{id}` | 公开 | 详情（仅含样例） |
| `POST /api/problems/{id}/submit` | 登录 | 提交{language, code}，同步返回判题结果 |
| `GET /api/submissions?mine` | 登录 | 本人提交历史 |
| `GET /api/submissions/{id}` | 本人/管理员 | 提交详情（含源码与逐点结果） |
| `GET /api/status` | 登录 | 本人题目状态标记 |
| `GET /api/leaderboard` | 公开 | 排行榜 |
| `POST /api/admin/problems` | 管理员 | 建题 |
| `PUT/DELETE /api/admin/problems/{id}` | 管理员 | 改/删题 |
| `POST /api/admin/problems/{id}/testcases` | 管理员 | 增用例 |
| `PUT/DELETE /api/admin/problems/{id}/testcases/{tid}` | 管理员 | 改/删用例 |
| `POST /api/admin/submissions/{id}/rejudge` | 管理员 | 重判并联动刷新状态/排行 |
| `GET/PUT /api/admin/users` | 管理员 | 用户列表 / 重置密码·改角色 |

> M3.7 说明（接收边界与删题）：
> - `POST /api/problems/{id}/submit` 的对外语义不变，但接收流程改为「容量预留 →
>   先持久化在途记录 → 入队」；未通过鉴权/参数/可见性/容量检查不产生在途任务，
>   写库失败明确返回 `500` 且不声称已接收。崩溃后可据在途记录恢复。
> - `DELETE /api/admin/problems/{id}` 在原有「已有提交记录拒绝删除」之外，**题目存在
>   未结算在途任务时同样返回 `409`**，避免已接收任务因删题无法保存终态；无提交且无
>   在途任务时连同用例、做题状态、残留中断在途记录一并删除。此为对 M2.1 删除判断的
>   明确扩展，既有「有提交 `409`」语义不变。

### 3.4 项目目录结构

```
Vibe-Coding/
├── SPEC.md                # 需求规格本文件；架构/接口/数据模型的唯一依据
├── dependence.md          # 依赖清单与安装命令（apt + jwt-cpp 源码 + tmpfs 挂载）
│
├── src/                   # C++ 后端源码根
│   ├── main.cpp           # 程序入口：解析配置、初始化 db/http/judge、Ctrl+C 优雅退出
│   ├── http/              # HTTP 层：cpp-httplib 路由注册、JWT 鉴权/权限中间件、JSON 响应、静态资源托管
│   ├── auth/              # 认证：argon2id 密码哈希、JWT 签发/校验、登录限速、admin 首登强制改密
│   ├── db/                # 数据层：SQLite 打开（WAL）、建表脚本、users/problems/testcases/
│   │                      #   submissions/user_problem_status 的查询封装
│   └── judge/             # 判题核心：
│                          #   - IExecutor 接口（沙箱策略可替换，可单测）
│                          #   - JudgeManager：有界线程池 min(NCPU,8)、提交入口、Rejudge
│                          #   - JudgeWorker：fork 子进程 + watchdog 超时 SIGKILL + waitpid 回收
│                          #   - JudgeRuntime：编译(g++/gcc + ASan/UBSan)、setrlimit、seccomp
│                          #     禁网/文件/读 proc、tmpfs 一次性目录、输出≤64KB、逐测试点比对汇总
│
├── web/                   # 前端源码根（无构建，静态托管于后端）
│   ├── index.html         # SPA 入口：hash 路由各页面容器（登录/注册/题目列表/题目页/
│   │                      #   提交历史/排行/后台）
│   ├── css/               # 全局样式：页面布局（题目页左右分屏）、导航、表格、状态标记、结果面板
│   ├── js/                # 前端逻辑：
│   │                      #   api.js    —— fetch 统一封装（带 JWT、错误处理、401 跳登录）
│   │                      #   router.js —— hash 路由分发 + 受保护路由重定向
│   │                      #   pages/    —— 各页面视图（题目列表/题目页/提交详情/排行/后台管理等）
│   │                      #   judge.js  —— 编辑器+提交逻辑（CodeMirror 初始化、语言选择、
│   │                      #     Ctrl+Enter 提交、逐测试点结果渲染）
│   └── assets/            # 静态素材：logo 图标、favicon 等
│
├── scripts/               # 运维与回归脚本
│   ├── regression.sh      # 一键回归：启动服务 → curl 提交已知 AC/WA 样例 → 断言状态
│   └── backup.sh          # cron 调用：sqlite3 .dump 生成 backup/oj-YYYYMMDD.sql
│
├── data/                  # 运行数据（含 .gitkeep 占位）
│   └── oj.db              # SQLite 数据库（WAL 模式，重启不丢数据）
│                          # tmpfs 判题运行目录按 dependence.md 挂在 /opt/oj-tmpfs
│
└── tests/                 # 测试
    └── unit/              # 判题核心单元测试（比对逻辑 AC/行尾空白、状态机边界 CE/TLE/MLE/RE 等）
```

---

## 4. TODO 清单

> 本清单用于安排开发顺序，具体要求以第 2～3 节为准，验收以第 5 节为准。
> 以 M0.1、M1.1 等小节作为开发批次，内部勾选项用于核对完成条件，不要求逐项拆成独立开发任务；较大的小节可按内部功能分批实现。
> 同一功能跨阶段出现时，前期完成基础实现，后期负责扩展和验证。
> 每完成一个开发小节，必须执行与本次改动相关的验证，通过后才能标记完成。可独立测试的核心逻辑同步编写或更新单元测试；接口、数据库、页面及进程行为采用相应的集成或操作验证。修复缺陷后复测原失败场景，并运行受影响的已有测试。M6 汇总执行已有测试、补足原定覆盖范围并完成最终验收，不将测试统一推迟到 M6。
> 各阶段验证可复用已有测试，不要求重复编写相同测试。
> 任务状态按实际代码和验证结果更新；未完成使用 [ ]，完成使用 [x]。
> 忽略规则、必要索引、测试数据隔离等属于对应任务的实施细节，不作为新增功能或独立交付项。

### M0 工程骨架

#### M0.1 项目结构

- [x] `git init`，创建约定目录结构（`src/`、`web/`、`scripts/`、`data/`、`tests/`）。
- [x] 整理 `dependence.md`，记录依赖与安装方式。

> 实施说明：随项目初始化配置忽略规则，排除构建产物、运行数据库、临时文件和敏感配置。

#### M0.2 构建与 HTTP 服务

- [x] 编写 CMake 配置，接入 cpp-httplib，生成后端可执行程序。
- [x] 实现程序入口与基础配置读取。
- [x] 启动 HTTP 服务，验证服务可访问。
- [x] 实现服务运行日志与启动失败提示。
- [x] 实现 `Ctrl+C` 优雅停止。
- [x] 验证构建、启动、访问、停止及再次启动。

> 实施说明：基础配置采用命令行参数（`--host`/`--port`，默认 `0.0.0.0:8080`），
> 非法输入与端口占用均报错并以非零退出码退出；信号处理仅置 `sig_atomic_t` 标志，
> 清理在信号处理函数之外完成；为正确检测端口占用，已关闭 cpp-httplib 默认的
> `SO_REUSEPORT`，改用 `SO_REUSEADDR`。构建与运行方法见 `README.md`。

#### M0.3 数据库基础

- [x] 接入 SQLite 依赖，封装数据库连接及资源释放。
- [x] 编写五张业务表的建表脚本与约束，按查询需要配置必要索引。
- [x] 启用 WAL 与外键检查。
- [x] 实现首次启动自动创建数据库、执行初始化。
- [x] 接入密码哈希依赖，创建预置 admin 账号及首次改密标记。
- [x] 将数据库初始化和关闭接入程序生命周期。
- [x] 验证重复初始化不清空数据、不重复创建 admin、不重置密码。

### M1 最小端到端链路

#### M1.1 注册

- [x] 封装用户查询、创建及密码验证所需的数据访问操作。
- [x] 实现随机 10 位数字账号分配，保证唯一且不重复分配。
- [x] 实现昵称唯一性检查与密码哈希存储。
- [x] 实现 `POST /api/register`。
- [x] 验证注册成功、重复昵称、账号碰撞及非法输入。

> 实施说明：
>
> - **账号分配**：`RandomAccountGenerator` 从 `/dev/urandom` 取随机数映射到
>   `[0000000000, 9999999999]`，输出 10 位纯数字字符串，**首位允许为 0**（账号以
>   字符串存储/返回）。随机只用于降低碰撞概率，唯一性最终由 `users.account`
>   的 `UNIQUE` 约束保证；碰撞时换号重试（默认 5 次，可注入确定序列单测），耗尽则报
>   内部错误。
> - **永久不复用**：当前无任何用户删除接口（M2.4 仅为列表/重置密码/改角色），用户
>   记录始终保留，故「现存账号唯一」即意味着已分配账号不会被再次分配。若未来引入
>   「物理删除用户」，需补充最小设计调整——例如软删除（禁用标记）或独立的
>   `used_accounts` 墓碑表——否则物理删除后该账号会被重新随机分配，破坏「永久不复用」。
> - **昵称规则**：去除首尾 ASCII 空白后非空、长度 ≤ 30（去空白后计）、内部空白保留；
>   查询与入库均使用规范化后的值，保证校验/查询/唯一性一致。**密码规则**：非空、
>   长度 ≤ 128，**不做任何裁剪或截断**，空白视为有效内容；只存 argon2id 哈希。
> - **错误约定**：成功 `201`；非法参数（含 JSON 解析失败、字段缺失/类型错误、非法昵称
>   或密码）`400`；昵称冲突 `409`；内部故障 `500`。错误响应体统一为 `{"error": "..."}`，
>   内部故障返回通用文案，不泄露数据库/哈希/SQL 细节。
> - **越权防护**：请求体只读取 `nickname`、`password`；`role`、`account` 等字段一律
>   忽略，账号、角色（固定 `user`）与首次改密标记（固定 0）由后端控制。昵称唯一性在
>   并发下由数据库唯一性约束兜底（同昵称并发注册最多一个成功）。
> - 请求/响应示例、输入规则详见 `README.md`。

#### M1.2 登录与身份验证

- [x] 配置 JWT 签名密钥、有效期及签发与验证逻辑。
- [x] 实现 `POST /api/login`。
- [x] 实现基础登录限速。
- [x] 封装 Bearer token 读取、登录检查及当前用户上下文。
- [x] 实现 `GET /api/me`。
- [x] 验证正确登录、错误密码、缺失及无效 token。

> 实施说明：
>
> - **JWT**：接入 `jwt-cpp`（header-only，使用 nlohmann-json traits）+ OpenSSL
>   libcrypto，仅允许 **HS256**。签发 claims：`iss=oj` / `aud=oj-api` / `iat` /
>   `exp` / `sub`（稳定的数据库用户 ID，十进制字符串）；验证时要求签名、算法、
>   iss、aud、exp、iat、sub 全部满足，`alg` 缺失或为 `none`/其它算法（如 HS384）
>   均被拒绝，无签名 token 同样被拒。`sub` 须为十进制正整数且对应存在的用户，
>   格式非法或引用不存在用户的 token 返回 401。
> - **密钥与有效期**：`OJ_JWT_SECRET`（必填，长度 ≥ 16 字节，无默认值）与
>   `OJ_JWT_EXPIRES_SECONDS`（可选，默认 3600）从环境变量读取；缺失、为空或过短
>   时服务启动即报错并以非零码退出，绝不使用公开默认密钥。密钥不落日志/源码/
>   版本控制；同一密钥重启后未过期 token 仍可验证。响应与日志均不记录密码、
>   密码哈希、密钥或完整 token。
> - **登录**：`POST /api/login` 读取 `account`/`password`（均须为非空字符串）。
>   普通用户用 10 位数字账号、预置 admin 用 `admin`；复用 argon2id `verify_password`，
>   密码不裁剪不截断。错误账号与错误密码返回一致的 `401 {"error":"账号或密码错误"}`，
>   不泄露账号是否存在；数据库等内部故障返回 `500`，不伪装成密码错误。
> - **登录响应**：`200` 返回 `token`、`token_type:"Bearer"`、`expires_in` 及
>   `user{id, account, nickname, role, reset_pwd_flag}`，不含密码/哈希。
> - **限速**：维度为来源 IP（`remote_addr`，不信任 `X-Forwarded-For`）；同一 IP
>   15 分钟窗口内 5 次失败后返回 `429` + `Retry-After`（秒）。成功登录清空计数；
>   窗口自最早失败起 15 分钟后自动解除。进程内互斥锁保护 + 周期性清理，线程安全、
>   并发不可绕过、状态不无限增长；不跨重启持久化（SPEC 未要求）。
> - **鉴权上下文**：`extract_bearer_token` 解析 `Authorization: Bearer <token>`；
>   `authenticate_request` 验证 token 后按 `sub` 查询数据库确认用户存在并读取当前
>   角色与首次改密标记，避免仅依赖 token 中可能过时的权限信息。`GET /api/me` 仅
>   登录可访问，返回当前数据库信息，不含密码哈希。
> - 错误约定：非法请求 `400`、登录失败/无效认证 `401`、限速 `429`、内部故障 `500`，
>   错误体统一 `{"error":"..."}`。请求/响应示例、限速规则见 `README.md`。

#### M1.3 改密与权限检查

- [x] 实现 `POST /api/me/password`。
- [x] 实现 admin 首次登录后的后端强制改密限制。
- [x] 封装可复用的管理员权限检查。
- [x] 验证普通用户改密及 admin 首次改密流程。
- [x] 验证未登录和非管理员请求被正确拒绝。

> 实施说明：
>
> - **改密接口**：`POST /api/me/password`（需登录），请求体仅读取 `old_password` 与
>   `new_password`（均为非空字符串）。目标用户由已验证的当前用户上下文决定（token
>   验证 + 按 `sub` 回查数据库），客户端传入的 `id`/`account`/`role` 等字段一律
>   忽略，无法修改他人密码或提权。新密码复用注册密码规则（非空、≤128、不裁剪不
>   截断），并要求与旧密码不同；只保存新密码的 argon2id 哈希。
> - **原子更新与并发**：改密在单个数据库事务内完成「读当前哈希 → 校验旧密码 →
>   更新密码哈希 + 清除 `reset_pwd_flag`」；密码与标记以单条 `UPDATE` 同时生效，
>   失败路径不产生部分更新。事务以 `BEGIN IMMEDIATE` 取写锁并配合连接级事务互斥锁
>   串行化，两个基于同一旧密码的并发请求只有最先提交者成功，后执行者因旧密码已
>   失效被拒，不会让旧密码再次覆盖新密码。
> - **首次改密限制**：`reset_pwd_flag=1` 的管理员允许登录、查询本人信息（`GET
>   /api/me`）与修改本人密码（`POST /api/me/password`）；其他需要登录的业务操作
>   （管理员业务入口）必须先改密。公开接口（注册/登录/题目列表等）继续按原权限
>   开放。受限业务返回 `403` + `{"error":"请先修改密码","code":"PASSWORD_CHANGE_REQUIRED"}`，
>   供前端识别并跳转改密。
> - **管理员权限检查**：`auth::check_admin` 基于已回查数据库的当前用户角色与
>   首次改密标记判断（不信客户端角色，也不信 token 中可能过时的角色），组合满足
>   「已登录 + 已完成必要改密 + 具备 admin 角色」才放行；统一用于所有管理员入口，
>   避免遗漏首改限制。改密成功后权限按当前数据库状态判断，无需修改 token。
> - **JWT 行为**：改密不撤销已有 token。当前无会话撤销机制，已签发的旧 token
>   持续有效至 `exp` 过期；鉴权每次按 `sub` 回查数据库，故旧 token 的权限随数据库
>   中最新角色与首次改密标记实时生效。SPEC 未要求改密即撤销全部 token，未新增撤销
>   机制（如需引入需先调整数据模型，不在本阶段范围）。
> - **错误约定**：未登录/无效 token `401`；旧密码错误 `401`；权限不足 `403`；
>   必须先改密 `403`（含 `code` 标识）；非法参数/非法新密码/新密码与旧密码相同
>   `400`；内部故障 `500`。响应与日志均不泄露密码、密码哈希、签名密钥或完整 token。
> - 本阶段尚无正式管理员业务接口，通过测试专用路由 `GET /api/test/admin-only`
>   （仅集成测试启用，正式服务不注册）验证首改限制与权限组合行为。请求/响应示例
>   与规则详见 `README.md`。

#### M1.4 最小题目数据与查询

- [x] 准备 2～3 道种子题目，包含公开样例及隐藏测试用例。
- [x] 实现题目列表和详情所需的数据查询。
- [x] 实现 `GET /api/problems` 的基础列表功能。
- [x] 实现 `GET /api/problems/{id}`。
- [x] 实现题目可见性检查，确保详情不下发隐藏用例。

> 实施说明：
>
> - **数据模型最小调整**：原 `testcases` 无法区分公开样例与隐藏用例，故新增
>   `is_sample INTEGER NOT NULL DEFAULT 0`（`1`=公开样例，`0`=隐藏用例），显式区分，
>   不采用「前 N 个默认公开」等隐含规则；`problems` 新增可空 `seed_key TEXT` 及唯一索引
>   （`idx_problems_seed_key`，允许多个 NULL），作为种子题的稳定幂等标识。两处均通过
>   `initialize_schema` 内的 `ALTER TABLE ADD COLUMN` 迁移旧库，不重建、不清空、保留数据。
> - **种子题**：内置 A+B Problem、整数求和、求最大值三道简单题，标准 ACM 输入输出，
>   含标题、纯文本题面、输入输出说明、公开样例、隐藏用例、难度、标签、时限与内存限制。
>   公开样例与隐藏用例内容刻意不同，便于验证不泄露。
> - **幂等导入**：`./build/oj_server --db <路径> --seed` 显式导入后退出，不在服务启动时
>   自动执行。以 `seed_key` 判定已存在则整题跳过，不重复创建、不覆盖任何已有修改、
>   不清空其它数据；每题在单事务内写入题目与用例。仅建/迁移表结构，不涉及 admin，
>   无需 `OJ_JWT_SECRET` / `OJ_ADMIN_PASSWORD`。
> - **数据层**：`src/db/problems.{h,cpp}` 封装列表（按 `id` 升序稳定排序）、详情、公开样例
>   与全部用例读取，全部参数绑定。公开查询结构体不含隐藏用例；含隐藏用例的
>   `list_testcases` 仅供判题/管理使用，不直接序列化下发。
> - **接口**：`GET /api/problems` 返回 `{"problems":[{id,title,difficulty,tags,visible}],"total":N}`；
>   `GET /api/problems/{id}` 返回元数据与 `samples`（仅公开样例），均不含隐藏用例。
> - **可见性**：游客与普通用户仅可见 `visible=1` 题目；通过 M1.3 管理员检查
>   （已登录 + 已完成首次改密 + admin 角色）者可看隐藏题。列表与详情规则一致，直接请求
>   隐藏题 ID 亦返回 `404`；未完成首次改密的管理员按普通用户处理。无效/伪造/过期 token
>   返回 `401`，不当作管理员。不存在的题目与无权查看的隐藏题目对游客/普通用户统一 `404`；
>   非法 ID `400`；数据库故障 `500` 通用文案。
> - 请求/响应示例与可见范围表见 `README.md`；测试见 `tests/integration/test_problems_api.cpp`
>   与 `tests/unit/test_problems_unit.cpp`。

#### M1.5 最小判题器

- [x] 定义 `IExecutor` 接口及判题结果结构。
- [x] 实现单线程判题流程的任务组织。
- [x] 实现用户源码编译，采集编译错误。
- [x] 实现测试输入传入、程序运行和输出采集。
- [x] 实现基础超时终止、子进程回收与临时文件清理。
- [x] 实现行尾空白及文末空行归一化后的输出比对。
- [x] 编译成功后执行并汇总测试点，生成基础 AC/WA 结果；编译失败返回 CE。
- [x] 使用已知样例验证编译、运行、比对完整流程。

> M1 判题器用于开发环境验证；完整沙箱、资源限制及异常分类在 M3 完成。

> 实施说明：
>
> - **接口与结构**（`src/judge/`）：`IExecutor`（`executor.h`）抽象进程执行；`JudgeEngine`
>   （`judge.h`）负责输入校验、工作目录、编译、逐点执行、比对与汇总；`normalize_output`
>   / `outputs_match`（`comparator.h`）为纯函数比对。判题核心不依赖 HTTP、不负责提交
>   入库，`LocalExecutor` 可被 M3 沙箱实现替换。
> - **编译选项**：C++17 `g++ -O2 -std=c++17 <src> -o program -lm`，C11
>   `gcc -O2 -std=c11 <src> -o program -lm`，均以参数数组 `execvp` 启动，不经 shell。
>   **M1.5 尚未接入 ASan/UBSan**（属 M3.4，未提前标记完成）。编译进程有保护超时，
>   非零退出码返回 `CE` 并附有长度上限的诊断；编译器不存在等环境故障返回 `SYSERR`。
> - **工作目录与运行**：每次判题用 `mkdtemp` 创建唯一目录（默认系统临时目录），保存
>   源码与编译产物，结束后只清理本次目录；每个测试点启动新进程，输入经 stdin 传入，
>   stdout/stderr 分别有界采集（64KB / 16KB），超限只标记截断并继续排空，标准输出
>   超限不判为 AC。单点超时 `SIGKILL` 强杀并 `waitpid` 回收。
> - **比对与汇总**：去除每行行尾空白与文末空行后逐字符比较，保留行首/行内空白与中间
>   空行；普通 WA 不阻断后续测试点；全部 AC 才 `AC`，否则按
>   `SYSERR > TLE > MLE > RE > WA` 取最严重状态。空测试集、非法语言、无效时间返回
>   明确 `SYSERR`，绝不因未执行测试点而返回 `AC`。
> - **测试**：单元 `tests/unit/test_judge_unit.cpp`（gtest + FakeExecutor，覆盖比对、
>   汇总、语言解析与编排），集成 `tests/integration/test_judge_integration.cpp`
>   （真实 g++/gcc 的 AC/WA/CE、顺序执行、每点新进程、超时回收、超大输出、提前退出、
>   编译器缺失、临时目录与子进程清理）。运行说明见 `README.md` 与 `dependence.md` 第 7 节。
> - **边界**：本阶段仅有基础超时与进程清理，无 setrlimit/seccomp/tmpfs/内存限制，
>   不是完整沙箱，仅限开发环境验证；完整 MLE、Sanitizer 诊断与异常分类、线程池与
>   Rejudge 等留待 M3。

#### M1.6 提交接口与持久化

- [x] 实现 `POST /api/problems/{id}/submit`。
- [x] 检查登录状态、题目访问权限、语言及源码参数。
- [x] 接入单线程判题器，同步返回判题结果。
- [x] 将源码、编译信息及逐测试点结果写入 `submissions`。
- [x] 更新用户题目状态、首次 AC 时间和提交次数。
- [x] 验证提交记录及做题状态在服务重启后仍保留。

> 实施说明：
>
> - **接口**：`POST /api/problems/{id}/submit`（需登录，同步返回）。请求体仅读取
>   `language` 与 `code`；用户归属取自已验证的当前用户上下文（token + 按 `sub`
>   回查数据库），客户端传入的 `user_id`/`id`/`status`/`testcases` 等字段一律忽略。
>   成功返回 `200` 与提交 ID、状态、逐点结果；判题结果（CE/WA/TLE/RE/MLE/SYSERR）
>   属于提交结果而非服务故障，同样返回 `200`。`memory_kb` 固定为 `null`，明确表示
>   M1.6 未采集内存，不伪造测量结果。
> - **参数与权限**：登录（未登录/无效 token `401`）、首次强制改密（`403` +
>   `PASSWORD_CHANGE_REQUIRED`）、题目存在性/可见性（复用 M1.4 规则，普通用户向
>   隐藏题提交与不存在题目统一 `404`）。`language` 仅接受 `cpp17`/`c11`（大小写不
>   敏感）；`code` 非纯空白且不超过 64 KiB；非法 JSON/字段缺失/类型错误/非法 ID
>   `400`；请求体超过 1 MiB 由服务器在解析前以 `413` 拒绝。除校验外不修改用户源码。
> - **判题数据**：完整测试用例（含隐藏用例）、顺序与时限全部从后端数据库读取，
>   构造为内存中的任务快照后即释放读取，判题期间不持有数据库事务；不接受客户端提供
>   的标准答案、用例或资源限制覆盖值。
> - **执行器与并发**：复用 M1.5 `JudgeEngine` + `LocalExecutor` 同步判题。M1.6 的
>   执行器不提供并发保证，故对判题阶段加简单可靠的互斥保护，避免 HTTP 并发请求下
>   临时目录/执行状态混用；本阶段不引入 M3 的 worker 线程池。
> - **持久化**：`submissions` 保存用户、题目、规范语言、完整源码、最终状态、逐点
>   结果 JSON、编译诊断、耗时（逐点之和）、时间戳；`user_problem_status` 首次有效
>   提交创建记录、每条已持久化提交使 `submit_count` 加一、首次 AC 设置 `accepted`
>   与 `first_ac_at`、重复 AC 不覆盖首次 AC 时间、AC 后失败不清除已通过状态。两者在
>   **同一个短事务**内写入，任一失败整体回滚；并发由连接级事务互斥 + `BEGIN IMMEDIATE`
>   串行化，并以 `UNIQUE(user_id, problem_id)` 兜底，不丢计数、不重复创建状态记录。
> - **统计口径**：参数或权限检查失败不产生记录、不增加次数；产生并保存的判题结果
>   （含 CE/WA/TLE/RE/MLE 与内部故障 `SYSERR`）均属提交记录并计入 `submit_count`，
>   保持记录与统计一致。`submissions.created_at` 与 `first_ac_at` 采用同一提交时间
>   口径，便于后续 Rejudge 按原提交时间重算。
> - **返回可见范围**：只有当前提交者获得本次结果；AC 测试点条目不含隐藏输入/标准
>   答案，WA 测试点附该失败点的输入/期望输出/实际输出（其余点不泄露）。WA 详情已在
>   本阶段按 SPEC PRB-05/JUDGE-07 返回，但 M3.4 的完整分类、Sanitizer 诊断与内存采集
>   仍未完成，故 M3.4 勾选状态不变。
> - **日志与安全**：记录提交 ID/用户/题目/语言/状态等必要运行日志；不将完整源码、
>   token 或隐藏用例写入普通请求日志；持久化失败返回通用 `500`，不泄露 SQL/路径。
> - **安全边界**：接口仍调用 M1.5 开发环境判题器（无 setrlimit/seccomp/tmpfs），
>   完成提交接口不代表已完成安全隔离，不得公开接收不可信代码；完整沙箱与线程池见 M3。
> - 测试见 `tests/unit/test_submit_unit.cpp`（语言/源码校验、状态计算）与
>   `tests/integration/test_submit_api.cpp`（真实 g++/gcc 的 AC/WA/CE、认证与权限
>   拒绝且不产生记录、额外字段防篡改、状态与计数、8 路并发、SYSERR、事务回滚、
>   隐藏用例不泄露、重启持久化）。接口与计数规则示例见 `README.md`。


#### M1.7 最小前端

- [x] 配置 cpp-httplib 静态资源托管。
- [x] 编写基础 API 请求封装，携带 JWT。
- [x] 实现注册、登录和改密页面。
- [x] 实现基础题目列表页面。
- [x] 实现题目页左右布局，使用 `textarea` 输入源码。
- [x] 实现语言选择、提交按钮及基础结果展示。
- [x] 跑通注册 → 登录 → 看题 → 提交 → 查看结果。

> 实施说明：
>
> - **静态托管**：`HttpServer` 新增 `web_root` 参数（正式启动由 `--web` 指定，默认
>   `web`），仅当该目录存在且不是项目根/系统根时，以 `set_mount_point("/", root)`
>   只读挂载到 URL 根路径；测试默认传空串，不启用静态托管。项目根目录、`data/oj.db`、
>   `src/`、配置与判题临时目录均不在托管范围；cpp-httplib 的路径校验拒绝 `..` 与
>   URL 编码越界（`/SPEC.md`、`/data/oj.db`、`/../SPEC.md`、`/%2e%2e/SPEC.md` 均
>   `404`）。`/api/*` 与 `/api/health` 路由行为不变。
> - **技术选型**：原生 HTML/CSS/ES Module，无构建流程、无框架；本阶段编辑器为
>   `textarea`（CodeMirror 留待 M4.3）。代码按 `js/api.js`（统一请求）、`js/auth.js`
>   （凭证存储）、`js/router.js`（hash 路由）、`js/nav.js`、`js/util.js`、
>   `js/judge.js`（结果渲染）与 `js/pages/*`（各页面）拆分，`index.html` 仅作容器。
> - **API 封装与凭证**：统一处理 JSON、`Authorization: Bearer <token>`、HTTP 错误
>   与网络异常；token 存 `localStorage`，只放请求头，绝不进入 URL 或日志。身份失效
>   （401）清理本地凭证并跳转登录（内部去重，避免并发 401 重复跳转）；登录失败显示
>   表单错误；改密接口的「旧密码错误」按表单错误处理，不误退出登录。后端返回
>   `403 PASSWORD_CHANGE_REQUIRED` 时引导到改密页。退出登录仅清理前端凭证，不声称
>   撤销后端 JWT。
> - **页面与流程**：hash 路由 `#/problems`、`#/problems/{id}`、`#/login`、
>   `#/register`、`#/password`；`#/password` 受保护，未登录以 `redirect` 参数跳登录并
>   在登录后返回原目标页。注册成功显著展示系统分配的 10 位账号并引导用该账号登录
>   （不依赖未实现的注册后自动登录）；admin 首登及后端「必须先改密」均引导到改密页；
>   改密后按现有 JWT 策略继续使用（不假定旧 token 失效）。题目列表展示后端提供的
>   ID/标题/难度/标签，含加载中、空列表、加载失败状态，不显示未提供的通过人数或本人
>   AC 状态；题目页左右布局（窄屏 ≤ 900px 改上下排列），左侧题面/公开样例/难度标签/
>   时空限制，右侧语言选择/`textarea`/提交/结果。游客可浏览公开题，提交按钮禁用并提示
>   登录。
> - **提交与结果**：复用 M1.6 提交接口同步等待结果；请求期间禁用按钮并显示「判题中」，
>   结束后恢复；失败保留源码、不自动重试；网络中断提示「结果无法确认」，不断言后端
>   未保存。展示提交 ID、总体状态、逐点结果、编译信息、诊断与 WA 失败点的输入/期望/
>   实际输出；后端未采集的内存显示「未采集」而非 0；不为展示额外请求隐藏用例。题面、
>   样例、昵称、编译信息与程序输出一律作为纯文本渲染（`textContent`/`<pre>`），HTML
>   特殊字符不被解释执行。
> - **验证**：`tests/unit/test_static_files.cpp`（gtest，7 项，隔离临时库 + 临时 web
>   目录 + 随机端口）覆盖 `/`→index.html 与 HTML/CSS/JS 的 MIME、未知路径 404、托管目录
>   之外同级文件不可访问、目录穿越（`..`/`%2e%2e`/`..%2f`/`.git/config`/越界系统文件）
>   被拒且不回显、空与不存在 `web_root` 降级而 `/api` 保持可用、静态托管不影响健康检查/
>   题目列表/注册 POST。另以隔离临时库 + 随机端口真实启动服务做 HTTP 级验证 54 项全通过
>   （静态资源 MIME、越界防护、注册/重复昵称/错误密码、公开与隐藏题可见性、C++17/C11 的
>   AC/WA/CE、admin 首改限制与改密、无效 token 等）。内网无法取得可用的无头/图形浏览器，
>   页面级验证改为在 jsdom 中真实执行 `web/js` 模块连接该服务完成，55 项全部通过（注册→
>   登录→看题→提交→查看结果、改密、退出、token 失效、游客提交受控、提交防重复、失败保留
>   源码、XSS 转义、无脚本错误）；真实浏览器渲染与真机窄屏验证未执行，属未验证项。
> - **边界**：未提前实现 M2/M4 的搜索筛选、分页、后台管理、CodeMirror、提交历史与
>   排行榜；判题器仍为开发环境实现，前端可提交不等于已具备公开运行不可信代码的能力。

### M2 题目与用例管理

#### M2.1 管理员题目接口

- [x] 实现创建题目接口及字段校验。
- [x] 实现修改题目接口。
- [x] 实现删除题目接口，明确关联数据处理方式。
- [x] 实现题目可见性设置及管理员查看隐藏题目的能力。
- [x] 对全部题目管理接口接入管理员权限检查。

> 实施说明：
>
> - **接口**：`POST /api/admin/problems`（建题，成功 `201` + `{"id":N}`）、
>   `PUT /api/admin/problems/{id}`（改题，成功 `200` + `{"id":N,"status":"ok"}`）、
>   `DELETE /api/admin/problems/{id}`（删题，成功 `200` + `{"status":"ok"}`）。
>   字段沿用已有存储结构：标题、纯文本描述、公开样例（`is_sample=1`）、难度、标签、
>   时限、内存上限、可见性。隐藏测试用例的增删改属 M2.2，不通过本组接口混合实现；
>   改/删题对用例的操作均显式限定 `is_sample`，绝不误动隐藏用例。
> - **校验与默认值**：必填 `title`（去首尾空白后非空、≤200 字节）与 `difficulty`
>   （`easy`/`medium`/`hard`）；可选 `description`（≤64KiB，默认 `""`）、`tags`
>   （字符串数组，去空白非空、不含逗号、每个 ≤30 字节、≤20 个、不可重复，默认空）、
>   `time_limit_ms`（整数，默认 `2000`，范围 `1..60000`）、`memory_limit_kb`（整数，
>   默认 `65536`，范围 `1..1048576`）、`visible`（布尔，默认 `true`）、`samples`
>   （对象数组，须含字符串 `input`/`output`，≤50 组，默认空）。**项目未定义「无限」
>   取值约定，故不以 0 或负数表示无限制**；时限上限与 SPEC JUDGE-10 的单次硬上限
>   一致。校验为纯函数（`src/problem/validation.{h,cpp}`），有独立单元测试。
> - **PUT 语义**：采用**部分更新**——仅更新请求体中出现的字段，未出现的字段保持原值，
>   避免遗漏字段被意外清空；需要清空时显式传空值（如 `"description":""`、
>   `"samples":[]`）。`samples` 出现时整体替换公开样例，隐藏用例不受影响。
> - **服务端管理字段**：`id`/`created_at`/`updated_at`/`seed_key` 不由客户端指定，
>   请求体中的同名字段一律忽略；普通题 `seed_key` 为 `NULL`。创建时 `created_at` 与
>   `updated_at` 由服务端写入当前 UTC 时间；修改仅更新 `updated_at`，保留 `created_at`。
> - **数据访问与事务**：`ProblemAdminStore`（`src/db/problem_admin.{h,cpp}`）封装写入，
>   全部参数绑定；创建（题目 + 公开样例）与修改、删除均在**单个事务**内完成，任一失败
>   整体回滚，不产生部分更新。
> - **权限**：三个接口统一复用 `require_admin`（登录 + 已完成首次改密 + 当前数据库角色
>   为 admin），角色与首次改密标记每次按 `sub` 回查数据库，不信客户端提交的角色，也不
>   依赖 JWT 中可能过时的角色。游客/无效 token `401`；非管理员或角色被撤销后 `403`；
>   未完成首次改密的管理员 `403` + `code:"PASSWORD_CHANGE_REQUIRED"`。
> - **可见性**：管理接口可设置 `visible`；现有列表/详情接口沿用 M1.4 规则——具备权限
>   的管理员可见隐藏题，游客与普通用户列表不含隐藏题、直接访问详情 `404`、向隐藏题提交
>   `404`。仅可见性变化不清除已有提交与做题状态，也不重判历史提交（Rejudge 见 M3.6）。
> - **删除策略（本次确定）**：**题目已有 `submissions` 记录时拒绝删除（`409`），
>   无提交时在同一事务内删除该题的全部测试用例（公开样例与隐藏用例）、
>   `user_problem_status` 记录与题目本身**，以保留学生提交历史、不留孤立记录、不新增
>   软删除字段、不级联清空提交。检查与删除在同一 `BEGIN IMMEDIATE` 事务内完成，并与
>   提交持久化复用同一事务互斥锁；提交链路在事务内复核题目仍存在，若判题期间题目被
>   删除则返回 `404`，不产生假成功或外键错误。相关外键仍保持无级联删除。
> - **错误约定**：非法参数/非法 JSON `400`；未登录/无效 token `401`；权限不足/须先改密
>   `403`；题目不存在 `404`；删除冲突 `409`；内部故障 `500` 通用文案，不泄露 SQL/路径。
> - 请求/响应示例、字段规则与可见范围见 `README.md`；单元测试
>   `tests/unit/test_problem_admin_unit.cpp`（26 项），集成测试
>   `tests/integration/test_admin_problems_api.cpp`（含权限、可见性、删除规则、并发删题与
>   提交一致性、重启持久化）。

#### M2.2 管理员测试用例接口

- [x] 实现测试用例新增。
- [x] 实现测试用例修改和删除。
- [x] 保存用例 `ord`，按该顺序读取、执行用例，并检查用例的题目归属。
- [x] 提供后台编辑所需的用例读取能力，并限制管理员访问。

> 实施说明：
>
> - **接口**：`GET /api/admin/problems/{id}/testcases`（管理员读取该题完整用例列表）、
>   `POST /api/admin/problems/{id}/testcases`（增，成功 `201` + `{"id","problem_id","ord"}`）、
>   `PUT /api/admin/problems/{id}/testcases/{tid}`（改，成功 `200` + `{"id","problem_id","ord","status":"ok"}`）、
>   `DELETE /api/admin/problems/{id}/testcases/{tid}`（删，成功 `200` + `{"status":"ok"}`）。
>   四个接口统一复用 `require_admin`（登录 + 已完成首次改密 + 当前数据库角色为 admin），
>   角色与首改标记每次按 `sub` 回查数据库；游客/无效 token `401`，普通用户 `403`，
>   未完成首改的管理员 `403` + `code:"PASSWORD_CHANGE_REQUIRED"`。普通用户即使知道题目
>   ID 或用例 ID 也无法读取完整用例或执行修改。
> - **存储模型（沿用 M1.4）**：公开样例与隐藏用例共用 `testcases` 表，`is_sample=1`
>   为公开样例、`0` 为隐藏用例。公开样例由 M2.1 的题目接口通过 `samples` 字段整体维护；
>   本组接口**只读写 `is_sample=0`**，从不读取/改写 `is_sample`，因此新增、修改、排序
>   都不可能把隐藏用例变成公开样例，也不会破坏公开样例；通过本组接口改/删公开样例或
>   不属于该题的用例统一 `404`。管理员读取接口是唯一返回完整用例的入口且受权限保护，
>   公开列表/详情仍只返回公开样例，不复用管理员响应。
> - **校验**：纯函数 `src/problem/testcase_validation.{h,cpp}`。新增必填 `input`/`output`
>   （字符串，允许空串，**空串与缺失区分**；≤64 KiB），可选 `ord`（整数 `0..1000000`）；
>   修改只读取出现过的 `input`/`output`/`ord`（部分更新，未出现保持原值，显式空串可清空，
>   无可更新字段 `400`）。保存**不做 trim 或输出归一化**，原样保留空格、制表符与换行；
>   归一化仅在判题比对阶段进行。整体请求体上限沿用 1 MiB（超限由服务器以 `413` 拒绝）。
> - **归属与定位**：用例归属只由 URL 中的题目 ID 决定，请求体中的 `id`/`problem_id`/
>   `is_sample` 等一律忽略，新增记录固定 `is_sample=0`；修改、删除均同时按
>   `problem_id` 与用例 ID 定位，用例不存在/不属于该题/属于公开样例统一 `404`，不会
>   修改其它题目的用例。新增时在事务内校验题目存在（`404`）。
> - **ord 规则（本次明确）**：起始 `0`，取值 `0..1000000`，允许重复；同一题内顺序由
>   `(ord ASC, id ASC)` 唯一确定（id 为稳定第二排序键），不依赖默认行顺序；新增缺省
>   `ord` = 该题全部用例（含公开样例）最大 `ord` + 1，无用例时为 `0`（追加末尾）；若
>   该题 `ord` 已达上限 `1000000`、缺省追加无法落在范围内，则拒绝并返回 `409`（约束
>   冲突），绝不写入越界值；显式 `ord` 越界仍为 `400`。删除不重排、不回收空号。数据库
>   读取（`ProblemStore::list_testcases`）、管理员列表与判题执行使用同一排序。
> - **数据访问与事务**：`TestcaseAdminStore`（`src/db/testcase_admin.{h,cpp}`）封装写入，
>   全部参数绑定；写入在短事务内以 `BEGIN IMMEDIATE` 完成并复用 `Database::transaction_mutex()`
>   与题目管理/提交持久化串行化，避免与并发删题交错产生孤立记录或假成功，任一失败整体
>   回滚、不产生部分更新。
> - **判题一致性**：提交在判题前读取完整用例并构造内存快照，判题全程使用该快照，判题
>   期间修改用例不会让同一次判题混用修改前后的数据；后续提交使用修改后的用例。新增/
>   修改/删除用例**不重判历史提交**，不更改已保存的逐点结果、AC 状态与提交次数
>   （Rejudge 见 M3.6）。管理员可暂时删空用例；空测试集**不判 AC**，提交返回约定的
>   `SYSERR`（不可判题）并正常持久化，零测试点绝不判为 AC。
> - **错误约定**：非法参数/非法 JSON `400`；未登录/无效 token `401`；权限不足/须先改密
>   `403`；题目或用例不存在（含归属不符/公开样例）`404`；缺省 `ord` 自动分配耗尽 `409`；
>   请求体超限 `413`；内部故障 `500` 通用文案，不泄露 SQL/路径/隐藏用例内容；运行日志
>   只记录题目/用例 ID，不记录完整隐藏用例。
> - 请求/响应示例、`ord` 规则与公开样例/隐藏用例关系见 `README.md`；单元测试
>   `tests/unit/test_testcase_admin_unit.cpp`（13 项），集成测试
>   `tests/integration/test_admin_testcases_api.cpp`（权限、归属、原样文本、`ord` 与判题
>   顺序一致、判题快照、历史数据不变、空测试集、并发删题、重启持久化等）。

#### M2.3 题目列表查询

- [x] 实现关键词搜索。
- [x] 实现难度和标签筛选。
- [x] 支持搜索与筛选条件组合。
- [x] 实现分页，每页 20 条。
- [x] 返回列表展示所需的通过人数及本人做题状态。
- [x] 验证游客、学生和管理员的题目可见范围。

> 实施说明：
>
> - **接口**：扩展 `GET /api/problems`（公开接口，未携带 token 按游客处理），新增
>   查询参数 `q`/`difficulty`/`tag`/`page`/`visible`，不另建重复列表接口。响应在原
>   `problems`/`total` 基础上新增 `page`、`page_size`（固定 `20`）、`total_pages`，
>   字段向后兼容，M1.7 最小前端无需改动即可继续显示列表并进入详情。
> - **搜索**：对 `title` 做子串匹配，ASCII 字母大小写不敏感、中文按字节匹配；空
>   （去首尾空白后）表示不限。参数全部绑定；`%`、`_`、`\` 经转义后按普通字符处理
>   （`LIKE ? ESCAPE '\'`），不会扩大匹配范围。搜索范围仅限标题，不含题面。
> - **难度/标签**：难度沿用 `problems.difficulty` 取值（`easy`/`medium`/`hard`），空
>   表示不限，非法取值 `400`。标签以逗号补齐首尾后用 `instr(','||tags||',', ?)` 做
>   **完整标签**匹配（如「图」不误配「图论」），沿用逗号分隔存储，不重构数据模型。
>   各条件与可见性之间为 **AND**，单独/组合/全部省略均正确；无匹配返回空列表。
> - **分页**：每页固定 20 条，`page` 默认 1，仅接受正整数 `1..1000000`；`0`、负数、
>   非整数、超出上限均 `400`（避免 `(page-1)*20` 溢出）；超出末页返回 `200` 空列表。
>   稳定排序为 `id ASC`（已有规则即最终排序键），静态数据跨页不重复、不遗漏。
> - **通过人数/本人状态**：`pass_count` 取 `user_problem_status` 中 `status='accepted'`
>   的**不同用户数**（`COUNT(DISTINCT user_id)`），重复 AC 只计一人、仅失败不计入。
>   列表用相关子查询在同一 SQL 中取得人数与本人状态，避免逐题再查或连接导致重复行、
>   统计膨胀与分页错乱；读取列表不修改做题状态、不重新判题。已登录用户返回 `solved`
>   布尔（无记录视为 `false`），游客不返回该字段；`viewer_user_id` 只来自已验证身份，
>   客户端参数（如 `user_id`/`role`）一律忽略。
> - **可见性**：非管理员（含未完成首次改密的管理员）始终只返回 `visible=1` 且
>   `total` 与可见数量一致，不泄露隐藏题；管理员可看全部。新增最小可见性筛选参数
>   `visible`（`all`/`1`/`true`/`0`/`false`，默认 `all`），仅对通过管理员检查的账号
>   生效；普通用户/游客传合法值时不据此筛选，非法取值仍按参数错误返回 `400`，均不
>   扩大访问范围。本阶段不实现筛选页面（属 M4.2）。
> - **列表与计数一致**：列表与 `total` 复用同一条件构造（同一 WHERE 与绑定参数），
>   保证口径一致；新增索引需求沿用 `idx_user_problem_status_problem` 与
>   `UNIQUE(user_id, problem_id)`，未新增缓存或复杂搜索组件。
> - **边界**：不进入 M2.4 用户管理、M4.2 搜索筛选分页页面、排行榜与 Rejudge；M1.7
>   前端保持不变。参数解析/转义/匹配模式为纯函数 `src/problem/list_query.{h,cpp}`，
>   有独立单元测试；实际筛选、分页、统计与权限由集成测试验证。
> - 请求/响应示例与规则见 `README.md`；单元测试
>   `tests/unit/test_problem_list_query.cpp`（18 用例），集成测试
>   `tests/integration/test_problem_list_query.cpp`（10 场景 / 143 项断言）。

#### M2.4 管理员用户接口

- [x] 实现用户列表查询。
- [x] 实现管理员重置用户密码。
- [x] 实现管理员修改用户角色。
- [x] 验证用户管理接口的权限限制。

> 实施说明：
>
> - **接口**：`GET /api/admin/users`（用户列表）、`PUT /api/admin/users`（重置密码 /
>   改角色）。两者统一复用 `require_admin`（登录 + 已完成首次改密 + 当前数据库角色为
>   admin），角色与首改标记每次按 `sub` 回查数据库，不信客户端角色，也不依赖 token 中
>   可能过时的角色。游客/无效 token `401`；普通用户 `403`；未完成首改的管理员 `403` +
>   `code:"PASSWORD_CHANGE_REQUIRED"`。
> - **请求约定（本次明确）**：`PUT` 请求体固定含 `action`（严格 `reset_password` /
>   `change_role`）与正整数 `user_id`；`reset_password` 另需 `new_password`，
>   `change_role` 另需 `role`（`admin`/`user`）。不同操作只读取并只修改对应字段，
>   `account`/`nickname`/`password_hash`/`reset_pwd_flag` 等一律忽略，不把请求体任意
>   映射到 `users` 表。校验为纯函数 `src/user/admin_user_validation.{h,cpp}`，有独立
>   单元测试。
> - **列表**：仅返回 `id`/`account`/`nickname`/`role`/`reset_pwd_flag`/`created_at`，
>   不含密码哈希、密码、token 或密钥。分页沿用 `GET /api/problems` 约定：`page` 默认
>   `1`，正整数 `1..1000000`（非法 `400`），每页固定 `20`，超出末页返回空列表；稳定排序
>   `id ASC`（主键，跨页不重不漏）。为避免越界，`LIMIT/OFFSET` 参数全部绑定。
> - **重置密码**：`new_password` 复用注册密码规则（非空、≤128、不裁剪不截断），
>   **不需要目标用户旧密码**；argon2id 哈希在事务外计算，单条 `UPDATE` 原子写入
>   `password_hash` 并将 `reset_pwd_flag` 置 `1`。成功响应仅 `{"user_id","status":"ok"}`，
>   不回显密码；日志只记录操作者/目标/结果，不记录密码或哈希。
> - **重置后首次改密策略（本次确定）**：重置即将 `reset_pwd_flag=1`，**对所有目标用户
>   生效**（含普通用户），直接复用 M1.3 的强制检查——普通用户被禁止提交、管理员被禁止
>   使用管理员接口，直到通过 `POST /api/me/password` 改密后清除标记。此为 M1.3 强制改密
>   范围向普通用户提交业务的明确扩展，后端存在对应限制，非只设标记。
> - **改角色**：仅允许 `admin`/`user`。**禁止取消最后一个管理员的权限**：把唯一管理员
>   降级返回 `409`；允许管理员自我降级（含预置 admin），预置 admin 无永久豁免。检查与更新
>   在同一 `BEGIN IMMEDIATE` 事务内完成并复用连接级事务互斥锁，并发降级下仍保留至少一名
>   管理员。角色修改只更新 `role`，不改变账号、昵称、密码哈希、历史提交与做题状态。
> - **JWT 行为**：沿用现有会话策略，**无会话撤销机制**：重置密码或改角色后旧 token 仍
>   有效至 `exp`，不声称改哈希即令旧 token 失效；但每次鉴权按 `sub` 回查数据库，旧 token
>   的权限与首改标记按数据库最新值实时生效（被降级者无法再调用管理员接口）。
> - **数据访问与事务**：`UserAdminStore`（`src/db/user_admin.{h,cpp}`）封装，全部参数
>   绑定；重置为单条原子 `UPDATE`（`RETURNING` 区分目标不存在），改角色在事务内完成
>   最后管理员检查与更新，任一失败整体回滚、不产生部分更新；目标不存在 `404`。
> - **错误约定**：非法参数/非法 JSON `400`；未登录/无效 token `401`；权限不足/须先改密
>   `403`；目标用户不存在 `404`；最后管理员保护冲突 `409`；内部故障 `500` 通用文案，
>   不泄露 SQL/路径/哈希。目标用户不存在、请求非法或写入失败时不产生部分修改。
> - 请求/响应示例、字段规则、角色保护与重置后改密/旧 token 行为见 `README.md`；单元测试
>   `tests/unit/test_user_admin_unit.cpp`，集成测试 `tests/integration/test_admin_users_api.cpp`
>   （权限、列表字段与分页、重置与新旧登录、首改限制与旧 token、提升/降级、越权字段、
>   自我降级/最后管理员/并发保护、旁路数据不变、数据库故障、重启持久化）。

#### M2.5 后台管理页面

- [x] 实现管理员入口及页面访问检查。
- [x] 实现题目列表、创建、编辑和删除交互。
- [x] 实现题目可见性控制。
- [x] 实现测试用例录入、编辑、删除，并按 `ord` 展示。
- [x] 实现用户列表、密码重置和角色修改。
- [x] 验证管理员操作与数据库数据一致。

> 实施说明：
>
> - **技术选型与结构**：沿用 M1.7 的原生 HTML/CSS/ES Module（无构建、无框架）与 hash
>   路由，复用 `web/js/api.js` 的请求封装、`auth.js` 的凭证与角色状态、`util.js` 的文本
>   安全渲染，新增 `web/js/pages/admin-common.js`（后台子导航、错误映射、状态区块）、
>   `admin-problems.js`（题目列表/表单/可见性/删除）、`admin-testcases.js`（隐藏用例管理）、
>   `admin-users.js`（用户列表/重置密码/改角色），并在 `router.js` 增加后端路由守卫。
> - **路由与访问检查**：新增 `#/admin`、`#/admin/problems`、`#/admin/problems/new`、
>   `#/admin/problems/{id}/edit`、`#/admin/problems/{id}/testcases`、`#/admin/users`。
>   只有满足「已登录 + 已完成首次改密 + 当前角色为 admin」的账号在导航栏与页脚显示管理
>   入口；直接访问后台路由时前端同样检查身份、角色与首改状态：游客重定向登录（带
>   `redirect`）、普通用户显示无权访问、未改密管理员引导到 `#/password`。前端检查仅用于
>   页面体验，真正权限仍由后端接口按数据库最新状态执行；使用期间权限被撤销（`403`）时
>   刷新本人状态并退出后台，不把所有 `403` 当作 token 失效，`401` 清理凭证并跳转登录。
> - **题目管理**：列表接入 `GET /api/problems` 的 `page`/`visible`（另附 `q`/`difficulty`）
>   查询，展示 ID、标题、难度、标签、可见性与通过人数，提供新建、编辑、用例管理与删除
>   入口。创建/编辑表单覆盖标题、纯文本题面、公开样例、难度、标签、时间限制（毫秒 ms，
>   默认 2000）、内存上限（千字节 KB，默认 65536）与可见性，严格沿用后端字段与单位，
>   前端基本校验、服务端校验为准。公开 / 隐藏通过 `PUT visible` 实现，成功后重新拉取列表，
>   失败保留真实状态并说明原因。删除前确认框展示题目身份与既定删除策略；已有提交时后端
>   `409`，页面解释不能删除的原因，不级联、不偷偷隐藏代替删除。
> - **测试用例**：读取 `GET /api/admin/problems/{id}/testcases`，按 `(ord ASC, id ASC)`
>   展示完整用例；公开样例（`is_sample=1`）只读并注明由题目表单维护，隐藏用例
>   （`is_sample=0`）支持新增、编辑、删除与 `ord`。输入/输出原样保存，不 trim、不归一化；
>   所有写请求同时携带题目 ID 与用例 ID，切换题目/快速导航时用 `disposed`/`token` 丢弃
>   过期响应，避免题目 A 的用例显示或保存到题目 B；隐藏用例不出现在公开详情。
> - **用户管理**：`GET /api/admin/users` 分页列表展示账号、昵称、角色、首改标记与注册
>   时间；提供密码重置与角色修改，不提供删除用户。重置密码使用密码输入控件，不显示明文、
>   不写日志、不做浏览器持久化，成功后清空并提示「下次登录后需改密」（沿用 M2.4 策略）；
>   角色修改前展示目标用户、原角色与新角色并确认，正确展示「最后一个管理员」保护
>   （`409`）与自我降级，自我降级成功后更新本机状态、隐藏管理入口并退出后台。
> - **状态与安全**：所有页面覆盖加载中、空列表、保存中、成功与失败状态；写入请求进行中
>   禁用重复提交，失败后恢复操作并尽量保留非敏感编辑内容；网络失败明确「无法确认后端
>   是否已执行」且不自动重试写入。题面、标签、昵称、用例与错误信息全部经 `textContent`
>   /`<pre>` 纯文本渲染，HTML 特殊字符不被执行；隐藏用例与用户列表不写入浏览器持久化
>   缓存，其中不混入公开页面。样式沿用既有主题，窄窗口下表单与用例编辑改为单列。
> - **验证**：真实启动服务（隔离临时库 + 随机端口 + 静态托管 `web/`），先在 jsdom 中执行
>   `web/js` 模块（连接同一服务）做 DOM 级验证，覆盖后台入口与访问检查、题目创建/编辑/
>   公开隐藏/删除与冲突、用例增改删与隐藏隔离及跨题不串用、用户列表与重置/改角色、最后
>   管理员与自我降级、权限被撤销、无效 token、网络失败、重复提交、XSS 文本渲染，且无浏览
>   器脚本错误。测试审查轮补充了表单校验负路径、列表筛选/空态、用例 `ord` 规则、公开样例
>   只读、可见性失败保真、保存中状态、敏感数据不入本地存储与分页等场景，最终 **31 个场景 /
>   115 项断言全部通过**；可重复运行的前端验证脚本入库于 `tests/frontend/`（可选、不注册
>   CTest）。随后执行全量常规回归 `ctest`，27/27 通过。真实图形浏览器渲染与手动点击仍为
>   未验证项（环境无可用浏览器）。

### M3 判题核心完整化（安全加固）

#### M3.1 判题任务调度

- [x] 实现 `JudgeManager` 与有界任务队列。
- [x] 接入 worker 线程池，数量为 `min(CPU 核数, 8)`。
- [x] 将提交接口接入线程池，保留同步返回方式。
- [x] 明确队列满载时的处理方式，避免无限积压。
- [x] 验证多个提交可以并发处理。

> 实施说明：
>
> - **调度分离**：新增 `src/judge/manager.{h,cpp}` 的 `JudgeManager`，统一负责任务接收、
>   入队、worker 调度与结果交付；单次判题仍复用 `JudgeEngine` + `LocalExecutor`，
>   持久化仍复用 `SubmitService`，不复制编译/运行/比对/落库逻辑。任务自带 `user_id` /
>   `problem_id` / `language` / `source_code` / `viewer_is_admin` / 原始提交时间，绝不
>   引用 HTTP 请求对象、回调局部变量或数据库语句对象。
> - **worker 数量**：`min(CPU 核数, 8)` 且至少 1。CPU 核数用 `sysconf(_SC_NPROCESSORS_ONLN)`
>   获取，失败退回 `std::thread::hardware_concurrency()`，仍为 0 或无法获取按 1 处理；
>   每个 worker 一次处理一个完整提交，同一提交的测试点不额外并行化。
> - **有界等待队列**：容量表示「已接收但尚未开始执行」的任务数，与正在执行数分开；
>   默认 `kDefaultQueueCapacity = 32`，经环境变量 `OJ_JUDGE_QUEUE_CAPACITY`（1..256，
>   非法启动报错退出）配置。入队判断与入队在同一锁内原子完成，并发无法突破容量。
> - **满载行为**：立即拒绝，`POST /api/problems/{id}/submit` 返回 `503` +
>   `{"error":"判题队列已满，请稍后重试","code":"JUDGE_QUEUE_FULL","retryable":true}`
>   与 `Retry-After` 头；前端沿用通用错误展示该文案、不自动重试。未接收请求不创建提交
>   记录、不增加提交次数。
> - **结果交付与异常**：每个已接收任务独立 `promise/future`，结果/异常必交付对应请求，
>   不串用、不重复完成、不永久等待；`handler` 抛异常被捕获为内部错误，worker 继续运行。
>   执行器异常在 `SubmitService` 内转换为 `SYSERR` 提交记录（沿用 JUDGE-12 约定）。
> - **执行器并发与 fork 安全**：`LocalExecutor` 无共享可变状态，可多 worker 并发使用；
>   移除 M1.6 的全局判题串行锁。子进程可执行文件在父进程内解析 `PATH` 后以 `execv`
>   启动，子进程不再调用非异步信号安全的 `execvp`；各任务只 `waitpid` 自己负责的子进程，
>   不互相抢退出状态。完整 watchdog 与强制终止仍属 M3.2。
> - **数据库并发**：连接以 FULLMUTEX 打开，单条语句线程安全；多语句持久化事务继续由
>   连接级事务互斥 + `BEGIN IMMEDIATE` 串行化，判题等待与编译运行期间不持有写事务。
>   并发计数、状态更新正确；`compute_status_update` 收敛首次 AC 为最早符合条件的原提交
>   时间，避免并发乱序完成导致首次 AC 时间错误；原始提交时间在入队时采集，排队时间不
>   计入运行耗时、不误判 TLE。
> - **HTTP 并发协调**：cpp-httplib 请求线程数显式设为 `worker 数 + 队列容量 + 8`，保证
>   同步等待判题最多占用 `worker 数 + 队列容量` 个线程，仍有保留线程响应健康检查与题目
>   查询；验证繁忙/满载时健康检查与题目查询仍可响应。
> - **生命周期**：`JudgeManager::shutdown()` 停止接收新任务、唤醒等待线程、排空所有已
>   接收任务并投递结果后回收 worker；`HttpServer::stop()` 先停 HTTP（含同步等待中的请求）
>   再停调度器，保证关闭数据库前无 worker 仍在使用数据库，无永久等待的 future。
> - **验证**：`tests/unit/test_judge_manager.cpp`（同步屏障/计数器，调度边界）、
>   `tests/integration/test_submit_scheduling_api.cpp`（HTTP 集成：503 不落库、繁忙可响应、
>   异常恢复、首次 AC 最早时间、排队不计时、真实 C++17/C11 并发、停止排空）与
>   `tests/unit/test_submit_unit.cpp`、`tests/unit/test_config.cpp`（首次 AC 收敛、队列容量
>   校验）；回归 `submit_api`、`judge_*`、`admin_*` 等全量 `ctest` 29/29 通过。
> - **边界**：未提前实现 M3.2～M3.6 的 watchdog 全局硬上限/强制终止、沙箱与资源限制、
>   完整分类与 Sanitizer、Rejudge；停止时排空已接收任务可能等待现有单次判题保护超时。

#### M3.2 子进程与超时控制

- [x] 完善 fork 子进程执行与父进程结果采集。
- [x] 实现 watchdog，执行题目时间限制。
- [x] 实现单次判题全局 60s 硬上限。
- [x] 实现超时 `SIGKILL` 与 `waitpid` 回收。
- [x] 覆盖编译失败、运行崩溃及服务停止时的子进程清理。

> 实施说明：
>
> - **fork 与结果采集**：`LocalExecutor::spawn`（`src/judge/local_executor.cpp`）继续以参数
>   数组 `execv` 启动，父进程按 `waitpid(pid)` 只回收自己负责的子进程（多 worker 不互抢退出
>   状态）；子进程侧在 `exec` 前只做异步信号安全操作（`setpgid`/`dup2`/`close`/`chdir`/
>   `execv`/`write`），不做日志、内存分配或业务逻辑。启动前在父进程解析可执行路径。
> - **启动失败与退出区分**：close-on-exec 状态管道回传 `exec` 失败的 `errno`，与「已启动后
>   正常退出 / 非零退出 / 信号终止」严格区分；编译器不存在、fork 失败等环境故障返回 `SYSERR`，
>   绝不标记为普通 `CE`；非零退出与信号终止即使输出碰巧匹配也判 `RE`。
> - **fd 卫生**：子进程 `dup2` 后用 `close_range(2)`（不可用时退回有界循环 `close`）关闭除
>   错误管道外的全部 fd，避免继承服务监听套接字、数据库连接与其它任务管道。所有管道以 RAII
>   关闭，成功/失败路径均不泄漏文件描述符。
> - **进程组边界**：每个编译/运行任务在子进程中 `setpgid(0,0)`，父进程以 `kill(-pid, SIGKILL)`
>   终止整个进程组，覆盖编译器后代（`cc1plus`/`as`）与用户程序 `fork` 的后代，而不是只杀外层
>   进程；即使外层已退出但后代仍持有输出管道，也会清理进程组后结束采集。该机制是普通进程组
>   管理，不是完整恶意进程隔离，进程逃逸等限制在 M3.3 完善。
> - **watchdog（单调时钟）**：`Deadline`（`src/judge/deadline.h`）基于 `steady_clock` 计算
>   截止时间与剩余毫秒（向上取整）；执行循环以短周期 `poll` 轮询并周期性比较截止时间，
>   即使程序没有任何输出也能及时发现超时，不依赖阻塞读写或等待自然退出。超时 `SIGKILL` 后
>   可靠 `waitpid` 回收（重试 `EINTR`）；处理「进程恰好在截止时退出」「读/写被信号中断」
>   「取消」等竞争。watchdog 为任务内同步逻辑，任务结束后不再有监控，不会误杀后续任务或
>   复用 PID。
> - **单点时限口径**：实际采用**墙钟经过时间**（非精确 CPU 时间）限制，从子进程启动到回收
>   计时；排队等待与编译时间不计入单个测试点的运行耗时（`runtime_ms` 为各点执行耗时之和）。
> - **全局硬上限**：`JudgeOptions::global_time_limit_ms` 默认 **60000ms**（SPEC JUDGE-10）；
>   在每次判题开始时确定截止时间，覆盖编译与全部测试点执行，不含队列等待；进入新测试点不
>   重置，题目时限为极大/无限制时仍然生效。每个测试点的有效时限取「题目时限（已按
>   `max_time_limit_ms` 裁剪）」与「剩余全局预算」的较小者。
> - **全局耗尽与结果保留**：剩余全局预算为 0 时不再启动新测试点，终止正在运行的进程组并保留
>   已获得的逐点结果；未执行的测试点不写入结果，绝不伪造成已运行或通过。
> - **状态分类（本次明确）**：
>   - 用户程序超过**单点时限** → 该点 `TLE`，普通失败不阻断后续测试点（全局预算允许时继续）；
>   - 普通源码编译错误 → `CE`；编译保护超时（未被全局裁剪）按 `CE` 处理并附诊断；
>   - **全局硬上限**触发（含编译预算被全局裁剪导致的超时）→ 终止剩余执行，总体 `SYSERR`
>     （内部兜底，不归咎于用户），触发点标记 `global_deadline_hit` 并以明确诊断区分；
>   - 编译器缺失/启动失败等环境故障、服务主动取消 → `SYSERR`（已有内部错误约定）；
>   - 多个逐点结果仍按 `SYSERR > TLE > MLE > RE > WA > AC` 汇总，但全局/取消终止会覆盖为
>     `SYSERR`。绝不新增未约定的持久化状态。
> - **服务停止取消**：`JudgeManager` 为每个已接收任务创建协作式 `CancellationToken`；
>   `cancel_all()` 停止接收新任务并取消全部已接收任务（正在执行的终止进程组、等待队列中的在
>   被取出后立即短路，不再启动新进程），已接收任务仍各自得到明确结果并持久化。`HttpServer::stop()`
>   在 `svr_.stop()`（等待同步等待判题的 HTTP 线程）**之前**先 `cancel_all()`，避免停止流程
>   与 HTTP 线程互相等待；随后 `shutdown()` 回收 worker，最后才关闭数据库，保证取消结果先落库。
> - **停止入口安全**：信号处理入口只置 `sig_atomic_t` 标志，复杂清理在正常控制流程中执行。
> - **清理边界**：工作目录只删除本次任务目录；进程清理按本次任务的进程组/pgid 进行，不以系统
>   范围的进程名匹配清理。
> - **验证**：单元 `tests/unit/test_judge_deadline.cpp`（截止时间与有效时限、全局硬上限跨点
>   与结果保留、编译保护超时与全局裁剪区分、协作式取消）；集成
>   `tests/integration/test_judge_integration.cpp` 新增全局硬上限、单点超时后续执行、编译保护
>   超时、后代进程组清理、取消终止运行、子进程不继承无关 fd、关闭标准输出、输出匹配但异常退出；
>   `tests/integration/test_submit_scheduling_api.cpp` 新增停止服务取消真实运行与排队任务并落库、
>   无遗留进程。全量 `ctest` **30/30** 通过。
> - **边界**：本阶段不实现 M3.3 的 seccomp、tmpfs 与内存限制，不实现 M3.4 的 ASan/UBSan 与
>   完整 MLE/异常分类，不实现 M3.6 Rejudge；进程组管理不声称完整恶意进程隔离。

#### M3.3 运行隔离与资源限制

- [x] 配置 tmpfs 判题目录。
- [x] 为每次提交创建独立随机运行目录。
- [x] 实现目录访问隔离与执行后清理。
- [x] 配置 `setrlimit` 资源限制，落实 CPU 与内存限制。
- [x] 配置 seccomp-bpf，限制网络、文件读写、`/proc` 访问及其他危险系统调用。
- [x] 实现输出采集上限 64KB。
- [x] 验证沙箱策略与编译、程序启动及 Sanitizer 运行兼容。

> 本小节可按运行目录隔离、资源限制、系统调用限制分批实现。

> 实施说明：
>
> - **工作目录（tmpfs）**：默认沿用 `/opt/oj-tmpfs`（`OJ_JUDGE_WORKSPACE` 可覆盖），
>   容量上限按 3.3 GiB 机器预算定为 **512 MiB**（`dependence.md` 3.9/8.6 节，含
>   fstab 与验证命令）。启动时检查该目录确为 tmpfs（`path_is_tmpfs`），并执行一次
>   真实沙箱自检（`LocalExecutor::sandbox_self_test`，以 `/bin/true` 验证命名空间/
>   最小根目录/setrlimit/seccomp），任一失败即报错退出，绝不降级为无保护执行。
>   无挂载权限的开发/测试环境可显式 `OJ_JUDGE_ALLOW_NON_TMPFS=1` 并显著告警；正式
>   部署必须挂载 tmpfs。本次本机无 sudo，未在 `/opt/oj-tmpfs` 实际挂载；已改用
>   `/dev/shm`（真实 tmpfs）端到端验证工作目录检查、启动与沙箱判题（见 8.5/8.6 节），
>   `/opt/oj-tmpfs` 的挂载仍属部署步骤。
> - **随机目录与清理**：每次判题以 `mkdtemp` 在基目录下原子创建 `oj_judge_XXXXXX`
>   （0700，随机后缀避免碰撞）；`Workspace` 析构仅删除本任务目录，并校验路径仍在
>   创建时基目录之下、若被替换为符号链接则只删除链接本身（不跟随、不越界）。
> - **目录访问隔离**：沙箱在 user/mount/pid/net/ipc/uts 命名空间内以 tmpfs 为根，
>   只读 bind `/usr` 并重建 `/lib`、`/lib64`、`/bin`、`/sbin` 符号链接，挂新 `/proc`
>   与最小 `/dev`；编译阶段将工作目录可写 bind 到 `/box`，运行阶段则把待执行程序复制
>   进沙箱自有的只读 `/box` tmpfs（不暴露宿主工作目录，也不依赖对父命名空间 tmpfs 的
>   只读重挂载——该操作在 `/dev/shm` 等挂载上会返回 EPERM）。用户程序无法读取沙箱外
>   文件/受保护目录，无法越权写入（`/box` 只读），可读自身可执行文件。实测样例确认：
>   读取宿主哨兵文件、`/etc/passwd`、对宿主路径与 `/box` 的写入均失败，宿主文件未被改写。
> - **setrlimit 与内存**：`RLIMIT_CPU`（单点时限向上取整 + 1s，编译更宽松）、
>   `RLIMIT_FSIZE`、`RLIMIT_STACK`、`RLIMIT_NOFILE`、`RLIMIT_CORE=0`；**内存不使用
>   `RLIMIT_AS`**（与 ASan/UBSan 的巨量虚拟地址预留冲突），改以 20ms 周期采样用户
>   进程 RSS，超限即 `SIGKILL` 进程组并标记 `memory_exceeded`→`MLE`，峰值写入逐点
>   结果。实测：测试限 32 MiB、受控分配 80 MiB 的样例判 `MLE` 且后续判题正常。
> - **seccomp-bpf**：手写经典 BPF，在父进程预构建、子进程 `prctl` 加载（避免多线程
>   fork 后分配）。拒绝网络（socket 家族、io_uring）、挂载/逃逸（mount/umount/
>   pivot_root/setns/open_tree/open_by_handle_at 等）、调试与内核接口（ptrace/bpf/
>   perf_event_open/keyctl/模块与 kexec 等）、时间/主机名修改及 x32 ABI；运行阶段
>   额外拒绝 `fork/vfork/clone/clone3`。文件访问以命名空间/chroot/只读挂载约束，而非
>   仅拦截 `open` 名称（动态加载器与 ASan 需读库与 `/proc/self`）；`/proc` 由 PID
>   命名空间隔离为仅本任务进程。实测：socket/socketpair/fork 均被拒，宿主 `/proc`
>   不可见。
> - **进程与 fd 卫生**：每任务独立进程组；运行阶段用户程序为 PID≠1 的载荷进程，另设
>   PID 1 init 回收孤儿（避免 PID 1 忽略默认信号把信号崩溃误判为 AC）。`close_range`
>   关闭无关继承 fd；子进程仅获受控最小环境，绝不传递 `OJ_JWT_SECRET`/
>   `OJ_ADMIN_PASSWORD`（实测未继承）。
> - **输出上限**：stdout 64 KiB、stderr 16 KiB、编译诊断 64 KiB，采集时按字节截断并
>   继续排空管道（不先无限读取）；实测 65535/65536 字节判 AC、65537 字节判非 AC 且
>   标记截断，1 MB stderr 有界采集且不挂死。
> - **编译并发门限**：`OJ_JUDGE_COMPILE_CONCURRENCY`（默认 2）单独约束高内存的编译
>   阶段（运行阶段仍为 worker=min(CPU,8)）；等待许可计入该次判题全局 60s 硬上限并可
>   被取消（`CompileGate` + `CompileGateGuard`，单元测试覆盖超时/取消/释放）。
> - **错误不降级**：沙箱初始化失败返回 `launch_error+sandbox_error`→`SYSERR`；工作
>   目录不可创建/写入失败→`SYSERR`；均保留明确诊断，不静默无保护执行、不永久等待。
> - **验证**：单元 `tests/unit/test_sandbox_unit.cpp`（18 项：编译门限、资源上限换算、
>   seccomp 过滤器、能力探测、Workspace 清理安全）；集成
>   `tests/integration/test_sandbox_integration.cpp`（真实 Linux 进程：正常 C++17/C11、
>   目录/网络/进程隔离、CPU/内存/输出限制、ASan/UBSan 兼容与越界诊断、环境不泄露、
>   失败路径与清理）；`test_config.cpp` 覆盖新环境变量。受影响回归 `judge_integration`、
>   `sandbox_unit`、`sandbox_integration`、`submit_api`、`submit_scheduling_api` 通过，
>   全量 `ctest` **32/32** 通过；4 路并发真实判题（编译门限 2）在 1 秒内全部 AC。
> - **边界**：ASan/UBSan 的默认接入与完整异常分类、内存/耗时汇总属 M3.4（本次仅以
>   `JudgeOptions::extra_compile_flags` 注入 `-fsanitize` 验证兼容性，未勾选 M3.4）；
>   真实 tmpfs 挂载需 root 权限，未在本机执行（后续 M5.3 第三/四轮已在生产路径
>   `/opt/oj-tmpfs` 的 namespaced 真实 tmpfs 与 systemd 持久挂载上补验，见
>   `tests/M5.3-test-report.md` 第 13/14 节）；受控样例通过不代表可安全公开运行任意
>   不可信代码，仍需 M3.4/M3.5/M5 的完整回归与验收。

#### M3.4 编译与结果分类

- [x] 完善 C++17、C11 两套编译模板及规定的编译选项。
- [x] 接入 ASan/UBSan，采集诊断信息。
- [x] 完善 AC、WA、CE、TLE、RE、MLE、SYSERR 分类。
- [x] 采集并汇总耗时、内存和编译信息。
- [x] 编译成功后执行全部测试点，单点失败继续；触发全局硬上限或无法继续的系统故障时终止，并返回对应状态及已获得的逐点结果。
- [x] 为 WA 测试点附加输入、期望输出与实际输出。
- [x] 验证编译环境等内部故障返回 SYSERR，不使服务假死。

> 实施说明：
>
> - **编译模板（SPEC JUDGE-01）**：`LocalExecutor::compile` 以参数数组 `execve`
>   启动，绝不经过 shell；C++17 `g++ -O2 -std=c++17 <src> -o <program> -lm`，C11
>   `gcc -O2 -std=c11 <src> -o <program> -lm`，默认叠加
>   `-fsanitize=address,undefined -fno-omit-frame-pointer -fno-sanitize-recover=all`。
>   用户源码与请求参数不能改变编译器路径、注入额外命令或绕过隔离。`JudgeOptions`
>   的 `sanitizers_enabled`（默认 `true`）统一开关，仅测试可显式关闭。
> - **Sanitizer 行为**：`-fno-sanitize-recover=all` 使 UBSan 报告未定义行为即中止，
>   杜绝「已报 UB 但继续执行、输出匹配被误判 AC」；运行环境 `UBSAN_OPTIONS` 设
>   `halt_on_error=1:print_stacktrace=1`，`ASAN_OPTIONS` 设 `detect_leaks=0`（与
>   禁 ptrace/禁创建进程的 seccomp 兼容，泄漏检测关闭不影响越界等诊断）。
> - **执行层结构化原因**：`ProcessResult.termination`（`TerminationReason`）为单一
>   权威来源，另有仅在异常终止时依据诊断标注的 `sanitizer_error`。分类层
>   `classify_case`（`src/judge/classification.{h,cpp}`）统一判定，优先级：
>   取消/启动失败 `SYSERR` > 可靠 RSS 证据 `MLE` > 全局裁剪超时 `TLE(=全局上限)`
>   > 单点超时 `TLE` > 信号/非零退出/非正常结束 `RE` > 输出超限 `RE` > `AC` > `WA`。
>   **不能仅凭用户可自行打印的 stderr 文本识别 Sanitizer 错误**，实际退出状态才是
>   判据；`MLE` 必须有 RSS 采样证据，SIGKILL/`bad_alloc`/分配失败无证据时按确定规则
>   处理（`RE`），不伪造确定性。总体汇总沿用 `SYSERR > TLE > MLE > RE > WA > AC`，
>   与遍历顺序无关。
> - **编译故障区分**：编译器不存在/不在 PATH/沙箱初始化失败等基础设施故障返回
>   `SYSERR`，不伪装为 `CE`；普通语法/类型/链接错误非零退出返回 `CE`。编译保护超时
>   （未被全局裁剪）按 `CE`，被全局预算裁剪则按全局硬上限 `SYSERR`（沿用 M3.2）。
> - **输出与诊断**：编译诊断、标准输出、标准错误分别有界采集（64KB/64KB/16KB），
>   截断显式标识（`compile_output_truncated`/逐点 `output_truncated`），标准错误绝不
>   混入用于答案比较的标准输出；编译诊断经 `scrub_compile_diagnostics` 清洗工作目录、
>   `.oj_sandbox` 与 `/box/` 等内部路径，仅保留定位用户代码所需信息。
> - **逐点与反馈**：编译成功后执行全部测试点，普通 WA/RE/TLE/MLE 不阻断后续点；全局
>   硬上限或无法继续的系统故障时终止剩余点，保留已获得结果、未执行点不伪造。WA 点
>   返回该点输入/期望输出/实际输出与结构化 `reason`/诊断，AC 点不含隐藏输入或答案。
> - **指标口径**：逐点 `time_ms` 为子进程墙钟经过时间（不含排队/编译），逐点
>   `memory_kb` 为 RSS 采样峰值（未采集为 `null`）；提交级 `runtime_ms` 为各点之和、
>   `memory_kb` 为各点峰值最大值；`compile_time_ms` 独立返回。排队时间不计入用户程序
>   运行时间。`runtime_ms`/`memory_kb` 随提交记录持久化，重启后可正确读取。
> - **接入**：判题结果经既有 `SubmitService` 单事务写入 `submissions`（源码、状态、
>   逐点 JSON、编译诊断、耗时/内存）与 `user_problem_status`；`POST .../submit` 响应
>   新增 `compile_time_ms`、真实 `memory_kb`、`compile_output_truncated` 与逐点
>   `reason`/`sanitizer_error`；前端 `judge.js` 分别展示运行耗时与编译耗时、峰值内存。
> - **验证**：单元 `tests/unit/test_judge_classification.cpp`（22 项：分类优先级、
>   多迹象、Sanitizer 文本不单独判失败、汇总顺序无关、诊断清洗）；集成
>   `tests/integration/test_m34_classification.cpp`（真实 g++/gcc + 沙箱：默认
>   ASan/UBSan 模板的 C++17/C11 AC/WA/CE、行尾空白、越界与 UB 判失败并留诊断、伪
>   Sanitizer 文本不误判、TLE/RE/MLE、超大输出与诊断有界、编译器故障 `SYSERR` 后恢复、
>   失败继续与混合优先级、指标口径）；`test_sandbox_integration.cpp` 的 UBSan 期望更新
>   为「不可恢复判失败」；`test_submit_api.cpp` 更新为真实峰值内存、验证持久化。
>   并发规模验证：`tests/integration/test_submit_scheduling_api.cpp` 的
>   `test_five_users_sustained_concurrency` 以 5 名用户并发、连续 3 轮共 15 次真实
>   ASan 判题（编译门限 2、worker=5）验证结果互不混用、计数一致、无遗留，实测最低可用
>   内存约 737 MiB；超出产品目标（5 人同时）的更高并发尚未验证。全量 `ctest` **34/34** 通过
>   （约 156s，单并发）。
> - **边界**：不实现 M3.5 的完整持久化与停止清理验收、M3.6 Rejudge。编译耗时/排队
>   时间为运行时/日志指标，未新增数据库列（数据模型仍为 SPEC 3.2 的 `runtime_ms`/
>   `memory_kb`）。

#### M3.5 持久化与停止清理

- [x] 完整保存判题结果、逐点信息和运行日志。
- [x] 验证失败及异常路径不会遗留子进程或运行目录。
- [x] 将队列、worker 与判题进程清理接入优雅停止流程。

> 实施说明：
>
> - **结果保存时机与内容**：判题（编译 + 逐点执行 + 比对 + 汇总）在事务外完成后，由
>   `SubmitService` 在**单个短事务**内写入 `submissions`（用户、题目、语言、完整源码、
>   最终状态、逐点 JSON、编译诊断、各点耗时之和、峰值 RSS、提交时间）并更新
>   `user_problem_status`（`submit_count`、`accepted`、`first_ac_at`）；事务不覆盖排队/
>   编译/运行阶段，任一写入失败整体回滚并返回通用 `500`，绝不声称已保存。逐点 JSON
>   保留 WA 输入/期望/实际输出、结构化 `reason`、`sanitizer_error`、`global_deadline_hit`、
>   编译诊断截断标识与有界诊断；**未采集内存以 `null` 表示、未执行测试点不写入结果**，
>   绝不用 0 伪装测量值、绝不把未运行点记为通过。沿用 M1.6/M3.4 已有表结构与字段，
>   未扩展数据模型。
> - **取消任务的状态与统计规则**：服务停止时等待队列中的已接收任务被取消、取出后立即
>   短路不再启动新进程；正在编译/运行的任务终止其进程组。取消按既有内部错误约定记为
>   `SYSERR`（明确「服务停止，判题已取消」，不归咎为学生的 TLE/RE），逐点已获得结果与
>   指标照常保存并计入提交次数。队列满载或调度器已停止而**未接收**的请求返回 `503`
>   （`JUDGE_QUEUE_FULL`/`JUDGE_UNAVAILABLE`），不创建记录、不增加次数。
> - **责任归属与终态保护**：每个被接收任务由 `JudgeManager` 恰好执行一次 handler，结果
>   经独立 `promise/future` 交付，正常完成与取消竞争时只产生一条提交记录与一次计数
>   （`test_cancel_completion_race_single_final` 与 `test_manager_handler_invoked_once_under_race`
>   各轮断言 handler 恰好执行一次、`submit_count`/状态行精确为 1）。任务在接收时分配
>   单调递增标识，接收/开始/完成/取消日志均带该标识；`shutdown()` 核对「已接收/已完成」
>   计数，不一致时记录错误而非宣称全部已保存。
> - **客户端断开 vs 服务器取消**：客户端在提交被接收后断开不会导致任务丢弃——判题继续
>   执行并落库（`test_client_disconnect_keeps_result` 以原始 socket 发送完整请求、任务
>   执行中关闭连接，断言结果仍保存且未被删除）；网络响应失败不回滚已提交事务。
> - **停止顺序与等待预算**：信号入口（`SIGINT`/`SIGTERM`）只置 `sig_atomic_t` 标志，复杂
>   清理在主循环外执行。`HttpServer::stop()` 顺序为：①`cancel_all()`（非阻塞通知取消、
>   停止接收）；②`svr_.stop()` 停止监听并等待 HTTP 处理线程（含同步等待判题的请求）
>   结束；③`shutdown()` 回收 worker（所有已接收任务的结果已先写库）；④主流程最后关闭
>   数据库。避免「HTTP 等判题、判题又等 HTTP」的互相阻塞。取消以约 20ms 周期被观察，
>   单次判题仍有全局 60s 硬上限兜底；停止耗时与已接收/已完成计数写入日志，超过 30s
>   预算仅告警，不强制杀死线程、不宣称已保存全部结果。重复/并发停止幂等（`std::once`
>   语义 + `shutdown_mutex_`），不死锁、不重复回收。
> - **清理边界与可重复性**：清理只针对本任务拥有的工作目录（`Workspace` 校验路径仍在
>   创建基目录之下、符号链接只删链接本身）与进程组（`kill(-pgid)`），不按进程名批量杀
>   进程、不递归清理未确认路径、不卸载共享 tmpfs；资源已不存在时静默收尾，真正的清理
>   失败（`remove_all` 返回错误码）记录具体资源路径并保留现场，不吞异常后报成功。结果
>   已入库但清理失败时保留已保存数据，仅单独报告清理问题（`test_m35_cleanup_unit`：
>   日志含资源路径、不误删他人目录、已不存在时可重复收尾）。
> - **日志**：判题任务接收/开始/完成（含状态）/取消/排空核对均带任务标识；提交成功与
>   持久化失败日志带提交/用户/题目（沿用 M1.6/M3.1）；普通运行日志不倾倒完整源码、隐藏
>   用例、密码或 token，未引入新日志平台。
> - **崩溃保障边界**：判题结果在判题结束后才入库，故进程被 `SIGKILL` 或整机断电时，
>   进行中（尚未完成）的任务可能丢失；正常停止流程保证先落库再关闭数据库。已有事务
>   提交的数据可在重启后读取（`test_restart_after_stop_consistent`）。本阶段未新增
>   持久化任务状态或恢复机制（避免擅自扩展数据模型）；如后续需要崩溃恢复，可考虑在
>   接收时写入一条 status='JUDGED' 之前的占位行并携带任务标识，但需先调整数据模型（该
>   恢复机制已立项于 M3.7）。
> - **验证**：单元 `tests/unit/test_m35_cleanup_unit.cpp`（2 项：清理失败可定位且不误删、
>   资源已不存在时可重复收尾）；集成
>   `tests/integration/test_m35_persistence_shutdown.cpp`（AC/WA/CE/SYSERR 关闭重开可读、
>   事务回滚、数据库锁短期竞争成功/长期竞争 5s 有界失败、完成/取消竞争只最终处理一次、
>   客户端断开保留结果、空闲/编译中/结果保存阶段停止与停止中新提交拒绝、并发重复停止
>   幂等、停止后无遗留子进程/判题目录、失败路径反复执行 fd/线程/内存无持续增长、停止
>   重启后数据一致且可继续判题）。停止取消真实运行/排队任务、子进程回收与真实并发判题
>   复用 M3.2/M3.1 的 `submit_scheduling_api`。全量 `ctest` **36/36** 通过（约 174s，
>   单并发）；M3.5 集成测试实测约 13s、最大常驻内存约 77 MiB；构建固定 `--parallel 1`。
> - **边界**：不进入 M3.6 Rejudge，不实现分布式队列或崩溃恢复平台（进程崩溃/断电后的
>   在途任务恢复已立项于 M3.7，尚未实现）；受控测试使用隔离
>   临时库/随机端口/专用判题目录，未停止正式服务、未修改正式提交记录。本机 `/opt/oj-tmpfs`
>   未挂载（无 sudo），真实进程验证沿用 `/dev/shm`/临时目录；tmpfs 挂载仍属部署步骤
>   （后续 M5.3 第三/四轮已在 namespaced 真实 tmpfs 与 systemd 持久挂载上补验，见
>   `tests/M5.3-test-report.md` 第 13/14 节）。

#### M3.6 Rejudge

- [x] 实现管理员 Rejudge 接口及权限检查。
- [x] 使用保存的源码、语言与当前测试用例重新判题，更新原提交结果，不增加提交次数。
- [x] 重新计算受影响的用户题目状态；首次 AC 时间取重判后仍为 AC 的最早原提交时间，无 AC 时清空。
- [x] 更新排行榜所依赖的统计数据，供 M4.5 查询与展示。
- [x] 在后台增加 Rejudge 操作与结果反馈。

> 实施说明：
>
> - **接口与调度**：`POST /api/admin/submissions/{id}/rejudge` 复用 `require_admin`
>   （已登录 + 已完成首次改密 + 当前数据库角色为 admin）；重判任务经 `JudgeManager`
>   的有界队列、worker、编译并发门限、取消机制与结果通道执行，与普通提交共用容量，
>   HTTP 同步返回。非法 ID `400`、记录不存在 `404`、未登录 `401`、权限不足/须改密
>   `403`、同一提交重复重判 `409 REJUDGE_IN_PROGRESS`、队列满载/调度器停止 `503`
>   （`JUDGE_QUEUE_FULL`/`JUDGE_UNAVAILABLE`）。
> - **数据来源**：源码、语言、用户归属与 `created_at` 全部从 `submissions` 原记录读取，
>   客户端请求体任何字段都不能替换；题目时限/内存与当前完整测试用例在判题前构造内存
>   快照，判题期间修改用例不会让同一次重判混用不同版本。管理员可重判隐藏题目的提交。
> - **持久化与状态重算**：在原记录上更新 `status/per_case/compile_msg/runtime_ms/
>   memory_kb`，不新增提交记录、不增加 `submit_count`；结果更新与 `user_problem_status`
>   重算在同一个短事务内完成——仍有 AC 提交时 `status='accepted'` 且 `first_ac_at` 取
>   最早 AC 的原提交时间，无 AC 时清空为 `none`，`submit_count` 不变；题目列表
>   `pass_count` 与该状态同源，随重判结果一致变化。
> - **并发**：按提交 ID 去重（完成/失败/取消后可靠释放），不同提交的重判与普通提交并发
>   时在数据库事务内依据最新提交记录重算状态，不丢新 AC、不覆盖首次 AC 时间。
> - **失败策略（本次确定）**：重判最终状态为 `SYSERR`（服务取消、全局硬上限、环境故障等）
>   时视为「无法完成本次重判」，保留原结果与统计并返回 `500` 及原因；持久化事务失败整体
>   回滚。普通结果（AC/WA/CE/TLE/RE/MLE）正常覆盖原记录。
> - **后台**：`#/admin/rejudge` 提供按提交 ID 发起重判的表单、操作确认（说明更新原结果
>   且不增加次数）、等待态禁用与结果/诊断展示，网络失败不自动重试；M4.4 完整提交历史
>   不在本阶段范围。
> - **验证**：新增 `tests/integration/test_rejudge_api.cpp`（11 场景 / 116 项断言，
>   含真实 g++/gcc 的 C++17/C11 AC↔WA、字段不可替换、状态重算、并发去重/队列满载、
>   SYSERR 与服务取消保留、客户端断开后重判仍收尾且去重释放、隐藏题提交、重启持久化）
>   通过；全量常规回归 `ctest` **37/37** 通过（约 187s，单并发）。前端已在
>   `tests/frontend/admin_pages_dom.mjs` 补充 S22 重判场景并完成 jsdom DOM 级执行：
>   **127/127** 通过（入口、确认与不增次数说明、等待态禁用、结果与诊断展示、记录
>   不存在与网络失败提示、不泄露无权信息）。DOM 验证过程中发现并修复一处前端缺陷：
>   `web/js/pages/admin-rejudge.js` 原将「重判完成」提示放入结果区，被
>   `renderJudgeResult` 的 `replaceChildren()` 清除，改为独立状态区承载；同时修正
>   测试场景顺序（S22 不得在 S21 自我降级之后使用已降级的 admin token）。修复后复测
>   127/127 通过。

#### M3.7 崩溃恢复与在途任务持久化

- [x] 明确崩溃保障边界与恢复目标：正常停止保证结果先落库；进程被 `SIGKILL` 或整机断电后，在途（已接收、尚未完成）任务可被识别，并能恢复或明确标记为中断。
- [x] 设计并落地在途任务持久化方案：提交在**入队/开始执行前**先写入一条可恢复的在途记录（含用户、题目、语言、完整源码、原提交时间与任务标识），使崩溃不丢源码与任务归属，且在途记录**不计入** `submit_count`、不设置 AC 状态。
- [x] 实现启动时恢复扫描：对崩溃前未完成的任务按固定策略处理——基于已保存源码**重新入队判题**，或按内部错误约定标记为中断（含明确原因）；绝不重复计数、不重复 AC、不把未运行点记为通过。
- [x] 数据模型扩展与迁移：新增在途状态/恢复元数据字段或独立在途表；迁移旧库不丢数据、不清空，保持既有读取接口兼容；若引入新状态，须同步状态机、统计口径与前端展示（不静默改变口径）。
- [x] 保证恢复与正常流程不冲突：恢复期间不产生重复终态（同一任务只最终落库一次），首次 AC 时间按原提交时间正确收敛，排行榜/状态统计与恢复结果一致。
- [x] 验证：模拟“接收后崩溃”（遗留未完成在途记录）重启后可识别并按策略恢复或标记中断；崩溃恢复不重复计数、不重复 AC；恢复后服务可继续正常判题；旧库迁移与数据完整；补充单元/集成测试并更新文档。

> **已通过独立测试验证**：新增单元 `tests/unit/test_m37_in_flight_unit.cpp`（8 用例）与
> 扩展 `tests/unit/test_judge_manager.cpp`（`JudgeManagerReservation` 5 用例）、集成
> `tests/integration/test_m37_recovery_api.cpp`（11 场景 / 103 断言）；全量常规回归
> `ctest --parallel 1` **39/39 通过**。完整逐项结果见 `tests/M3.7-test-report.md`。

> 实施说明（M3.7 已实现）：
>
> - **数据方案（本次确定）**：新增**独立表** `in_flight_tasks`（SPEC 3.2），不在
>   `submissions` 上扩展 `status='PENDING'` 或新增结算标记。原因：`submissions.status`
>   的 CHECK 约束只允许终态；复用该表会使在途记录进入既有 `submit_count`/AC/通过人数/
>   排行榜查询，需在所有读取处过滤，且旧库迁移需重建表。独立表天然与终态隔离，迁移
>   仅 `CREATE TABLE IF NOT EXISTS`（不重建、不清空），已有终态提交不会被识别为待恢复
>   任务。字段含 `task_id`（持久化任务标识，`UNIQUE`，跨重启定位同一任务）、
>   `user_id/problem_id/language/source_code/submitted_at`、`state`、`owner/claimed_at`。
> - **接收与结算流程**：`POST .../submit` 顺序为「鉴权/参数/可见性检查 → 容量预留
>   （`JudgeManager::reserve`）→ 持久化在途记录（独立短事务）→ 入队（`commit`）」。
>   未通过检查、容量不足不产生在途任务；写库失败释放预留并返回 `500`，不声称已接收；
>   写库成功后才入队，故「已接受但没保存」「明确拒绝却仍执行」均被排除。任务完成时在
>   原短事务内**先删除在途记录（`changes==1` 才继续）再写入 `submissions` 与更新
>   `user_problem_status`**，同一任务只结算一次；删除未命中即回滚，不写第二条记录。
>   首次 AC 时间沿用 `submitted_at`（原提交时间）收敛，排队/恢复时间不计入运行耗时或
>   首次 AC 口径。
> - **启动恢复**：数据库迁移与判题环境检查完成后、开始接收新提交前，`RecoveryService`
>   扫描 `state='pending'` 记录：启动时先将上次崩溃遗留的 `claimed` 重置为 `pending`
>   （解除失效占用），随后分批（默认每批 16 条）读取并逐条原子认领
>   （`pending→claimed`），再经 `reserve` 等待队列容量后重新入队。恢复使用**当前题目
>   配置与测试用例**（与 Rejudge 同口径，不持久化判题快照），并保留原 `submitted_at`；
>   题目不存在等无法判题时置 `interrupted` 保留任务信息。队列暂满时等待重试、绝不丢弃；
>   每次投递仅少量源码驻留内存。恢复任务与普通提交共用同一 worker 池、有界队列、编译
>   门限与沙箱，不新开无限制执行路径。
> - **多实例互斥**：`main` 启动时对 `<db_path>.lock` 取 `flock(LOCK_EX|LOCK_NB)`，
>   进程崩溃由操作系统自动释放（失效占用自然解除）；即便该保护被绕过，任务级原子认领
>   与结算事务的删除唯一性仍保证同一任务不产生重复终态。不引入分布式平台。
> - **删题保护（对 M2.1 的明确扩展）**：题目存在未结算在途任务（`pending`/`claimed`）
>   时 `DELETE /api/admin/problems/{id}` 与「已有提交」一样返回 `409`；无提交且无在途
>   任务时连同用例、做题状态、残留 `interrupted` 记录一并删除。检查与删除复用同一
>   `BEGIN IMMEDIATE` 事务与事务互斥锁，避免并发接收绕过。**行为变化**：判题进行中删题
>   由「删除成功、提交失败」改为「删除被拒」，已同步更新 `test_admin_problems_api` 的
>   对应场景并经回归验证（见 `tests/M3.7-test-report.md`）。
> - **Rejudge 兼容**：重判不写、不读在途表，仍沿用「以原提交记录为数据源 + 单一短事务
>   更新」；重判崩溃时原提交记录保持原样（未见半更新），不改变 M3.6 的失败处理策略。
>   本节恢复范围仅覆盖**新提交**的在途任务。
> - **故障保障边界**：SQLite 仍为 WAL + `synchronous=NORMAL`（沿用既有配置，未擅自
>   调整）；`INSERT` 返回成功仅表示已提交到 WAL，进程 `SIGKILL` 一般可恢复，整机断电
>   的持久性受底层存储与 `synchronous` 级别限制，不承诺绝对保障。崩溃时正在执行的旧
>   子进程/工作目录沿用现有进程组与 `Workspace` 生命周期清理，不依据数据库中的旧
>   PID 杀进程、不按目录名批量删除。恢复扫描使用当前配置，旧执行进程不会被「接管」。
> - **未新增对外接口/页面**：恢复为内部启动流程；既有提交读取与统计接口口径不变，
>   前端不为内部恢复机制新增页面。

### M4 前端完整页面

#### M4.1 页面基础设施

- [x] 完善 hash 路由，整合全部页面。
- [x] 完善导航、昵称展示和退出登录。
- [x] 完善 API 错误处理和 401 跳转登录。
- [x] 实现受保护页面登录后返回原目标页。
- [x] 接入管理员首次强制改密页面流程。

> 实施说明（M4.1 功能已实现，待独立测试验证）：
>
> - **路由整合与访问条件**：`web/js/router.js` 集中维护路由匹配、参数解析与访问条件，
>   路由以 `access`（`public`/`auth`/`admin`）声明。已接入题目列表、题目详情、注册、
>   登录、改密及全部后台页面与既有 Rejudge 入口，保持链接兼容；未提前实现 M4.4 提交
>   历史与 M4.5 排行榜页面，导航与页脚不提供会进入空白页的入口。默认空 hash 回退
>   `#/problems`；未知路由显示「页面不存在」；非法路由参数/编码异常（`decodeURIComponent`
>   失败、控制字符）显示「地址参数无效」；直接打开带 hash 的地址、刷新与浏览器前进
>   后退走同一套规则。
> - **身份状态机**：`web/js/auth.js` 区分 `unknown`（有 token 待核实）、`guest`、
>   `authenticated`。新增 `web/js/session.js` 的 `ensureAuth` 单飞调用 `GET /api/me`
>   核实当前用户，后台访问权限依据服务端最新角色与 `reset_pwd_flag`，不凭本地保存的
>   角色；核实期间导航不显示后台入口/昵称，避免初始化误跳转或短暂显示无权内容。角色
>   与首改状态经 `setUser` 更新后同步刷新导航与路由判断。
> - **导航与退出**：`web/js/nav.js` 按认证状态渲染昵称、管理员标记、登录/注册入口、
>   管理后台入口（导航栏与页脚）与退出登录，当前页高亮按路径边界匹配。退出登录清理本地
>   凭证、用户状态、待返回目标，重置会话核实与错误去重标记，跳转公开页面；`epoch` 机制
>   使退出前发起、退出后才返回的旧请求无法恢复已退出的身份。界面不声称前端退出已撤销
>   服务端 JWT。
> - **API 错误处理**：`web/js/api.js` 保留统一封装、Bearer token、JSON 解析与网络异常，
>   并保留 `status`/`code`/`retryable` 供页面按接口与错误约定分支（不依赖中文文案）：受
>   保护请求 `401` 清理凭证并只触发一次登录跳转；登录失败 `401` 只显示表单错误；改密旧
>   密码错误 `401` 按表单错误处理、不误退出；`403 PASSWORD_CHANGE_REQUIRED` 进入改密
>   流程；普通 `403` 显示权限不足并按需刷新身份；`404/409/429/503`（含 `JUDGE_QUEUE_FULL`
>   / `JUDGE_UNAVAILABLE` / `REJUDGE_IN_PROGRESS`）显示对应业务提示且不自动重试；网络
>   中断恢复可操作状态并准确提示。并发 401 或改密要求只做一次状态转换。
> - **登录后返回原目标**：`web/js/storage.js` 保存经 `sanitizeTarget` 校验的站内目标，并
>   在登录页 `redirect` 参数中携带。`completePostAuthRedirect` 在跳转前重新校验目标合法性
>   与当前用户权限，只接受可匹配路由且非流程页的站内地址，拒绝外部 URL、`//` 协议地址、
>   反斜杠/控制字符/恶意编码，避免开放重定向；普通用户不因目标指向后台而越权，无效/无权
>   目标回退 `#/problems` 并说明。登录/注册/改密流程页不互相回跳；注册成功仍展示分配
>   账号，不改为自动登录。
> - **首次强制改密**：登录响应、`GET /api/me` 与后端 `403 PASSWORD_CHANGE_REQUIRED` 任一
>   触发同一流程；登录后若 `reset_pwd_flag=1`，保留原目标先到 `#/password`；改密页本身
>   允许该用户访问，不被守卫反复拦截；未改密账号进入后台被引导改密。改密成功后回查
>   `/api/me` 刷新状态，再按当前权限返回原目标；沿用既有 JWT 策略，不假定旧 token 失效。
> - **生命周期**：`web/js/lifecycle.js` 为每次渲染提供统一的过期/清理机制，页面读取经
>   `signal`/代次丢弃过期响应，切换页面统一清理，避免旧响应覆盖新页面或累积监听；写请求
>   不随页面切换取消（前端停止等待不代表后端取消）。
> - **请求超时与启动排错**：`web/js/api.js` 对所有请求设 15s 超时（`DEFAULT_TIMEOUT_MS`，
>   可用 `timeoutMs` 覆盖），后端不可达/连接被接受却无响应时显示明确失败并可重试，不再
>   永久停留「加载中」，且超时不清理有效 token；写请求仍按“结果无法确认、不自动重试”
>   处理。新增 `scripts/run_dev_server.sh` 处理判题目录 tmpfs 与首次初始化 admin 密码，
>   避免服务启动即退出（退出码 1）导致端口转发后页面空白/一直加载。
> - **范围与验证**：本轮仅做基础设施与已有页面的必要适配，不重做视觉，不触及 M4.2～M4.5。
>   前端开发说明见 `README.md`「前端基础设施（M4.1）」。
> - **已通过独立测试验证**：逻辑层 `tests/frontend/m41_logic_test.mjs`（73 项）与页面级
>   `tests/frontend/m41_infrastructure_dom.mjs` + `run_m41.sh`（45 项）通过；新增真实浏览器
>   验证 `tests/frontend/browser/`（`playwright-cli` + 真实 Chromium，桌面 1280×800 与窄视口
>   360×640 / 390×844 / 768×1024，37 项）通过；受影响旧功能 M2.5 后台页面 DOM 回归
>   `tests/frontend/admin_pages_dom.mjs`（31 场景 / 128 项）通过；后端未改，抽样
>   `static_files_unit`、`http_server_unit` 通过。测试中发现并修复 `router.renderBadAddress`
>   非法参数被 not-found 兜底覆盖的缺陷。完整逐项结果见 `tests/M4.1-test-report.md`。
>   真实浏览器本轮以服务器本机 Chromium 执行：无头整轮 37/37，另以 Xvfb 虚拟显示
>   （`--headed`）复跑同一场景 37/37、0 脚本错误并留 6 张截图。Windows 转发链路已由用户
>   在真实浏览器手动验证通过（游客 → 登录 → 首次强制改密 → 进入主页面），原先的“空白/
>   一直加载”问题已定位为后端未运行/启动失败并修复（`scripts/run_dev_server.sh` +
>   `api.js` 请求超时）。服务器虚拟显示的有头运行不等同于 Windows 有头；Windows 有头
>   完整场景脚本（`browser/run_m41_browser.ps1 -Headed`）仍为可选复核项，本环境无法观测
>   其截图与 Network 明细。

#### M4.2 题目列表

- [x] 按主页结构完善表格及页面布局。
- [x] 接入搜索、难度筛选和标签筛选。
- [x] 接入分页及行点击跳转。
- [x] 展示通过人数和已登录用户的 AC/未AC 状态。
- [x] 实现管理员隐藏题目的查看与筛选交互。

> 实施说明（M4.2 功能已实现，待独立测试验证）：
>
> - **页面实现**：`web/js/pages/problems.js` 重写为 SPEC 2.6.1 主页结构，复用 M4.1 的
>   hash 路由、身份状态机、API 错误处理与页面生命周期，未改导航/主题，不做无关全站
>   视觉改造。列为 `#`、本人状态（仅登录用户）、标题、难度、标签、通过人数，管理员额外
>   显示可见性列；昵称、标题、标签等一律 `textContent` 纯文本渲染。
> - **真实字段**：使用 `GET /api/problems` 的 `pass_count`（不同用户通过人数）、
>   `solved`（当前登录用户 AC 状态；无记录为未 AC，不细分为后端未提供的状态）、
>   `difficulty`（映射易/中/难）。游客不显示本人状态列。
> - **组合筛选与标签来源**：接入 `q`/`difficulty`/`tag`/`page`/`visible`，条件可组合，
>   任一变化回第 1 页；空值不发送，界面「全部」不作为实际难度/标签。标签选项来自新增的
>   最小接口 `GET /api/problem-tags`（见 SPEC 3.3），在完整可见范围去重、按字节稳定
>   排序，游客/普通用户不含仅隐藏题目使用的标签，管理员含隐藏题目标签；不从分页结果
>   拼凑，按会话缓存并在进入页面时刷新，已选标签缺失时保留，翻页不会使其消失。搜索由
>   按钮/回车明确触发（不逐键请求），难度/标签/可见性变更即时应用。
> - **分页与路由状态**：使用后端 `page`/`page_size`（固定 20）/`total`/`total_pages`，
>   展示总题数与当前页，提供上一页/下一页与紧凑页码范围；超出末页或空结果页码 > 1 时
>   修正到合法页并重取，不在前端二次分页。筛选、搜索、页码（管理员另含可见性）承载于
>   `#/problems?...` 查询串，经 `URLSearchParams` 编码，刷新/前进后退可恢复；从详情页
>   返回时用 `sessionStorage` 记录的列表地址恢复条件与页码并重新拉取。URL 中非法难度/
>   可见性归一为「不限」、非法页码归一为 1，避免请求循环。标题保留真实链接（支持键盘
>   与中键/新标签打开），整行可点击，行内交互控件不误触发跳转。
> - **身份与管理员范围**：本人状态只取自已验证身份（不接受客户端 `user_id`）；仅通过
>   权限检查的管理员显示「全部/公开/隐藏」筛选并标识隐藏题目，普通用户/游客残留
>   `visible` 参数也不生效，后端继续执行最终可见性检查。身份/角色/首改状态变化时按最新
>   状态重建；退出或降级后清除本人状态与管理员内容，隐藏题目筛选回到合法普通列表条件。
> - **请求与页面状态**：区分首次加载、条件更新、空题库、无匹配结果（提供清空入口）与
>   请求失败（提供重试）；读取请求可取消或按代次丢弃，只有最新结果更新列表；页面销毁
>   取消未完成读取并解除身份订阅，避免重复进入累积请求。查询失败保留已应用条件，不把
>   所有错误当作登录失效（复用 M4.1 错误处理）。
> - **范围**：未进入 M4.3（CodeMirror/做题页）、M4.4 提交历史与 M4.5 排行榜；仅新增
>   标签选项所需的最小后端读取能力（`ProblemStore::list_tags` + `GET /api/problem-tags`）。
> - 页面与接口说明见 `README.md`「题目列表页（M4.2）」与「题目标签选项接口」；本轮未
>   编写或运行测试，功能与回归验证由后续独立任务完成。

#### M4.3 题目与做题页面

- [x] 完善左侧题面、样例、限制及本人状态展示。
- [x] 通过 CDN 引入 CodeMirror，替换 `textarea`，接入 C/C++ 高亮。
- [x] 实现语言切换与 `Ctrl+Enter` 提交。
- [x] 完善提交中、提交成功及请求失败状态。
- [x] 展示返回的全部测试点结果，包括状态、耗时和内存。
- [x] 展示 WA 测试点的输入、期望输出与实际输出。
- [x] 展示编译错误及其他判题诊断信息。

> 实施说明（M4.3 已完成并已通过独立测试验证）：
>
> - **编辑器（CodeMirror 5）**：通过 CDN 引入固定版本 **5.65.21**（cdnjs，
>   `https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.21/`），加载
>   `codemirror.min.css`/`codemirror.min.js` + `mode/clike` 及
>   `matchbrackets`/`closebrackets`/`active-line`/`placeholder` addon；仅使用 5.x
>   API，不使用 `latest`；`cpp17`→`text/x-c++src`、`c11`→`text/x-csrc`。切换
>   语言只更新模式，不清空源码，也不使用会覆盖已编辑内容的语言模板。接入代码见
>   `web/js/editor.js`，页面见 `web/js/pages/problem.js`。
> - **加载失败降级**：动态注入并设 8 秒超时，CDN 失败/超时/初始化异常时保留可编辑的
>   `textarea#source-code` 并明确提示，页面不空白、源码不丢失；编辑器就绪前提交逻辑
>   经 `getValue()` 读取 `textarea`，始终有确定数据来源。
> - **提交行为**：按钮与 `Ctrl+Enter` 共用同一入口，执行相同的登录/必要改密/语言/
>   源码/提交中检查，`submitting` 去重不会重复提交；提交瞬间保存题目 ID、语言与源码
>   快照，等待期间编辑不改变已发出的提交。请求期间禁用按钮并显示「判题中」，不伪造
>   进度；判题结果与请求失败分开处理，网络中断说明结果无法确认、保留源码与语言选择、
>   不自动重试；页面切换后旧响应不写入新页面。
> - **结果展示**：显示提交 ID、总体状态、运行耗时（不含排队/编译）、编译耗时、峰值
>   内存与提交时间；未采集指标显示「未采集」而非 0。逐测试点按后端顺序显示状态/耗时/
>   内存/原因，部分执行（全局硬上限/服务取消/内部故障）明确说明，未执行点不伪造成
>   通过；WA 点展示输入/期望/实际输出并区分空串与缺失、标识截断；编译诊断、标准错误
>   与标准输出分开、截断标识；长输出可滚动；全部按纯文本渲染。
> - **本人状态**：左侧仅对登录用户显示本人该题 AC/未 AC，游客不显示；数据来自详情
>   接口新增的 `solved`（后端 `user_problem_status`，用户身份来自后端鉴权），不使用
>   「列表第一页是否包含该题」推断；判题后从该接口刷新，不把本次结果直接映射为永久
>   状态，与 M4.2 返回列表时刷新一致。
> - **释放与布局**：通过 `lifecycle.onDispose` 在页面销毁时 `toTextArea()` 还原、
>   断开 `ResizeObserver` 与窗口监听，重复进入不产生重复编辑器/提交事件；布局变化
>   刷新尺寸。桌面左右分屏，窄视口（≤900px）上下排列。
> - **接口最小补充**：`GET /api/problems/{id}` 登录时新增 `solved`；提交响应在 M3.4
>   字段基础上补充逐点 `output_truncated` 与提交级 `global_deadline_hit`/`cancelled`，
>   用于标识截断与部分执行原因；未下发隐藏用例、未新增管理员用例读取。
> - **范围**：不实现 M4.4 完整提交历史/`GET /api/status` 与 M4.5 排行榜，不改写判题
>   分类、统计或崩溃恢复规则；未新增草稿、隐藏用例或诊断的额外持久化。

> **已通过独立测试验证**：新增单元 `tests/unit/test_problem_owner_status_unit.cpp`
> （4 用例，覆盖 `viewer_solved` 的 accepted/none/无记录/游客/题目隔离）、集成
> `tests/integration/test_m43_problem_page_api.cpp`（59 项断言，覆盖详情 `solved` 与
> 鉴权可见性、提交响应新增字段、截断、执行阶段与编译阶段全局硬上限、服务取消）、页面级
> `tests/frontend/m43_problem_page_dom.mjs` + `run_m43.sh`（jsdom，54 项断言，覆盖题面/
> 样例/限制/本人状态、降级 textarea、语言切换、`Ctrl+Enter` 去重、结果渲染规则、
> 编辑器适配与释放、网络失败保留源码）、真实浏览器
> `tests/frontend/browser/m43_browser_scenarios.js` + `run_m43_browser.sh`（Chromium，
> 28 项断言，覆盖真实 CDN CodeMirror 5.65.21 加载与 C/C++ 高亮、行号、语言切换、
> ResizeObserver 触发的 refresh 实际布局调整、360/390/640 窄视口单列布局与可操作性、
> 离开页面 `toTextArea` 释放）。前端覆盖率经 c8 采集（`run_m43_coverage.sh`：
> editor.js 81.0%、judge.js 89.0%、problem.js 86.1% 行覆盖）。全量常规回归
> `ctest --parallel 1` **41/41 通过**。完整逐项结果见 `tests/M4.3-test-report.md`。

#### M4.4 提交历史与详情

- [x] 实现本人提交历史接口及页面。
- [x] 实现提交详情接口，限制本人或管理员访问。
- [x] 展示源码、编译信息及逐测试点结果。
- [x] 实现 `GET /api/status` 并接入相关状态展示。

> 实施说明（已通过独立测试验证）：
>
> - **接口**：`GET /api/submissions?mine`（本人历史，需登录，分页、最新优先
>   `created_at DESC, id DESC`、可选 `problem_id` 筛选）；`GET /api/submissions/{id}`
>   （详情，本人/管理员）；`GET /api/status`（本人题目状态，可选 `problem_id`）。
>   身份只来自后端验证后的当前用户（token + 按 `sub` 回查数据库），客户端传入的
>   `user_id` 等字段一律忽略；历史接口无论是否携带 `mine` 都只返回本人记录，`mine`
>   以参数是否出现解析（`?mine` 空值按本人历史处理），未提供时默认本人历史并记录日志，
>   显式非法取值返回 `400`。
> - **列表摘要**：仅提交 ID、题目 ID、题目标题、语言、状态、耗时、内存与原提交时间；
>   不加载源码、逐点 JSON、编译信息或 WA 用例详情。列表与总数使用相同身份与筛选条件，
>   每页固定 20，参数非法 `400`，不一次返回全部；按查询需要新增
>   `(user_id, created_at DESC, id DESC)` 索引；读取不修改提交、计数或做题状态。
> - **详情**：普通用户仅本人记录，管理员按现有管理员权限（数据库最新角色 + 首改标记）
>   查看他人记录；不存在与无权访问统一 `404`，不泄露他人源码/提交信息。返回数据库
>   保存的源码、语言、状态、编译信息、逐点结果、运行指标与原提交时间；复用既有结果
>   序列化的逐点结构，**不重新判题、不按当前 testcases 重拼历史 WA**，Rejudge 后展示
>   原记录当前保存结果。未持久化指标（如编译耗时）省略并显示「未采集」，不把 `null`
>   显示为真实的 `0`；逐点 JSON 损坏时以 `per_case_parse_error` 标记不可解析，不伪装
>   成 AC 或正常空结果。WA 对比只返回已授权保存结果，通过点不额外附带隐藏输入或答案。
> - **题目可见性（本次确定）**：区分「访问当前题目」与「读取本人历史提交」。本人提交
>   历史、详情（含保存的 WA 对比）与本人状态属用户自身数据，不因题目后来隐藏而收回；
>   但题面/公开样例/当前隐藏用例仍按题目可见性规则控制，历史/状态接口不下发题面或
>   隐藏用例，历史中的题目链接仍由题目接口执行可见性检查（隐藏题显示不存在且页面不
>   崩溃），不能通过提交详情绕过题面权限。
> - **在途任务（沿用 M3.7）**：在途记录与终态提交明确区分，在途不进入历史、不显示为
>   AC、不计入已完成统计；未新增状态枚举、未改变恢复结算规则。
> - **状态接口**：返回 `accepted`/`none`、`first_ac_at`、`submit_count`；无记录不返回
>   表示该题从未提交（前端按未 AC 显示），接口不为未提交题目创造状态行；只返回本人
>   记录，不返回隐藏题目额外资料。详情页与 `GET /api/status?problem_id=` 合并展示；
>   题目列表/题目页继续复用各自响应内的 `solved`，不额外重复查询，保持 M4.2/M4.3 兼容。
> - **页面**：hash 路由新增 `#/submissions` 与 `#/submissions/{id}` 并接入导航（登录后
>   显示「提交历史」）；复用 M4.1 登录检查/错误处理/登录后返回与 M4.3 `renderJudgeResult`。
>   列表展示提交 ID、题目、语言、状态、耗时、内存与时间，支持分页与题目筛选，查询条件
>   承载于 hash 并可前进后退恢复；详情以只读文本区域展示完整源码（不提供提交入口，
>   浏览历史不会误提交），按实际持久化字段显示指标，长源码/多测试点可滚动，全部纯文本
>   渲染；提供加载中/无历史/无权访问/不存在/读取失败状态，快速翻页或切换详情按代次丢弃
>   旧响应，退出登录清除受保护内容。管理员可复用已有重判入口，在详情页就地重判，成功后
>   重新读取详情与状态，失败时保留原结果展示。
> - **范围**：未进入 M4.5，不实现排行榜、源码编辑重提、批量重判、结果版本历史或导出；
>   未新增无关接口或将历史结果改为重新判题。

> **已通过独立测试验证**：新增单元 `tests/unit/test_m44_submission_history_unit.cpp`
> （7 用例，覆盖 `SubmissionStore::list_by_user` 的分页/最新优先/用户隔离/题目筛选/
> 摘要字段与 `UserProblemStatusStore::list_by_user` 的排序/字段/筛选/隔离）；集成
> `tests/integration/test_m44_history_api.cpp`（90 项断言，覆盖历史 `mine` 解析/身份
> 不可指定/分页校验/题目筛选、详情本人与管理员权限/首改规则/越权与非法 ID/损坏
> `per_case`/更新后保存结果/隐藏题可见性、`/api/status` 字段与隔离）；页面级
> `tests/frontend/m44_history_dom.mjs` + `run_m44.sh`（jsdom，31 项断言，覆盖历史列表与
> 分页、题目筛选空态、详情只读源码与结果组件/无提交入口、404/越权/游客重定向/退出清理）。
> 全量常规回归 `ctest --parallel 1` **43/43 通过**；旧前端回归 M4.1 逻辑 78/78、M4.1 DOM
> 53/53（其中 1 处为 M4.4 需求导致的旧断言更新）、M2.5 后台 128/128、M4.3 页面 54/54 通过。
> 审查过程中未发现 M4.4 实现缺陷；失败项均为测试脚本缺陷或已确认的需求变更。完整逐项
> 结果见 `tests/M4.4-test-report.md`。

#### M4.5 排行榜

- [x] 基于已维护的统计数据实现排行榜查询，按 AC 数降序、总提交次数升序、首次 AC 时间戳升序、注册时间升序排列。
- [x] 实现 `GET /api/leaderboard`。
- [x] 实现排行榜页面。
- [x] 验证重复 AC、失败提交及 Rejudge 后的统计和排序。（已通过独立测试验证）

> 实施说明（M4.5 已实现并已通过独立测试验证）：
>
> - **排名对象（本次确定）**：仅统计至少有 1 次已结算提交的用户；从未提交的用户
>   不出现在公开榜。管理员按普通用户口径参与排名（与「管理员也可做题」一致，无特殊
>   排除）。隐藏题目（`problems.visible=0`）的 AC 与提交均不计入公开榜。
> - **统计来源（复用既有持久化）**：`LeaderboardStore`（`src/db/leaderboard.{h,cpp}`）
>   基于 `user_problem_status`（AC 状态、`first_ac_at`、`submit_count`）与
>   `users.created_at`（注册时间）聚合，不新增用户汇总字段、不引入缓存或后台统计
>   服务，重启后可由 SQLite 恢复。AC 数按 accepted 的**不同题目**计数（重复 AC 不
>   重复增加）；总提交次数为可见题目上的 `submit_count` 合计，沿用既有结算规则，
>   含 CE/WA/TLE/RE/MLE/SYSERR 等失败提交，不因 Rejudge 增加；首次 AC 时间取当前
>   仍有效 accepted 题目中最早的 `first_ac_at`（沿用 M3.6/M3.7 已有约定）。
> - **边界与一致性**：未接收请求、未结算在途任务、未执行测试点不参与统计；读取
>   排行榜不修改记录、不重新判题、不触发补计数。查询以单个 GROUP BY 聚合子查询先
>   聚合再与 `users` 一对一连结，避免多表连接放大 AC 数/提交次数；使用短只读事务，
>   使 `total` 与当前页来自同一数据视图，且不长期持有覆盖判题执行的事务；全部参数
>   绑定，沿用 `UNIQUE(user_id, problem_id)` 等既有索引，不逐用户额外查询、不加载源码。
> - **排序与 null 规则**：AC 数降序 → 总提交次数升序 → 首次 AC 时间升序 → 注册时间
>   升序 → 用户 ID 升序（稳定最终键）。首次 AC 时间为空者排在有值者之后（显式
>   `IS NULL` 判定，不依赖数据库默认行为）；无 AC 用户之间继续按规定比较提交次数、
>   注册时间与 ID。null 时间响应为 `null`，前端显示「—」，不用注册时间/当前时间/0
>   伪造。
> - **接口**：`GET /api/leaderboard`（公开，游客可访问）返回 `leaderboard` 数组
>   （`rank` 全局名次、`nickname`、`ac_count`、`submit_count`、`first_ac_at`、
>   `created_at`）以及 `page`/`page_size`（固定 20）/`total`/`total_pages`。分页在
>   全局排序后进行，名次不按页从 1 重置；`page` 沿用项目约定（默认 1、正整数
>   `1..1000000`，非法 `400`）。只返回展示所需字段，不返回账号、密码哈希、token、
>   源码或逐点结果；`user_id` 仅用于前端可靠高亮当前用户，不通过昵称猜测身份。
> - **页面**：hash 路由 `#/leaderboard` 接入导航（游客与登录用户均可见），复用
>   M4.1 的路由/身份/错误处理/生命周期；展示名次、昵称、AC 题目数、总提交次数与首次
>   AC 时间（无 AC 显示「—」），提供分页、加载中、空结果与请求失败（含重试/刷新）；
>   快速翻页或离开页面按代次丢弃旧响应，进入页面或主动刷新时获取，当前用户仅在
>   `user_id` 可靠匹配时高亮，退出登录后不残留。昵称等文本一律纯文本渲染。
> - **范围**：不增加竞赛榜、积分系统、时间段榜单、奖章或批量重算平台；不改动已确认
>   的提交计数、Rejudge 与崩溃恢复规则。
> - **独立测试验证**：新增单元 `tests/unit/test_m45_leaderboard_unit.cpp`（9 用例，
>   覆盖重复 AC/失败提交/AC 后失败、Rejudge 重算、排序级联、null 首次 AC、隐藏题
>   排除、仅已结算用户、管理员同口径、分页）与集成
>   `tests/integration/test_m45_leaderboard_api.cpp`（4 场景 / 52 项断言，覆盖公开
>   访问、排序与全局名次、分页与参数校验、字段范围、隐藏题排除与 Rejudge 联动）；
>   页面级 `tests/frontend/m45_leaderboard_dom.mjs` + `run_m45.sh`（jsdom，23 项）。
>   全量常规回归 `ctest --parallel 1` **45/45** 通过。完整逐项结果见
>   `tests/M4.5-test-report.md`。真实图形浏览器渲染与真机窄视口仍属未验证项。

### M5 安全与异常补全

#### M5.1 账号与认证回归

- [x] 补全密码策略、输入边界及唯一性冲突处理。
- [x] 验证并发注册及账号永久不复用要求。
- [x] 验证登录限速及限制解除行为。
- [x] 验证伪造、篡改、过期 JWT 被拒绝。
- [x] 验证 admin 首次改密限制无法通过直接请求绕过。
- [x] 检查角色修改、密码重置后的认证与权限行为。

> **已通过回归验证**：新增集成 `tests/integration/test_m51_auth_regression.cpp`
> （10 场景 / 72 项断言）补充既有测试未覆盖的边界：登录限速不信任 `X-Forwarded-For`/
> `X-Real-IP`、成功登录清除失败计数、受控并发不可绕过、窗口边界（900s 仍受限、901s
> 解除）；昵称规范化唯一性与内部空白、密码不裁剪不截断（含纯空白）、昵称按字节计长；
> 并发不同昵称注册账号唯一无部分记录；账号永久不复用（`users.account` UNIQUE 约束 +
> 重启后保留）；失败响应不泄露哈希/明文。审查轮在 `test_register_gtest` 补充「昵称按
> 字节计长（10/11 个多字节字符）」与「去空白后再判长度」，在 `test_rate_limit_gtest`
> 补充「窗口边界（恰好一个窗口仍受限、超过后解除）」单元用例；既有认证测试（`jwt_gtest`、
> `login_gtest`、`register_api`、`login_api`、`password_unit`、`password_api`、
> `admin_users_api`）与前端逻辑 `m41_logic_test.mjs`（78/78）复用通过；
> JWT 缺失/过短密钥启动退出以操作检查验证（exit=1，明确报错，无默认密钥）。
> 全量常规回归 `ctest --parallel 1` **46/46 通过**；本轮未发现业务实现缺陷，仅修正 1 处
> 测试断言（扩展结果码）。待澄清项：`README.md:770` 昵称长度未标明单位（实现按字节计，
> 错误文案写「个字符」），已如实记录、未擅自修改规则。完整逐项结果见
> `tests/M5.1-test-report.md`；`M5.2`/`M5.3` 仍未开始。

#### M5.2 数据访问与权限回归

- [x] 检查所有管理员接口的权限校验。
- [x] 验证学生不能访问他人的提交与源码。
- [x] 验证不可见题目不能被学生直接访问或提交。
- [x] 验证学生仅能通过本人 WA 反馈获取规定的失败测试点详情，题面及其他通过点不泄露隐藏用例。
- [x] 验证非法参数、不存在的记录及错误关联 ID 的处理。
- [x] 验证删题、删用例及 Rejudge 的数据一致性。

> **已通过回归验证**：新增集成 `tests/integration/test_m52_access_control.cpp`（14 场景 /
> 238 项断言），以隔离临时库 + 随机端口 + 可控执行器覆盖：全部 10 个管理员入口的四类身份
> 矩阵（游客 401 / 普通用户 403 / 未完成首改管理员 403 + `PASSWORD_CHANGE_REQUIRED` /
> 有权限管理员成功），并补齐既有覆盖缺口（`PUT /api/admin/problems/{id}` 未改密管理员、
> `PUT /api/admin/users` 游客）；提交归属跨用户 404 且不泄露源码/隐藏用例、`user_id`/
> `account`/`role` 参数不能扩大范围、未改密管理员按普通处理、改密后管理员可读；
> `GET /api/problem-tags` 不泄露隐藏题标签（该接口首个 C++ 测试）；题目转隐藏后本人历史/
> 详情/状态保留而题面与新提交按规则拦截；用例归属（A 题路径 + B 题用例 ID 404）、
> `problem_id`/`is_sample`/`id` 客户端字段被忽略、隐藏用例不可转公开样例；非法/越界/溢出
> ID、非法 JSON、>1 MiB 请求体（413）与错误不泄露、无部分写入；提交/建题/重判的
> `user_id`/`role`/`status`/`created_at` 等服务端字段不可篡改、角色撤销后旧 token 立即失权；
> 删题在有提交/在途任务时 409、无提交时事务内清理关联且不误删其它题目与学生历史；
> 用例增删改按 `(ord,id)` 判题、公开样例不受影响、历史结果不被静默改写；Rejudge 经真实
> HTTP 更新原记录、不增次数并联动通过人数/排行榜/首次 AC；受控并发（Rejudge vs 新提交、
> 删题 vs 提交结算）关联约束与统计一致。测试审查轮补充：在途任务 `claimed`/`pending`
> 状态下删题均 409、管理员用例读取返回完整隐藏用例与 `is_sample=false`、10 个管理员入口
> 携带伪造 Bearer 均 401，并复核既有断言无空断言/恒真断言。全量常规回归
> `ctest --parallel 1` **47/47 通过**（约 232s，单并发）。本轮未发现业务实现缺陷，仅修正
> 2 处新增测试自身缺陷（临时 JSON 悬垂引用、样例参与判题的预期设定）。歧义记录：公开样例
> 是否作为判题测试点 SPEC 未明确，当前实现将全部 `testcases` 参与判题（样例内容本就公开，
> 不构成隐藏用例泄露，未改动）。删题 vs 崩溃恢复以确定性在途状态边界覆盖，未构造运行期恢复
> 线程并发（恢复为启动流程，不为测试改产品代码）。完整逐项结果见 `tests/M5.2-test-report.md`；
> `M5.3` 仍未开始。

#### M5.3 判题异常与安全回归

- [x] 验证死循环、崩溃、越界、超内存和超大输出。
- [x] 验证 fork 轰炸、网络访问、文件读写等恶意提交被阻止。
- [x] 验证单次恶意提交不影响后续正常判题。
- [x] 验证编译环境故障及其他 SYSERR 路径。
- [x] 验证线程池满载和并发提交无假死。
- [x] 验证运行中停止服务的资源回收及记录处理。

> **已通过测试审查、覆盖补充与阶段回归验证**：本轮未改动任何 `src/` 生产代码，
> 仅新增测试与构建注册。新增常规集成 `tests/integration/test_m53_judge_abuse.cpp`
> （21 项断言，注册为 CTest `m53_judge_abuse`）补足既有覆盖缺口：不读取大输入/
> 提前关闭 stdin 的 EPIPE 处理（服务不因 SIGPIPE 退出）、分配失败/超大申请不得
> 误判 MLE、clone/vfork 进程创建变体被拒、向宿主进程发信号被命名空间隔离拒绝、
> 异常序列后两套语言恢复正常。新增特殊（破坏性类别）集成
> `tests/integration/test_m53_special.cpp`（执行 15 项断言 + 1 项主动跳过），包含
> 持续无限输出 TLE、受控超内存 MLE、有限次 fork 尝试零后代，以及默认跳过的
> 宿主机内存压力场景；该目标**有意不注册到 CTest**（避免常规回归误触发），通过
> 自定义目标 `run_m53_special` 显式执行，并自带可用内存自检。**复审补充**：审查发现
> 既有新增测试集中在引擎层，缺少经真实提交接口的服务级覆盖，故新增服务级集成
> `tests/integration/test_m53_service_abuse.cpp`（30 项断言，注册为 CTest
> `m53_service_abuse`），经真实 HTTP + JudgeManager + SubmitService + SQLite 验证
> 网络/文件/进程创建隔离尝试、真实 MLE（`reason=memory_exceeded` + 峰值 RSS 证据）、
> 真实超大输出截断与恶意序列后恢复。审查未发现生产实现
> 缺陷，仅修正 2 处新增测试自身缺陷（C11 源码误用 `<cstdio>`、占位断言）。
> 全量常规阶段回归 `ctest --parallel 1` **49/49 通过**（约 240s，单并发），
> `ctest -N` 确认特殊目标未混入常规回归；测试全程串行、可用内存最低约 610 MiB，
> 结束后无遗留子进程与任务目录。既有 M3.1～M3.7、M4.x、M5.1、M5.2 测试在本轮
> 阶段回归中复用重跑通过。**第三轮补充（tmpfs）**：在非特权 user/mount namespace
> 内于生产路径 `/opt/oj-tmpfs` 挂载真实内核 tmpfs（namespaced，无系统级改动），
> 重跑完整 `sandbox_integration`（`TMPDIR=/opt/oj-tmpfs`）通过，并以
> `OJ_JUDGE_WORKSPACE=/opt/oj-tmpfs` 真实启动服务，验证 `path_is_tmpfs` 与沙箱自检
> 通过、健康检查可用、优雅退出。**第四轮补充（持久挂载）**：以 systemd mount unit
> （规范 unit 名 `opt-oj\x2dtmpfs.mount`）在 `/opt/oj-tmpfs` 落地**系统级持久 tmpfs**，
> 实测 `enabled` + `active/mounted`；服务以默认工作目录启动并通过 `path_is_tmpfs`
> 与沙箱自检，真实 AC/WA/CE 提交端到端通过。**如实记录的边界**：真正无约束的 fork
> 轰炸、宿主机内存耗尽与填满共享 tmpfs 测试未执行（受控样例替代，非“沙箱绝对安全”
> 证明）；内存限制为 20ms 周期 RSS 采样，采样间隔内的瞬时超限不保证被检测；
> **真实重启主机**验证开机自动挂载未执行（禁止擅自重启），以 `enabled` + unit 有效
> 作为等价证据。完整逐项结果见 `tests/M5.3-test-report.md`。

> **M5 阶段结论**：M5.1、M5.2、M5.3 均已完成独立验证并纳入阶段全量常规回归，
> 判题异常与安全相关的既有实现（异常分类、资源限制、隔离、调度/停止/恢复）在
> 受控范围内通过验证；`/opt/oj-tmpfs` 的判题与启动路径已由 M5.3 第三/四轮补验：
> namespaced 真实 tmpfs（第 13 节）与 systemd 持久挂载 `opt-oj\x2dtmpfs.mount`
> （第 14 节，`enabled`+`active`，真实 AC/WA/CE 端到端通过）。
> 未验证项（无约束破坏性压力、真实重启主机验证开机自动挂载、真实浏览器
> 渲染等）已在各报告中列明，不据此宣称“沙箱绝对安全”。

### M6 测试与交付

#### M6.1 自动化测试

- [x] 整理判题核心单元测试，共 5～10 个用例，覆盖输出比对和状态边界。
- [x] 覆盖 C++17、C11 的已知 AC/WA 样例。
- [x] 编写 `scripts/regression.sh`，完成启动、提交和状态断言。
- [x] 汇总执行已有认证、权限和持久化回归测试。

> 实施说明（已通过测试审查、补充与阶段全量常规回归验证）：测试使用隔离数据，避免破坏
> 正式数据库；复用各阶段已有测试，不重复编写相同用例。本轮未改动 `src/` 生产代码，
> 仅新增回归脚本与文档。
>
> - **判题核心单元测试（输出比对 + 状态边界）**：核心逻辑已存在且有效，集中在
>   `tests/unit/test_judge_unit.cpp`（纯函数比对与 `JudgeEngine` 编排）、
>   `tests/unit/test_judge_classification.cpp`（单点分类与汇总优先级）、
>   `tests/unit/test_judge_deadline.cpp`（全局硬上限、编译超时、取消）。其中直接对应
>   本条的**核心集合**为：输出比对 8 项（完全匹配、行尾空白/制表符、`\r\n`、文末空行、
>   行首空白显著、行内空白显著、中间空行保留、空输出/纯空白）+ 汇总与状态边界 4 项
>   （空集合 `SYSERR`、全 AC 为 `AC`、WA 压过 AC、`SYSERR > TLE > MLE > RE > WA`
>   顺序无关）；`test_judge_classification.cpp` 另以 12 项覆盖 AC/WA/CE/TLE/RE/MLE/
>   SYSERR 的确定性优先级、多迹象、缺失内存证据不误判 MLE、Sanitizer 文本不单独判失败。
>   **数量约定说明**：本条原写「共 5～10 个用例」，实际整理后核心集合已达 12 项，
>   加上编排/分类/截止时间扩展测试远超该区间。按「不为满足数量而删除有价值的测试或
>   合并独有断言」的要求，保留全部断言并明确记录此数量偏离；未静默修改需求。
> - **C++17 / C11 已知 AC/WA**：真实编译链路由 `judge_integration`（真实 g++/gcc 的
>   AC/WA/CE）、`m34_classification`（默认 ASan/UBSan 模板的 AC/WA/CE 与异常分类）、
>   `submit_api`（HTTP + 持久化）覆盖；本轮新增 `scripts/regression.sh` 在真实 HTTP 上
>   对两套语言各提交一个已知正确与一个已知错误程序，断言 HTTP 200 与 JSON
>   `status` 分别为 `AC`/`WA`，并区分「请求失败」与「判题结果」。
> - **`scripts/regression.sh`**：端到端冒烟回归入口（非全量测试入口）。独立临时库、
>   随机测试 JWT 密钥与测试管理员密码、空闲端口、优先 `/opt/oj-tmpfs`→`/dev/shm` 的
>   tmpfs 判题目录；`--seed` 复用既有种子题初始化，经真实注册/登录/鉴权/提交接口完成
>   链路；有截止时间的健康检查、统一清理（只停本轮 PID，先停服务再删数据）、退出码
>   语义（全通过 0，任一断言失败或启动失败非零）。实测正常路径 **23/23 断言通过、
>   退出码 0**；另以非 tmpfs 工作目录实测「启动失败非零退出并保留诊断、清理无残留」，
>   以 `OJ_REGRESSION_INJECT_FAILURE=1` 实测「断言失败非零退出并清理」。完整服务日志
>   保留在 `build/regression-logs/<时间戳>/server.log`，不泄露密钥/token。本轮已修复
>   脚本自身一处显示缺陷（工作目录文件系统标识误标）。
> - **认证/权限/持久化/Rejudge/在途恢复回归接入**：`register_api`、`login_api`、
>   `password_api`、`m51_auth_regression`（认证/限速/JWT/账号不复用）、
>   `m52_access_control`（全部管理员入口四类身份矩阵、越权与隐私、删题/用例/重判一致
>   性）、`m35_persistence_shutdown`（持久化与停止清理）、`rejudge_api`、`m37_recovery_api`
>   （在途持久化与崩溃恢复）均已注册于 CTest，本轮全量常规回归复用重跑通过，未复制
>   相同场景。
> - **实际执行**：构建 `cmake --build build --parallel 1`（退出码 0，增量约 1.5s，峰值
>   RSS 约 17 MiB）；全量常规回归
>   `ctest --test-dir build --parallel 1 --output-on-failure --timeout 120`
>   **49/49 通过（退出码 0，约 241.8s）**；执行期间实测可用内存最低约 709 MiB、
>   I/O 等待最高约 7，结束后无遗留 `oj_server`/编译器进程与判题目录。破坏性/压力测试
>   （`oj_m53_special`）保持独立、不注册 CTest、本轮未执行。真实图形浏览器渲染与真机
>   窄视口仍为未验证项。
>
> 说明：本轮为 M6.1 自动化测试，不实现 M6.2 备份恢复、M6.3 部署文档或 M6.4 全部验收；
> 冒烟回归通过不代表整个项目已完成交付。

#### M6.2 备份与恢复

- [x] 编写 `scripts/backup.sh`，使用 `sqlite3 .dump` 生成 `backup/oj-YYYYMMDD.sql`。
- [x] 编写 cron 配置说明。
- [x] 实际验证备份文件可以恢复数据库。

> 实施说明（M6.2 已实现并通过独立测试验证，见 `tests/M6.2-test-report.md`）：
>
> - **备份脚本**（`scripts/backup.sh`，可执行）：用 `sqlite3 .dump` 导出为 SQL 文本，默认写入
>   仓库内 `backup/oj-YYYYMMDD.sql`；源库 `--db`/`OJ_DB`（默认 `data/oj.db`）、备份目录
>   `--out-dir`/`OJ_BACKUP_DIR`（默认 `backup/`）。脚本自解析仓库根、路径不依赖调用者当前
>   目录，命令与目录均按含空格处理；先确认源库存在、为普通文件且可读，避免路径写错时
>   SQLite 自动建空库后误报成功；所需命令（`sqlite3`、`flock`、`timeout`、`mktemp`、`stat` 等）
>   缺失时明确失败。
> - **一致性与 WAL**：通过 SQLite 连接读取（包含 WAL 中已提交数据），不复制主库文件、不删除
>   或移动运行中的 `-wal`/`-shm`；在单连接内 `BEGIN;` → `.dump` → `COMMIT;`，多张表来自同一读
>   快照；WAL 模式下读事务不阻塞业务写入（仅可能推迟检查点）。设置 `.timeout` 锁等待
>   （`OJ_BACKUP_BUSY_TIMEOUT_MS`，默认 5000ms）与 `timeout` 总执行超时（`OJ_BACKUP_TIMEOUT`，
>   默认 600s），错误/超时明确退出。
> - **原子写入与并发**：先写备份目录内临时文件，校验 sqlite3 退出码为 0、文件非空、含表结构
>   且以 `COMMIT;` 正常收尾，全部满足才 `mv` 原子发布；失败清理本次临时文件、不留下看似可用
>   的正式文件。同一备份目录用 `flock` 互斥（等待 `OJ_BACKUP_LOCK_WAIT`，默认 30s，超时失败）；
>   同日重复执行默认在新备份完成后原子替换，`--no-replace`/`OJ_BACKUP_NO_REPLACE=1` 则跳过；
>   临时文件名含日期与随机后缀。只操作本次拥有且确认的路径，不批量删除备份目录文件。
> - **安全与输出**：文件权限 `0600`，位于 `backup/`（已被 `.gitignore` 忽略，仅保留
>   `backup/.gitkeep` 占位），不在 `web/` 托管范围、不提交 Git；流式写入不把导出内容读入内存；
>   磁盘空间预检不足即失败且不自动清理其它数据；只输出开始/结束/目标/大小/表数/耗时与失败
>   原因，不打印 SQL、密码哈希、源码或隐藏用例；成功返回 0、失败返回非零。本阶段不自动删除
>   历史备份、不设置保留天数、不上传云端。
> - **日期时区**：文件名日期默认使用服务器本地时区，可用 `OJ_BACKUP_TZ`（如 `Asia/Shanghai`、
>   `UTC`）显式指定，并在日志中打印实际时区与偏移。
> - **cron 与恢复说明**（`README.md`「备份与恢复（M6.2）」）：给出可调整的 cron 示例（执行时间、
>   时区、运行账号、绝对路径、`PATH`/`OJ_BACKUP_TZ` 等环境、日志位置），说明手动调用、查看退出码
>   与日志、确认定时任务实际运行的方法，以及执行账号的最小数据库/目录权限（推荐与服务的同一账号、
>   不以 root 运行、定时执行同样遵守并发锁与资源限制）。恢复说明要求先恢复到**新建隔离库**，
>   给出导入退出码、`integrity_check`、`foreign_key_check` 与关键业务数据（用户、题目、样例/隐藏
>   用例、完整提交源码、判题结果、用户题目状态、`in_flight_tasks`）的核对方法（本轮不执行）；
>   区分 SQL 逻辑备份与运行配置（保留表/索引/约束/序列，不保留连接级 PRAGMA、JWT 密钥、服务
>   配置或外部文件），说明首次打开时的幂等迁移不重置管理员密码/角色/种子数据；特别说明 M3.7 的
>   在途任务恢复行为（可能重新入队判题、仅为查看数据不可启动服务、快照后新提交不在备份中），并
>   给出正式恢复顺序（先隔离验证、再停服务、保留旧数据与回退路径、最后切换，且不混用旧
>   WAL/SHM 与恢复库）。本阶段不提供自动覆盖正式库的一键恢复命令。
> - **独立测试验证**：新增集成测试 `tests/scripts/test_backup_restore.sh`（注册为 CTest
>   `m62_backup_restore`，**117 项断言**）并全量常规回归 `ctest --parallel 1` **50/50 通过**（约
>   245s，最低单次可用内存约 452 MiB、未触及 256 MiB 停止线）。验证覆盖：真实 `oj_server --seed`
>   建库后备份并在全新隔离库恢复，`integrity_check=ok`、`foreign_key_check` 无输出，六表与
>   样例/隐藏用例计数一致，提交源码/逐点结果/索引/`CHECK` 约束/`AUTOINCREMENT` 序列/
>   `in_flight_tasks` 原样保留；仅存于 WAL 的已提交数据被包含且 `-wal`/`-shm` 未被删除或移动；
>   源库缺失/目录/非库/空库/非法参数/非法环境/不可写目录/磁盘不足/并发锁等错误路径返回约定
>   非零码且不留正式文件；同日默认替换、`--no-replace`、失败保留旧备份、含空格路径与脱离 cwd、
>   时区、SIGTERM 中断清理、忽略规则均已验证。本轮另补验：备份进行中并发写入不被长时间阻塞
>   （存活期完成 33 次写入、峰值 55ms）；SIGKILL 备份进程不产生正式文件、源库完整、锁由 OS
>   释放，提交后 SIGKILL 写连接仍可恢复已提交数据（进程崩溃，非整机断电）；将含 `pending` 在途
>   任务的备份恢复到新库并隔离启动服务，观测到 M3.7 启动恢复重新入队并结算（在途清空、提交 +1、
>   终态 WA）；以最小 `env -i` 环境与临时 crontab 验证 cron 最小环境调用及真实守护进程调度。
>   完整逐项结果见 `tests/M6.2-test-report.md`。

#### M6.3 部署文档

- [ ] 更新 `dependence.md` 中的依赖安装说明，并在 README 中说明编译与 Linux 部署步骤。
- [ ] 在 README 中整理一键启动方式，随启动步骤说明必要的故障处理。
- [ ] 编写配置项、默认管理员账号及首次登录改密说明。
- [ ] 编写数据位置与备份恢复说明。
- [ ] 检查 Git 历史清晰、交付文件完整，排除敏感配置和运行临时产物。

#### M6.4 最终验收

- [ ] 按第 5 节 A 验证完整业务流程。
- [ ] 按第 5 节 B 验证判题正确性。
- [ ] 按第 5 节 C 验证工程与交付。
- [ ] 按第 5 节 D 验证安全及性能。

> 验收时记录通过情况、已知限制及未完成事项，作为验收记录保留，不另设独立交付文档。

---

## 5. 验收标准（Acceptance Criteria）

### A. 端到端业务流程（手动验收）
1. 首次访问可注册：返回 10 位数字账号；同一账号不能被重复分配；重复昵称被拒。
2. admin 首登强制改密后才能进入后台。
3. 管理员创建题目（含多组隐藏用例、时限/内存、难度、标签）→ 学生列表可见、可搜索筛选。
4. 学生提交 AC 答案 → 列表题目标记为已 AC，排行榜见 AC 数 +1。
5. 断网/重启服务后，题目、提交记录、源码、做题状态、排行榜全部完好（从 SQLite 恢复）。

### B. 判题正确性（单元测试 + 回归脚本）
6. 已知 AC 样例 → `AC`；已知 WA 样例 → `WA`；两套语言（g++/gcc）各验证一次。
7. 输出末尾多余空行/行尾空格 → 仍算 `AC`。
8. 死循环代码 → `TLE`，且判题机主体不受影响（恢复正常提交）。
9. 越界/野指针代码（ASan 生效）→ `RE`（采集到错误信息）。
10. 超内存代码 → `MLE`；超大输出 → 截断/`RE`，不撑爆磁盘。
11. 编译错误代码 → 返回 `CE` 与编译器信息。
12. 判题线程池满载时并发提交仍全部在合理时间内返回，无假死。
13. 伪造/篡改 token 的请求被拒绝（401）；学生无法访问他人提交详情。

### C. 工程与交付
14. `scripts/regression.sh` 一键运行通过（绿色输出）。
15. 判题单元测试 `tests/` 全部通过。
16. `scripts/backup.sh` 生成备份文件，且能从备份恢复数据库。
17. `git` 仓库历史清晰、`README` 描述启动步骤与默认管理员账号。

### D. 非功能抽查
18. 恶意代码（尝试 `fork` 轰炸 / 网络连接 / 读 `/etc/passwd` / 写文件）被沙箱阻止，只影响自身子进程，返回 `RE`/`SYSERR`，系统其余功能正常。
19. 单次典型提交端到端耗时 < 数秒（教学规模体感可接受）。